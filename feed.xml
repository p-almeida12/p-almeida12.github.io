<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://p-almeida12.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://p-almeida12.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-05-13T13:35:08+00:00</updated><id>https://p-almeida12.github.io/feed.xml</id><title type="html">blank</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">JVM Garbage Collection - What Actually Matters in Production?</title><link href="https://p-almeida12.github.io/blog/2026/garbage-collector/" rel="alternate" type="text/html" title="JVM Garbage Collection - What Actually Matters in Production?"/><published>2026-02-25T14:59:00+00:00</published><updated>2026-02-25T14:59:00+00:00</updated><id>https://p-almeida12.github.io/blog/2026/garbage-collector</id><content type="html" xml:base="https://p-almeida12.github.io/blog/2026/garbage-collector/"><![CDATA[<p><span style="margin-left: 10px;"></span> Garbage collection is not just JVM internals. For a Java backend developer, GC is connected to request latency, allocation patterns, object lifetime, framework behavior, container memory limits, observability, and incident response.</p> <h2 id="from-http-request-to-heap-allocation">From HTTP Request to Heap Allocation</h2> <p><span style="margin-left: 10px;"></span> When a request enters a Java backend service, it does not only consume CPU and network resources. It also creates objects.</p> <p><span style="margin-left: 10px;"></span> A lot of them!!!</p> <p><span style="margin-left: 10px;"></span> For a single request, that may not matter. But in a production service handling hundreds or thousands of requests per second, those allocations become one of the main drivers of garbage collection behavior.</p> <h3 id="a-request-is-an-allocation-pipeline">A Request Is an Allocation Pipeline</h3> <p><span style="margin-left: 10px;"></span> Considering a typical Spring Boot REST API request:</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@PostMapping</span><span class="o">(</span><span class="s">"/beers"</span><span class="o">)</span>
<span class="kd">public</span> <span class="nc">BeerResponse</span> <span class="nf">createBeer</span><span class="o">(</span><span class="nd">@RequestBody</span> <span class="nc">CreateBeerRequest</span> <span class="n">request</span><span class="o">)</span> <span class="o">{</span>
    <span class="nc">Beer</span> <span class="n">beer</span> <span class="o">=</span> <span class="n">beerService</span><span class="o">.</span><span class="na">createBeer</span><span class="o">(</span><span class="n">request</span><span class="o">);</span>
    <span class="k">return</span> <span class="n">beerMapper</span><span class="o">.</span><span class="na">toResponse</span><span class="o">(</span><span class="n">beer</span><span class="o">);</span>
<span class="o">}</span>
</code></pre></div></div> <p><span style="margin-left: 10px;"></span> This may look simple, but several allocations happen before and after this method runs.</p> <p><span style="margin-left: 10px;"></span> A typical request may involve:</p> <ul> <li>request and response DTOs</li> <li>service-layer objects</li> <li>domain entities</li> <li>collections and strings</li> <li>logging messages</li> <li>database result objects</li> <li>etc…</li> </ul> <p><span style="margin-left: 10px;"></span> Most of these objects are short-lived. They exist only during the lifetime of the request.</p> <p><span style="margin-left: 10px;"></span> That is good. The JVM is optimized for this pattern.</p> <p><span style="margin-left: 10px;"></span> But at scale, even short-lived objects can become expensive.</p> <h3 id="backend-services-allocate-constantly">Backend Services Allocate Constantly</h3> <p><span style="margin-left: 10px;"></span> A Java backend service is usually allocation-heavy by design.</p> <p><span style="margin-left: 10px;"></span> Frameworks like Spring, Hibernate, Jackson, Bean Validation, Micrometer, logging libraries, and HTTP servers create many temporary objects to provide abstraction and developer productivity.</p> <p><span style="margin-left: 10px;"></span> That is not automatically a bad thing, the problem starts when allocation rate becomes too high.</p> <p><span style="margin-left: 10px;"></span> Allocation rate means how much memory the application allocates per second.</p> <p><span style="margin-left: 10px;"></span> For example:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>500 MB/s allocation rate
</code></pre></div></div> <p><span style="margin-left: 10px;"></span> This means that the application is creating half a gigabyte of new objects every second.</p> <p><span style="margin-left: 10px;"></span> Even if most objects die quickly, the garbage collector still needs to process that memory. This is why allocation rate is often more important than total heap usage. A service can have plenty of free heap and still suffer from frequent garbage collections because it allocates too aggressively.</p> <h3 id="most-request-objects-should-die-young">Most Request Objects Should Die Young</h3> <p><span style="margin-left: 10px;"></span> Modern JVM garbage collectors rely heavily on the hypothesis that most objects die young. This fits backend applications very well. For example, these objects usually die quickly:</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">CreateBeerRequest</span> <span class="n">request</span>
<span class="nc">BeerResponse</span> <span class="n">response</span>
<span class="nc">List</span><span class="o">&lt;</span><span class="nc">BeerBreweryResponse</span><span class="o">&gt;</span> <span class="n">breweries</span>
<span class="nc">String</span> <span class="n">formattedMessage</span>
<span class="nc">Map</span><span class="o">&lt;</span><span class="nc">String</span><span class="o">,</span> <span class="nc">Object</span><span class="o">&gt;</span> <span class="n">logContext</span>
</code></pre></div></div> <p><span style="margin-left: 10px;"></span> They are created during request processing and become unreachable shortly after the response is sent. These objects are usually allocated in the young generation, specifically in Eden space. When Eden fills up, the JVM performs a young GC to remove dead objects. This is normally cheap because most young objects are already garbage. That is the happy path :)</p> <p><span style="margin-left: 10px;"></span> Problems start when objects that should be short-lived accidentally survive.</p> <p><span style="margin-left: 10px;"></span> For example:</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">private</span> <span class="kd">static</span> <span class="kd">final</span> <span class="nc">List</span><span class="o">&lt;</span><span class="nc">CreateBeerRequest</span><span class="o">&gt;</span> <span class="n">requests</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">ArrayList</span><span class="o">&lt;&gt;();</span>
</code></pre></div></div> <p><span style="margin-left: 10px;"></span> If these objects remain reachable, the GC cannot collect them. After surviving enough young collections, they may be promoted to the old generation, which changes the situation completely.</p> <p><span style="margin-left: 10px;"></span> Young generation garbage is expected. Old generation growth is more serious. In backend systems, old generation growth often means one of these things:</p> <ul> <li>a legitimate cache is growing</li> <li>a queue is not being drained fast enough</li> <li>a memory leak exists</li> <li>request-scoped data escaped into long-lived state</li> <li>the application is holding too many database entities</li> <li>the service is processing payloads that are too large</li> </ul> <p><span style="margin-left: 10px;"></span> This code looks harmless at first glance:</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="nc">List</span><span class="o">&lt;</span><span class="nc">BeerResponse</span><span class="o">&gt;</span> <span class="nf">getBeers</span><span class="o">(</span><span class="nc">User</span> <span class="n">user</span><span class="o">)</span> <span class="o">{</span>
    <span class="k">return</span> <span class="n">beerRepository</span><span class="o">.</span><span class="na">findByUserId</span><span class="o">(</span><span class="n">user</span><span class="o">.</span><span class="na">getId</span><span class="o">())</span>
            <span class="o">.</span><span class="na">stream</span><span class="o">()</span>
            <span class="o">.</span><span class="na">map</span><span class="o">(</span><span class="n">beer</span> <span class="o">-&gt;</span> <span class="k">new</span> <span class="nc">BeerResponse</span><span class="o">(</span>
                    <span class="n">beer</span><span class="o">.</span><span class="na">getId</span><span class="o">(),</span>
                    <span class="n">beer</span><span class="o">.</span><span class="na">getStatus</span><span class="o">().</span><span class="na">name</span><span class="o">(),</span>
                    <span class="n">beer</span><span class="o">.</span><span class="na">getCreatedAt</span><span class="o">().</span><span class="na">toString</span><span class="o">()</span>
            <span class="o">))</span>
            <span class="o">.</span><span class="na">toList</span><span class="o">();</span>
<span class="o">}</span>
</code></pre></div></div> <p><span style="margin-left: 10px;"></span> But under load, it may allocate many objects:</p> <ul> <li>database entity objects</li> <li>stream pipeline objects</li> <li>lambda-related structures</li> <li>response DTOs</li> <li>strings from name()</li> <li>strings from toString()</li> <li>internal list storage</li> </ul> <p><span style="margin-left: 10px;"></span> This does not mean streams are bad. It means hot paths must be understood in terms of allocation behavior. In low-traffic code, readability may matter more. In high-QPS endpoints, allocation patterns can affect GC frequency and latency.</p> <h3 id="json-serialization-a-major-allocation-source">JSON Serialization: a Major Allocation Source</h3> <p><span style="margin-left: 10px;"></span> For backend APIs, JSON processing is one of the most common sources of allocation.</p> <p><span style="margin-left: 10px;"></span> When using Jackson, the application may allocate:</p> <ul> <li>request DTOs</li> <li>nested DTOs</li> <li>temporary parsing buffers</li> <li>strings</li> <li>collections</li> <li>reflection metadata structures</li> <li>response serialization buffers</li> </ul> <p><span style="margin-left: 10px;"></span> Example:</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@PostMapping</span><span class="o">(</span><span class="s">"/users/search"</span><span class="o">)</span>
<span class="kd">public</span> <span class="nc">List</span><span class="o">&lt;</span><span class="nc">UserResponse</span><span class="o">&gt;</span> <span class="nf">search</span><span class="o">(</span><span class="nd">@RequestBody</span> <span class="nc">SearchRequest</span> <span class="n">request</span><span class="o">)</span> <span class="o">{</span>
    <span class="k">return</span> <span class="n">userService</span><span class="o">.</span><span class="na">search</span><span class="o">(</span><span class="n">request</span><span class="o">);</span>
<span class="o">}</span>
</code></pre></div></div> <p><span style="margin-left: 10px;"></span> If the response contains thousands of users, the service may allocate a large object graph:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>List&lt;UserResponse&gt;
  -&gt; UserResponse
  -&gt; AddressResponse
  -&gt; List&lt;RoleResponse&gt;
  -&gt; String fields
  -&gt; serialization buffers
</code></pre></div></div> <p><span style="margin-left: 10px;"></span> Large responses increase memory pressure, even if the endpoint seems functionally correct. This is why pagination is not only a database concern, but it is also a memory and GC concern.</p> <h3 id="hibernate-can-increase-object-lifetime">Hibernate Can Increase Object Lifetime</h3> <p><span style="margin-left: 10px;"></span> ORM frameworks can also influence GC behavior. With Hibernate/JPA, a query does not just return raw database rows. It may create:</p> <ul> <li>entity objects</li> <li>proxy objects</li> <li>collections</li> <li>persistence context entries</li> <li>lazy-loading structures</li> </ul> <p><span style="margin-left: 10px;"></span> For example:</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">List</span><span class="o">&lt;</span><span class="nc">Beer</span><span class="o">&gt;</span> <span class="n">beers</span> <span class="o">=</span> <span class="n">beerRepository</span><span class="o">.</span><span class="na">findAll</span><span class="o">();</span>
</code></pre></div></div> <p><span style="margin-left: 10px;"></span> This can be dangerous if the result set is large. The persistence context may keep entities reachable longer than expected. That means objects that could have died quickly may stay alive until the transaction or session ends. In batch jobs or large backend operations, this can cause old generation pressure.</p> <p><span style="margin-left: 10px;"></span> A safer pattern may be:</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">Page</span><span class="o">&lt;</span><span class="nc">Beer</span><span class="o">&gt;</span> <span class="n">page</span> <span class="o">=</span> <span class="n">beerRepository</span><span class="o">.</span><span class="na">findAll</span><span class="o">(</span><span class="n">pageable</span><span class="o">);</span>
</code></pre></div></div> <p>or streaming/clearing the persistence context carefully in batch processing.</p> <p><span style="margin-left: 10px;"></span> This seems obvious, but I thought it was worth mentioning that database result size affects heap pressure.</p> <h3 id="logging-can-allocate-more-than-expected">Logging Can Allocate More Than Expected</h3> <p><span style="margin-left: 10px;"></span> Logging is another common source of hidden allocation. This is especially true when logs are built eagerly:</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">log</span><span class="o">.</span><span class="na">debug</span><span class="o">(</span><span class="s">"Beer details: "</span> <span class="o">+</span> <span class="n">expensiveObject</span><span class="o">.</span><span class="na">toString</span><span class="o">());</span>
</code></pre></div></div> <p><span style="margin-left: 10px;"></span> Even if debug logging is disabled, the string concatenation may still happen before the logger decides whether to write the message.</p> <p><span style="margin-left: 10px;"></span> I prefer parameterized logging:</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">log</span><span class="o">.</span><span class="na">debug</span><span class="o">(</span><span class="s">"Beer details: {}"</span><span class="o">,</span> <span class="n">expensiveObject</span><span class="o">);</span>
</code></pre></div></div> <p><span style="margin-left: 10px;"></span> But even parameterized logging is not free if the object’s toString() is eventually called. This becomes important in high-throughput systems where every request logs multiple lines. Logging full payloads is especially risky. This can create large strings, increase allocation rate, slow down request processing and of course expose sensitive data.</p> <p><span style="margin-left: 10px;"></span> Good backend logging should be intentional, bounded, and production-safe. So only log what is necessary.</p> <h3 id="heap-usage-alone-is-not-enough">Heap Usage Alone Is Not Enough</h3> <p><span style="margin-left: 10px;"></span> I’ve seen many developers look only at heap usage:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Heap used: 2.5 GB / 4 GB
</code></pre></div></div> <p><span style="margin-left: 10px;"></span> That number is useful, but incomplete. For backend performance, we also need to understand:</p> <ul> <li>Allocation rate</li> <li>Young GC frequency</li> <li>Old generation growth</li> <li>Promotion rate</li> <li>GC pause duration</li> <li>Live object set size</li> </ul> <p><span style="margin-left: 10px;"></span> A service with stable heap usage may still have bad GC behavior if it allocates too much temporary memory.</p> <h3 id="what-this-means-for-java-backend-developers">What This Means for Java Backend Developers</h3> <p><span style="margin-left: 10px;"></span> A Java backend developer does not need to manually manage memory like in C or C++. But that does not mean memory is irrelevant. In Java, our responsibility is different:</p> <ul> <li>keep object lifetimes short when possible</li> <li>avoid unbounded data structures</li> <li>be careful with large responses</li> <li>understand framework allocation behavior</li> <li>monitor allocation rate and GC pauses</li> <li>investigate old-generation growth</li> <li>use evidence before tuning JVM flags</li> </ul> <p><span style="margin-left: 10px;"></span> Backend engineers must know that performance is not only about algorithms or database indexes. It is also about runtime behavior!</p> <h2 id="the-jvm-heap-model-for-backend-services">The JVM Heap Model for Backend Services</h2> <p><span style="margin-left: 10px;"></span> A Java backend developer does not need to manage memory manually, but they do need to understand where objects live, how long they live, and why that affects latency. The JVM heap is not just “where objects go.” In production, it is the space where request traffic, framework behavior, caching, JSON processing, database access, and application design all meet.</p> <p><span style="margin-left: 10px;"></span> The most important idea is this:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>The heap is shaped by object lifetime.
</code></pre></div></div> <p><span style="margin-left: 10px;"></span> Not all objects are equal. A request DTO that lives for 20 milliseconds is very different from a cache entry that lives for hours.</p> <h3 id="heap-vs-stack">Heap vs Stack</h3> <p><span style="margin-left: 10px;"></span> In Java, local variables and method calls are managed on thread stacks, while objects usually live on the heap. Example:</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="nc">BeerResponse</span> <span class="nf">getBeer</span><span class="o">(</span><span class="nc">Long</span> <span class="n">id</span><span class="o">)</span> <span class="o">{</span>
    <span class="nc">Beer</span> <span class="n">beer</span> <span class="o">=</span> <span class="n">beerRepository</span><span class="o">.</span><span class="na">findById</span><span class="o">(</span><span class="n">id</span><span class="o">).</span><span class="na">orElseThrow</span><span class="o">();</span>
    <span class="k">return</span> <span class="n">beerMapper</span><span class="o">.</span><span class="na">toResponse</span><span class="o">(</span><span class="n">beer</span><span class="o">);</span>
<span class="o">}</span>
</code></pre></div></div> <p>In simplified terms:</p> <p>Stack:</p> <ul> <li>id</li> <li>beer reference</li> <li>return value reference</li> </ul> <p>Heap:</p> <ul> <li>Long object, depending on boxing/caching</li> <li>Beer entity</li> <li>BeerResponse DTO</li> <li>Strings, collections, nested objects</li> </ul> <p><span style="margin-left: 10px;"></span> The stack stores references and method execution state. The heap stores the actual objects.</p> <p><span style="margin-left: 10px;"></span> When the method returns, stack variables disappear. But heap objects are only collectible if nothing still references them, and that distinction is critical. This object can be collected after the request:</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">BeerResponse</span> <span class="n">response</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">BeerResponse</span><span class="o">(...);</span>
</code></pre></div></div> <p><span style="margin-left: 10px;"></span> Unless something keeps a reference to it:</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">recentResponses</span><span class="o">.</span><span class="na">add</span><span class="o">(</span><span class="n">response</span><span class="o">);</span>
</code></pre></div></div> <p><span style="margin-left: 10px;"></span> The garbage collector does not care whether the request is finished. It only cares whether the object is still reachable.</p> <h3 id="reachability">Reachability</h3> <p><span style="margin-left: 10px;"></span> The JVM collects objects that are no longer reachable from known starting points called GC roots. Common GC roots may include:</p> <ul> <li>active thread stacks</li> <li>static fields</li> <li>class metadata references</li> <li>running thread references</li> </ul> <p><span style="margin-left: 10px;"></span> Example:</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">private</span> <span class="kd">static</span> <span class="kd">final</span> <span class="nc">Map</span><span class="o">&lt;</span><span class="nc">Long</span><span class="o">,</span> <span class="nc">UserProfile</span><span class="o">&gt;</span> <span class="n">cache</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">HashMap</span><span class="o">&lt;&gt;();</span>
</code></pre></div></div> <p><span style="margin-left: 10px;"></span> Anything inside this static map is reachable through a GC root. That means the GC cannot collect it, even if the data is no longer useful. This is why memory leaks in Java are not usually “lost memory” in the C/C++ sense. They are usually unwanted reachable objects. A Java memory leak means that the application still holds references to objects it no longer needs.</p> <p><span style="margin-left: 10px;"></span> That is one of the most important distinctions for us backend developers.</p> <h3 id="young-generation">Young Generation</h3> <p><span style="margin-left: 10px;"></span> Most new objects are initially allocated in the young generation. The young generation commonly contains:</p> <ul> <li>Eden space</li> <li>Survivor space 1</li> <li>Survivor space 2</li> </ul> <p><span style="margin-left: 10px;"></span> The usual flow is:</p> <ol> <li>Eden</li> <li>survives young GC</li> <li>Survivor space</li> <li>survives multiple collections</li> <li>promoted to Old Generation</li> </ol> <p><span style="margin-left: 10px;"></span> This is the ideal case. Short-lived request objects should be born, used, and collected quickly.</p> <p><span style="margin-left: 10px;"></span> A young GC collects the young generation. Usually, it is efficient because most young objects are dead.</p> <p>Before young GC:</p> <ul> <li>Eden: many request DTOs, JSON buffers, temporary lists</li> </ul> <p>After young GC:</p> <ul> <li>dead objects removed</li> <li>surviving objects copied to Survivor or promoted</li> </ul> <p><span style="margin-left: 10px;"></span> Young GCs are normally expected in Java backend applications. The problem is not that young GCs happen, it is when they happen too often or take too long. Frequent young GCs usually indicate high allocation rate. That often comes from:</p> <ul> <li>large responses</li> <li>excessive DTO mapping</li> <li>object-heavy transformations</li> <li>unnecessary intermediate collections</li> <li>verbose logging</li> <li>inefficient serialization</li> <li>high request volume</li> <li>large database result sets</li> </ul> <h3 id="survivor-spaces">Survivor Spaces</h3> <p><span style="margin-left: 10px;"></span> Objects that survive a young GC may be moved into a Survivor space. Survivor spaces exist to avoid promoting objects to the old generation too quickly. It is something like this:</p> <ol> <li>Request starts</li> <li>Object allocated in Eden</li> <li>Young GC happens while request is still running</li> <li>Object is still reachable</li> <li>Object moves to Survivor</li> <li>Request finishes</li> <li>Next young GC collects it</li> </ol> <p><span style="margin-left: 10px;"></span> And this is normal!!!</p> <p><span style="margin-left: 10px;"></span> A request object may survive one young GC simply because the request was still being processed at the time. That does not mean it is a leak. But if many objects keep surviving multiple young GCs, the JVM may eventually promote them to the old generation. That is where things become more expensive.</p> <h3 id="old-generation">Old Generation</h3> <p><span style="margin-left: 10px;"></span> The old generation stores objects that survived long enough to be considered long-lived. In backend services, old generation commonly contains:</p> <ul> <li>Spring beans</li> <li>application configuration</li> <li>caches</li> <li>connection pools</li> <li>thread pools</li> <li>loaded class structures and framework objects</li> <li>long-lived domain data</li> <li>retained Hibernate entities</li> <li>queued messages</li> <li>large collections</li> <li>session data</li> </ul> <p><span style="margin-left: 10px;"></span> Old generation is not bad, the problem is unexpected old-generation growth. For example:</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">private</span> <span class="kd">final</span> <span class="nc">List</span><span class="o">&lt;</span><span class="nc">BeerEvent</span><span class="o">&gt;</span> <span class="n">goodBeers</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">ArrayList</span><span class="o">&lt;&gt;();</span>
</code></pre></div></div> <p><span style="margin-left: 10px;"></span> If this list grows without bounds, old generation usage may continuously increase. The GC can run repeatedly and still fail to reclaim memory because those objects are still reachable. That is not a garbage collector failure its object retention!</p> <h3 id="live-set">Live Set</h3> <p><span style="margin-left: 10px;"></span> A very important production concept is the live set. The live set is the amount of memory still occupied after a full or major collection. For example:</p> <ul> <li>Heap before GC: 6 GB</li> <li>Heap after GC: 2 GB</li> <li>Live set: about 2 GB</li> </ul> <p><span style="margin-left: 10px;"></span> The live set represents objects the application is actually retaining. If the live set keeps growing over time, you may have:</p> <ul> <li>a memory leak</li> <li>an unbounded cache</li> <li>growing queues</li> <li>large long-lived maps</li> <li>legitimate growth in business data</li> </ul> <p><span style="margin-left: 10px;"></span> This is much more useful than looking only at total heap usage. A healthy service often has a pattern like this:</p> <ol> <li>heap grows</li> <li>GC runs</li> <li>heap drops</li> <li>heap grows</li> <li>GC runs</li> <li>heap drops</li> </ol> <p><span style="margin-left: 10px;"></span> An unhealthy service may look like this:</p> <ul> <li>heap grows</li> <li>GC runs</li> <li>heap drops less than before</li> <li>heap grows again</li> <li>GC runs</li> <li>heap drops even less</li> <li>old generation keeps increasing</li> </ul> <h3 id="heap-size-lies">Heap Size Lies</h3> <p><span style="margin-left: 10px;"></span> A common mistake is assuming that a bigger heap always means fewer problems, sometimes increasing heap may help. For example, if a service has a bursty allocation pattern, a slightly larger heap may reduce GC frequency. But bigger heap can also make things worse.</p> <p><span style="margin-left: 10px;"></span> Larger heaps may:</p> <ul> <li>hide memory leaks for longer</li> <li>increase time spent scanning live objects</li> <li>delay failure instead of fixing it</li> <li>increase container memory cost</li> <li>make old-generation collections more expensive</li> </ul> <p><span style="margin-left: 10px;"></span> For backend systems, heap sizing should be based on:</p> <ul> <li>live set size</li> <li>allocation rate</li> <li>latency requirements</li> <li>traffic profile</li> <li>container memory limits</li> <li>collector choice</li> <li>headroom for spikes</li> </ul> <h3 id="modern-collectors">Modern Collectors</h3> <p><span style="margin-left: 10px;"></span> When learning GC, diagrams often show this:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Heap
├── Young Generation
│    ├── Eden
│    ├── Survivor 0
│    └── Survivor 1
└── Old Generation
</code></pre></div></div> <p><span style="margin-left: 10px;"></span> This model is useful, but modern collectors may implement it differently. For example, G1 divides the heap into regions. A region can be used as Eden, Survivor, or Old depending on current GC needs. Conceptually, the generational model still matters. But physically, the heap may not be one large contiguous young area and one large contiguous old area. The young/old model is still useful conceptually, but collectors like G1 implement the heap using regions rather than fixed contiguous generations.</p> <h3 id="backend-example">Backend Example</h3> <p><span style="margin-left: 10px;"></span> Considering this endpoint:</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@GetMapping</span><span class="o">(</span><span class="s">"/beers/{id}"</span><span class="o">)</span>
<span class="kd">public</span> <span class="nc">BeerResponse</span> <span class="nf">getBeers</span><span class="o">(</span><span class="nd">@PathVariable</span> <span class="nc">Long</span> <span class="n">id</span><span class="o">)</span> <span class="o">{</span>
    <span class="nc">Beer</span> <span class="n">beer</span> <span class="o">=</span> <span class="n">beerService</span><span class="o">.</span><span class="na">getBeer</span><span class="o">(</span><span class="n">id</span><span class="o">);</span>
    <span class="k">return</span> <span class="n">beerMapper</span><span class="o">.</span><span class="na">toResponse</span><span class="o">(</span><span class="n">beer</span><span class="o">);</span>
<span class="o">}</span>
</code></pre></div></div> <p><span style="margin-left: 10px;"></span> A healthy memory profile might look like this:</p> <p>Long-lived:</p> <ul> <li>BeerService</li> <li>BeerRepository</li> <li>DataSource</li> <li>BeerMapper</li> <li>Spring MVC infrastructure</li> </ul> <p>Short-lived:</p> <ul> <li>request wrapper</li> <li>path variable objects</li> <li>Beer entity</li> <li>BeerResponse DTO</li> <li>JSON serialization buffer</li> </ul> <p><span style="margin-left: 10px;"></span> After the response is sent, most request-specific objects should become unreachable. That is exactly what the JVM is good at.</p> <p><span style="margin-left: 10px;"></span> Now consider this:</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Component</span>
<span class="kd">public</span> <span class="kd">class</span> <span class="nc">ProductDebugStore</span> <span class="o">{</span>

    <span class="kd">private</span> <span class="kd">final</span> <span class="nc">List</span><span class="o">&lt;</span><span class="nc">ProductResponse</span><span class="o">&gt;</span> <span class="n">responses</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">ArrayList</span><span class="o">&lt;&gt;();</span>

    <span class="kd">public</span> <span class="kt">void</span> <span class="nf">save</span><span class="o">(</span><span class="nc">ProductResponse</span> <span class="n">response</span><span class="o">)</span> <span class="o">{</span>
        <span class="n">responses</span><span class="o">.</span><span class="na">add</span><span class="o">(</span><span class="n">response</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div> <p><span style="margin-left: 10px;"></span> And this endpoint:</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@GetMapping</span><span class="o">(</span><span class="s">"/products/{id}"</span><span class="o">)</span>
<span class="kd">public</span> <span class="nc">ProductResponse</span> <span class="nf">getProduct</span><span class="o">(</span><span class="nd">@PathVariable</span> <span class="nc">Long</span> <span class="n">id</span><span class="o">)</span> <span class="o">{</span>
    <span class="nc">Product</span> <span class="n">product</span> <span class="o">=</span> <span class="n">productService</span><span class="o">.</span><span class="na">getProduct</span><span class="o">(</span><span class="n">id</span><span class="o">);</span>
    <span class="nc">ProductResponse</span> <span class="n">response</span> <span class="o">=</span> <span class="n">productMapper</span><span class="o">.</span><span class="na">toResponse</span><span class="o">(</span><span class="n">product</span><span class="o">);</span>

    <span class="n">productDebugStore</span><span class="o">.</span><span class="na">save</span><span class="o">(</span><span class="n">response</span><span class="o">);</span>

    <span class="k">return</span> <span class="n">response</span><span class="o">;</span>
<span class="o">}</span>
</code></pre></div></div> <p><span style="margin-left: 10px;"></span> Now every response is kept alive. The object is no longer request-scoped. It is reachable through a Spring singleton! The GC cannot collect it. Eventually, these objects may move to the old generation and stay there. This kind of issue often appears in production as:</p> <ul> <li>old generation keeps growing</li> <li>GC runs more often</li> <li>pause times increase</li> <li>eventually OutOfMemoryError or container restart</li> </ul> <p><span style="margin-left: 10px;"></span> The root cause is not “bad GC.” Its accidental retention.</p> <h2 id="gc-and-api-latency-p99-over-average">GC and API Latency: p99 Over Average</h2> <p><span style="margin-left: 10px;"></span> In backend systems, garbage collection becomes important when it affects request latency. A GC event is not just a memory-management detail. During certain phases of garbage collection, the JVM may pause application threads. When that happens, your service temporarily stops processing requests. That means GC can directly affect:</p> <ul> <li>HTTP response time</li> <li>p95 / p99 latency</li> <li>timeouts</li> <li>retries</li> <li>service-to-service communication</li> </ul> <p><span style="margin-left: 10px;"></span> The key production idea is that GC problems usually appear as latency problems before they appear as memory problems.</p> <h3 id="average-latency-can-hide-gc-issues">Average Latency Can Hide GC Issues</h3> <p><span style="margin-left: 10px;"></span> Many developers look at average latency first. For example the average latency is 25ms. That number may look healthy, but averages are often misleading in backend systems. A service can have a good average latency while still having serious tail-latency problems.</p> <p><span style="margin-left: 10px;"></span> For example:</p> <ul> <li>Average latency: 25ms</li> <li>p95 latency: 80ms</li> <li>p99 latency: 750ms</li> </ul> <p><span style="margin-left: 10px;"></span> This means most requests are fine, but the slowest 1% are very slow and for high-traffic services, that 1% matters. If a service handles 2,000 requests per second, then 1% means 20 slow requests every second!</p> <p><span style="margin-left: 10px;"></span> For backend APIs:</p> <ul> <li>p50 tells you the normal user experience</li> <li>p95 tells you about common slowness</li> <li>p99 tells you about production pain</li> <li>p999 tells you about rare but dangerous behavior</li> </ul> <p><span style="margin-left: 10px;"></span> GC often appears in p99 or p999 because pauses may not happen constantly. They happen periodically, under pressure, or during traffic bursts. That is why GC issues can stay hidden if you only monitor average latency.</p> <h3 id="stop-the-world-pauses">Stop-the-World Pauses</h3> <p><span style="margin-left: 10px;"></span> A stop-the-world pause means application threads are paused so the JVM can perform some GC work safely. During this pause:</p> <ul> <li>request threads stop running</li> <li>scheduled tasks stop running</li> <li>database calls may wait for application code to resume</li> </ul> <p><span style="margin-left: 10px;"></span> From the outside, the service looks slow or frozen. For example:</p> <ol> <li>Request starts</li> <li>Service begins processing</li> <li>GC pause happens for 300ms</li> <li>Application resumes</li> <li>Request completes</li> </ol> <p><span style="margin-left: 10px;"></span> Even if the application logic only needed 20ms, the observed latency may become 20ms application work + 300ms GC pause = 320ms response time. The user does not care that only 20ms was “real work.” The user sees a 320ms response.</p> <p><span style="margin-left: 10px;"></span> A database query usually affects one request. A slow external API call usually affects one request. A GC pause can affect many requests at the same time. Suppose a given service that has 100 active request-processing threads. If a stop-the-world pause lasts 200ms, all those threads may be paused. That means one GC event can delay many in-flight requests simultaneously. For example:</p> <ol> <li>100 requests in progress</li> <li>200ms stop-the-world pause</li> <li>all 100 requests become at least 200ms slower</li> </ol> <p><span style="margin-left: 10px;"></span> This is why GC has a strong effect on tail latency. The pause is not isolated to one unlucky request. It can hit the whole process. And that why the problem gets worse under load.</p> <p><span style="margin-left: 10px;"></span> While application threads are paused, new requests may continue arriving. Those requests wait in queues and after the JVM resumes, the service may need to process both requests that were paused and requests that arrived during the pause</p> <p><span style="margin-left: 10px;"></span> This can create a temporary backlog. A 200ms GC pause may cause more than 200ms of user-visible latency if the service was already close to capacity. The pattern is something like this:</p> <ol> <li>GC pause</li> <li>request processing stops</li> <li>queues grow</li> <li>service resumes</li> <li>backlog causes more latency</li> <li>clients timeout or retry</li> </ol> <p><span style="margin-left: 10px;"></span> And it gets worse in microservices, because latency does not stay local. Imagine Service A calls Service B. If Service B has a GC pause, Service A may timeout. Then Service A retries:</p> <ol> <li>Service B pauses</li> <li>Service A times out</li> <li>Service A retries</li> <li>Service B receives even more requests</li> </ol> <p><span style="margin-left: 10px;"></span> Now Service B has more load than before. More load means:</p> <ul> <li>more request objects</li> <li>more JSON parsing</li> <li>more DTO creation</li> <li>more logging</li> <li>more allocations</li> <li>more GC pressure</li> </ul> <p><span style="margin-left: 10px;"></span> This creates a dangerous feedback loop:</p> <ol> <li>GC pause</li> <li>latency spike</li> <li>timeout</li> <li>retry</li> <li>more traffic</li> <li>higher allocation rate</li> <li>more GC pressure</li> <li>more pauses</li> </ol> <p><span style="margin-left: 10px;"></span> This is how a local JVM memory issue can become a distributed-system incident.</p> <h3 id="gc-and-thread-pools">GC and Thread Pools</h3> <p><span style="margin-left: 10px;"></span> Java backend services usually depend on thread pools:</p> <ul> <li>Tomcat request threads</li> <li>database connection pools</li> <li>async executor pools</li> <li>scheduler pools</li> <li>Kafka listener threads</li> </ul> <p><span style="margin-left: 10px;"></span> GC pauses interact badly with these pools. During a pause, worker threads stop making progress. If requests keep arriving, thread pools and queues may fill up. This is why GC should be analyzed together with thread pool metrics. A p99 spike may not be caused only by “slow code.” It may be caused by a JVM pause that prevented otherwise healthy threads from running.</p> <h3 id="latency-oriented-vs-throughput-oriented-gc">Latency-Oriented vs Throughput-Oriented GC</h3> <p><span style="margin-left: 10px;"></span> Garbage collectors make trade-offs. A throughput-oriented collector tries to maximize the amount of useful application work completed over time. A latency-oriented collector tries to minimize pause duration, often by doing more work concurrently.</p> <p><span style="margin-left: 10px;"></span> Backend services usually care about latency because they serve users or other services. But that does not mean the lowest-pause collector is always the best choice. There is a trade-off, lower pause times mean more concurrent GC work, more CPU overhead and potentially lower throughput. For a backend service, the question is not which GC is fastest? The better question is which GC gives acceptable p99 latency at acceptable CPU and memory cost for this workload? That is the production mindset in my point of view.</p> <h3 id="example-a-gc-pause-hidden-in-p99">Example: A GC Pause Hidden in p99</h3> <p><span style="margin-left: 10px;"></span> Imagine this service:</p> <ul> <li>Endpoint: GET /products/{id}</li> <li>Average latency: 18ms</li> <li>p95 latency: 45ms</li> <li>p99 latency: 480ms</li> <li>Error rate: low</li> <li>CPU: normal</li> <li>Database latency: normal</li> </ul> <p><span style="margin-left: 10px;"></span> At first glance, nothing obvious is broken. Then you check GC logs and see something like this:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>12:00:01.200 Young GC pause: 12ms
12:00:05.700 Young GC pause: 18ms
12:00:10.400 Young GC pause: 22ms
12:00:15.900 Mixed GC pause: 410ms
12:00:16.000 p99 latency spike observed
</code></pre></div></div> <p><span style="margin-left: 10px;"></span> The application code did not suddenly become slower, the JVM paused request processing. The important skill is not memorizing GC terminology. The important skill is connecting the runtime event to user-visible behavior.</p> <h3 id="gc-pauses-can-break-health-checks">GC Pauses Can Break Health Checks</h3> <p><span style="margin-left: 10px;"></span> In containerized systems, GC pauses can also interfere with health checks. If a service pauses long enough, Kubernetes or a load balancer may see failed readiness or liveness checks. Possible outcomes may be:</p> <ul> <li>service temporarily removed from load balancer</li> <li>pod restarted</li> <li>traffic shifted to other pods</li> <li>remaining pods receive more load</li> <li>more GC pressure on remaining pods</li> </ul> <p><span style="margin-left: 10px;"></span> This can create a cascading failure. A single service with unstable GC behavior can cause the platform to make the situation worse by restarting or rerouting traffic. That does not mean health checks are bad. It means GC pauses need to be considered when setting timeouts and thresholds.</p> <h3 id="why-low-traffic-testing-misses-gc-problems">Why Low Traffic Testing Misses GC Problems</h3> <p><span style="margin-left: 10px;"></span> GC behavior depends heavily on workload. Local testing usually has:</p> <ul> <li>low request volume</li> <li>small payloads</li> <li>short test duration</li> <li>low concurrency</li> </ul> <p><span style="margin-left: 10px;"></span> Production has:</p> <ul> <li>high QPS</li> <li>large payloads</li> <li>long-running process</li> <li>real user traffic</li> <li>traffic bursts</li> <li>large caches</li> <li>background jobs</li> <li>database variability</li> <li>retries</li> </ul> <p><span style="margin-left: 10px;"></span> A service can look perfect locally and still have GC-related p99 spikes in production. This is why load testing matters. A realistic performance test should simulate:</p> <ul> <li>expected QPS</li> <li>concurrent users</li> <li>payload size distribution</li> <li>large responses</li> <li>database behavior</li> <li>cache warmup</li> <li>background jobs</li> <li>traffic bursts</li> </ul> <p><span style="margin-left: 10px;"></span> GC tuning without realistic load is mostly guesswork.</p> <h3 id="what-to-monitor">What to Monitor</h3> <p><span style="margin-left: 10px;"></span> For backend services, monitor GC together with application metrics. Useful JVM metrics:</p> <ul> <li>GC pause duration</li> <li>GC pause count</li> <li>young GC frequency</li> <li>old GC frequency</li> <li>heap used before/after GC</li> <li>old generation usage</li> <li>allocation rate</li> <li>promotion rate</li> </ul> <p><span style="margin-left: 10px;"></span> Useful application metrics:</p> <ul> <li>request latency p95/p99</li> <li>request throughput</li> <li>error rate</li> <li>timeout rate</li> <li>retry rate</li> <li>thread pool usage</li> <li>connection pool usage</li> <li>CPU usage</li> <li>container memory usage</li> </ul> <p><span style="margin-left: 10px;"></span> The goal is correlation. You may want to answer:</p> <ul> <li>Did p99 latency increase at the same time as GC pauses?</li> <li>Did old generation grow before the incident?</li> <li>Did allocation rate increase after a deployment?</li> <li>Did retries increase after GC pauses?</li> <li>Did thread pools saturate after the JVM resumed?</li> </ul> <h2 id="modern-collectors-1">Modern Collectors</h2> <p><span style="margin-left: 10px;"></span> I do not need to know every garbage collector in detail. What matters is knowing which collector fits which production problem. The decision is usually about the trade-off: latency vs throughput vs CPU cost vs memory footprint</p> <h3 id="g1-gc">G1 GC</h3> <p><span style="margin-left: 10px;"></span> For most modern Java backend services, G1 GC is the default choice. Oracle’s Java 25 documentation states that G1 is selected by default on most hardware and operating system configurations. It also recommends starting with G1 defaults, optionally setting a pause-time goal and maximum heap size. G1 is a good fit for:</p> <ul> <li>Spring Boot APIs</li> <li>microservices</li> <li>medium-size heaps</li> <li>general backend workloads</li> <li>systems that need balanced latency and throughput</li> </ul> <p><span style="margin-left: 10px;"></span> Why it works well:</p> <ul> <li>region-based heap</li> <li>incremental collection</li> <li>predictable pause-time goals</li> <li>good default behavior</li> <li>less manual tuning than older collectors</li> </ul> <p><span style="margin-left: 10px;"></span> The production mindset is to start with G1 unless you have evidence that your latency requirements need something else.</p> <h3 id="zgc">ZGC</h3> <p><span style="margin-left: 10px;"></span> ZGC is designed for low-latency applications where long GC pauses are unacceptable. Modern ZGC is generational, it splits the heap into young and old generations so it can collect recently allocated objects separately from long-lived objects. ZGC is interesting for backend services with:</p> <ul> <li>large heaps</li> <li>strict p99/p999 latency targets</li> <li>real-time-ish APIs</li> <li>highly interactive systems</li> <li>services where GC pauses are visible to users</li> </ul> <p><span style="margin-left: 10px;"></span> You might consider ZGC when:</p> <ul> <li>G1 pause times are too high</li> <li>old-gen collections affect p99 latency</li> <li>heap size is large</li> <li>the business cares more about predictable latency than maximum throughput</li> </ul> <p><span style="margin-left: 10px;"></span> The trade-off is that ZGC reduces pause times by doing more work concurrently, but that work consumes CPU. So ZGC is not automatically “better”. It may reduce latency while increasing CPU usage.</p> <h3 id="shenandoah">Shenandoah</h3> <p><span style="margin-left: 10px;"></span> Shenandoah is also a low-pause collector. Its key idea is to reduce pause times by doing more garbage collection work concurrently with the running Java application, including concurrent compaction. It is useful for similar cases as ZGC.</p> <p><span style="margin-left: 10px;"></span> Like ZGC, Shenandoah shifts more work out of stop-the-world pauses and into concurrent execution. The trade-offs are also similar.</p> <h3 id="how-to-choose-in-backend-systems">How to Choose in Backend Systems</h3> <p><span style="margin-left: 10px;"></span> A practical decision model:</p> <ul> <li>Default service? Use G1</li> <li>Typical Spring Boot microservice? Use G1</li> <li>No clear GC problem? Stay with G1</li> <li>Strict p99/p999 latency target? Test ZGC or Shenandoah</li> <li>Large heap with painful pauses? Test ZGC or Shenandoah</li> <li>CPU-constrained environment? Be careful with concurrent collectors</li> <li>Batch job focused on total throughput? Do not blindly choose low-latency GC</li> </ul> <p><span style="margin-left: 10px;"></span> The important point is that the collector choice should be driven by workload and latency requirements, not by hype.</p> <h2 id="java-backend-memory-leaks-that-look-like-gc-problems">Java Backend Memory Leaks That Look Like GC Problems</h2> <p><span style="margin-left: 10px;"></span> Many production “GC problems” are not caused by the garbage collector. They are caused by the application keeping references to objects it no longer needs. The key idea is that GC cannot collect objects that are still reachable. A memory leak is often an object-retention problem, not a garbage-collector problem. In Java, memory leaks usually do not happen because memory is “lost.” They happen because objects are still reachable from somewhere: a static field, a cache, a queue, a thread, a session, a listener, or a framework context.</p> <h3 id="unbounded-caches">Unbounded Caches</h3> <p><span style="margin-left: 10px;"></span> Caching is one of the most common sources of Java backend leaks. For example:</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">private</span> <span class="kd">final</span> <span class="nc">Map</span><span class="o">&lt;</span><span class="nc">String</span><span class="o">,</span> <span class="nc">UserProfile</span><span class="o">&gt;</span> <span class="n">cache</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">HashMap</span><span class="o">&lt;&gt;();</span>
</code></pre></div></div> <p><span style="margin-left: 10px;"></span> If this map has no size limit, expiration policy, or eviction strategy, it can grow forever. This often happens with:</p> <ul> <li>user profiles</li> <li>authorization data</li> <li>product catalogs</li> <li>API responses</li> <li>tenant configuration</li> <li>lookup tables</li> </ul> <p><span style="margin-left: 10px;"></span> A cache should usually have:</p> <ul> <li>maximum size</li> <li>time-based expiration</li> <li>metrics</li> <li>eviction policy</li> </ul> <p><span style="margin-left: 10px;"></span> Using a library like Caffeine is usually better than building a cache with a raw HashMap.</p> <h3 id="static-collections">Static Collections</h3> <p><span style="margin-left: 10px;"></span> Static collections are dangerous because they live as long as the classloader lives. For example:</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">import</span> <span class="nn">java.util.ArrayList</span><span class="o">;</span>
<span class="kn">import</span> <span class="nn">java.util.List</span><span class="o">;</span>

<span class="kd">public</span> <span class="kd">class</span> <span class="nc">DebugStore</span> <span class="o">{</span>
    <span class="kd">private</span> <span class="kd">static</span> <span class="kd">final</span> <span class="nc">List</span><span class="o">&lt;</span><span class="nc">Object</span><span class="o">&gt;</span> <span class="no">EVENTS</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">ArrayList</span><span class="o">&lt;&gt;();</span>

    <span class="kd">public</span> <span class="kd">static</span> <span class="kt">void</span> <span class="nf">add</span><span class="o">(</span><span class="nc">Object</span> <span class="n">event</span><span class="o">)</span> <span class="o">{</span>
        <span class="no">EVENTS</span><span class="o">.</span><span class="na">add</span><span class="o">(</span><span class="n">event</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div> <p><span style="margin-left: 10px;"></span> Every object added to EVENTS remains reachable. This is especially dangerous for:</p> <ul> <li>debug stores</li> <li>temporary registries</li> <li>in-memory audit logs</li> <li>test utilities accidentally used in production</li> <li>static maps of request data</li> </ul> <p><span style="margin-left: 10px;"></span> <br/> A static reference is effectively a global root. If it grows without bounds, GC cannot help.</p> <h3 id="forgotten-listeners-and-callbacks">Forgotten Listeners and Callbacks</h3> <p><span style="margin-left: 10px;"></span> Listeners can leak memory when they are registered but never removed. For example:</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">eventBus</span><span class="o">.</span><span class="na">register</span><span class="o">(</span><span class="n">listener</span><span class="o">);</span>
</code></pre></div></div> <p><span style="margin-left: 10px;"></span> If the event bus is long-lived and the listener references request, user, or component state, that state may remain alive. Common places are:</p> <ul> <li>event buses</li> <li>message listeners</li> <li>application lifecycle hooks</li> <li>WebSocket subscriptions</li> <li>observer patterns</li> <li>reactive streams</li> <li>scheduler callbacks</li> </ul> <p><span style="margin-left: 10px;"></span> The fix is to unregister listeners when they are no longer needed:</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">eventBus</span><span class="o">.</span><span class="na">unregister</span><span class="o">(</span><span class="n">listener</span><span class="o">);</span>
</code></pre></div></div> <p><span style="margin-left: 10px;"></span> This matters especially in long-running backend services where leaks accumulate slowly.</p> <h3 id="threadlocal-misuse">ThreadLocal Misuse</h3> <p><span style="margin-left: 10px;"></span> ThreadLocal is useful, but dangerous in backend applications because request threads are reused. For example:</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">private</span> <span class="kd">static</span> <span class="kd">final</span> <span class="nc">ThreadLocal</span><span class="o">&lt;</span><span class="nc">RequestContext</span><span class="o">&gt;</span> <span class="n">context</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">ThreadLocal</span><span class="o">&lt;&gt;();</span>

<span class="kd">public</span> <span class="kt">void</span> <span class="nf">handle</span><span class="o">(</span><span class="nc">RequestContext</span> <span class="n">requestContext</span><span class="o">)</span> <span class="o">{</span>
    <span class="n">context</span><span class="o">.</span><span class="na">set</span><span class="o">(</span><span class="n">requestContext</span><span class="o">);</span>
<span class="o">}</span>
</code></pre></div></div> <p><span style="margin-left: 10px;"></span> If you do not call remove(), the request context may stay attached to the thread after the request finishes. In servlet containers, executor pools, and async processing, those threads may live for the lifetime of the application. The correct pattern is:</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">try</span> <span class="o">{</span>
    <span class="n">context</span><span class="o">.</span><span class="na">set</span><span class="o">(</span><span class="n">requestContext</span><span class="o">);</span>
    <span class="n">process</span><span class="o">();</span>
<span class="o">}</span> <span class="k">finally</span> <span class="o">{</span>
    <span class="n">context</span><span class="o">.</span><span class="na">remove</span><span class="o">();</span>
<span class="o">}</span>
</code></pre></div></div> <p><span style="margin-left: 10px;"></span> Leaks through ThreadLocal are common with:</p> <ul> <li>security context</li> <li>tenant context</li> <li>correlation IDs</li> <li>request metadata</li> <li>large user/session objects</li> <li>custom tracing data</li> </ul> <p><span style="margin-left: 10px;"></span> The rule is simple, if you set a ThreadLocal in request processing, remove it in a finally block or try-with-resources.</p> <h3 id="classloader-leaks">Classloader Leaks</h3> <p><span style="margin-left: 10px;"></span> Classloader leaks usually appear in application servers, plugin systems, hot reload environments, or repeated redeployments. A classloader can be retained by:</p> <ul> <li>static fields</li> <li>running threads</li> <li>ThreadLocals</li> <li>JDBC drivers</li> <li>logging frameworks</li> <li>custom registries</li> </ul> <p><span style="margin-left: 10px;"></span> The result is that old application classes and objects cannot be collected after redeployment. In modern Spring Boot applications packaged as containers, this is less common than in traditional app servers, but it still matters in:</p> <ul> <li>Tomcat deployments</li> <li>application servers</li> </ul> <h3 id="large-session-objects">Large Session Objects</h3> <p><span style="margin-left: 10px;"></span> Sessions can retain much more memory than expected. Example:</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">session</span><span class="o">.</span><span class="na">setAttribute</span><span class="o">(</span><span class="s">"cart"</span><span class="o">,</span> <span class="n">largeCartObject</span><span class="o">);</span>
<span class="n">session</span><span class="o">.</span><span class="na">setAttribute</span><span class="o">(</span><span class="s">"userProfile"</span><span class="o">,</span> <span class="n">fullUserProfile</span><span class="o">);</span>
<span class="n">session</span><span class="o">.</span><span class="na">setAttribute</span><span class="o">(</span><span class="s">"lastSearchResults"</span><span class="o">,</span> <span class="n">hugeResultList</span><span class="o">);</span>
</code></pre></div></div> <p><span style="margin-left: 10px;"></span> This becomes dangerous when:</p> <ul> <li>sessions are long-lived</li> <li>many users are active</li> <li>objects stored in session are large</li> <li>session replication is enabled</li> <li>old session data is not removed</li> </ul> <p><span style="margin-left: 10px;"></span> A common mistake is storing full domain objects or large result sets in session instead of storing small identifiers. Prefer:</p> <ul> <li>user ID instead of full user object</li> <li>cart ID instead of full cart graph</li> <li>pagination token instead of full search result list</li> </ul> <p><span style="margin-left: 10px;"></span> Sessions should be small, intentional, and time-bounded.</p> <h3 id="orm-persistence-context-growth">ORM Persistence Context Growth</h3> <p><span style="margin-left: 10px;"></span> Hibernate/JPA can retain entities longer than expected. Inside a transaction, the persistence context tracks managed entities for dirty checking and identity management. This is fine for small operations:</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">Beer</span> <span class="n">beer</span> <span class="o">=</span> <span class="n">entityManager</span><span class="o">.</span><span class="na">find</span><span class="o">(</span><span class="nc">Beer</span><span class="o">.</span><span class="na">class</span><span class="o">,</span> <span class="n">id</span><span class="o">);</span>
</code></pre></div></div> <p><span style="margin-left: 10px;"></span> But it becomes dangerous in large operations:</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">List</span><span class="o">&lt;</span><span class="nc">Beer</span><span class="o">&gt;</span> <span class="n">beers</span> <span class="o">=</span> <span class="n">beerRepository</span><span class="o">.</span><span class="na">findAll</span><span class="o">();</span>

<span class="k">for</span> <span class="o">(</span><span class="nc">Beer</span> <span class="n">beer</span> <span class="o">:</span> <span class="n">beers</span><span class="o">)</span> <span class="o">{</span>
    <span class="n">process</span><span class="o">(</span><span class="n">beer</span><span class="o">);</span>
<span class="o">}</span>
</code></pre></div></div> <p><span style="margin-left: 10px;"></span> The persistence context may retain many entities until the transaction ends. In batch jobs, this can cause old-generation pressure or OutOfMemoryError. Safer approaches may include:</p> <ul> <li>pagination</li> <li>streaming carefully</li> <li>smaller transactions</li> <li>read-only queries</li> <li>clearing the persistence context</li> <li>avoiding huge findAll operations</li> </ul> <p><span style="margin-left: 10px;"></span> Example in batch processing:</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">entityManager</span><span class="o">.</span><span class="na">flush</span><span class="o">();</span>
<span class="n">entityManager</span><span class="o">.</span><span class="na">clear</span><span class="o">();</span>
</code></pre></div></div> <h3 id="queues-growing-faster-than-consumers">Queues Growing Faster Than Consumers</h3> <p><span style="margin-left: 10px;"></span> Queues are another common backend leak pattern. Example:</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">private</span> <span class="kd">final</span> <span class="nc">BlockingQueue</span><span class="o">&lt;</span><span class="nc">Event</span><span class="o">&gt;</span> <span class="n">queue</span> <span class="o">=</span> <span class="k">new</span> <span class="nc">LinkedBlockingQueue</span><span class="o">&lt;&gt;();</span>
</code></pre></div></div> <p><span style="margin-left: 10px;"></span> If the queue is unbounded and producers are faster than consumers, memory usage grows continuously. This happens with:</p> <ul> <li>async event processing</li> <li>Kafka/RabbitMQ consumers</li> <li>email jobs</li> <li>audit logging</li> <li>background task queues</li> <li>retry queues</li> </ul> <p><span style="margin-left: 10px;"></span> Symptoms:</p> <ul> <li>heap grows during traffic spikes</li> <li>old generation increases</li> <li>latency gets worse</li> <li>consumer lag increases</li> </ul> <h2 id="spring-boot-hibernate-jackson-and-gc-pressure">Spring Boot, Hibernate, Jackson, and GC Pressure</h2> <p><span style="margin-left: 10px;"></span> In real Java backend systems, GC pressure rarely comes from one obvious new statement. It usually comes from the ecosystem around the request:</p> <ul> <li>Spring MVC</li> <li>Jackson</li> <li>Hibernate/JPA</li> <li>Bean Validation</li> <li>logging</li> <li>DTO mappers</li> <li>AOP proxies</li> <li>reflection</li> <li>metrics/tracing</li> </ul> <p><span style="margin-left: 10px;"></span> These tools are productive and valuable, but they also create objects. At production traffic levels, framework allocation becomes part of your performance profile. The point is not to avoid frameworks, its to understand their runtime cost.</p> <p>###DTO Mapping Overhead</p> <p><span style="margin-left: 10px;"></span> Most backend services use DTOs to separate API models from domain models. For example:</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kd">public</span> <span class="nc">BeerResponse</span> <span class="nf">toResponse</span><span class="o">(</span><span class="nc">Beer</span> <span class="n">beer</span><span class="o">)</span> <span class="o">{</span>
    <span class="k">return</span> <span class="k">new</span> <span class="nf">BeerResponse</span><span class="o">(</span>
            <span class="n">beer</span><span class="o">.</span><span class="na">getId</span><span class="o">(),</span>
            <span class="n">beer</span><span class="o">.</span><span class="na">getStatus</span><span class="o">().</span><span class="na">name</span><span class="o">(),</span>
            <span class="n">beer</span><span class="o">.</span><span class="na">getCustomer</span><span class="o">().</span><span class="na">getName</span><span class="o">(),</span> 
            <span class="n">beer</span><span class="o">.</span><span class="na">getBreweries</span><span class="o">().</span><span class="na">stream</span><span class="o">()</span>
            <span class="o">.</span><span class="na">map</span><span class="o">(</span><span class="k">this</span><span class="o">::</span><span class="n">toBreweryResponse</span><span class="o">)</span>
            <span class="o">.</span><span class="na">toList</span><span class="o">()</span>
    <span class="o">);</span>
<span class="o">}</span>
</code></pre></div></div> <p><span style="margin-left: 10px;"></span> This is clean design, as is MapStruct, but it allocates:</p> <ul> <li>BeerResponse</li> <li>BreweryResponse objects</li> <li>lists</li> <li>strings</li> <li>stream pipeline objects</li> <li>temporary mapping structures</li> </ul> <p><span style="margin-left: 10px;"></span> DTO mapping becomes expensive when:</p> <ul> <li>responses are large</li> <li>mapping happens in hot endpoints</li> <li>nested object graphs are deep</li> <li>multiple mapping layers exist</li> <li>intermediate collections are created</li> </ul> <p><span style="margin-left: 10px;"></span> This does not mean DTOs are bad. It means large mappings should be intentional, measured, and paginated where possible.</p> <h3 id="jackson-object-creation">Jackson Object Creation</h3> <p><span style="margin-left: 10px;"></span> JSON serialization and deserialization are major allocation sources in REST APIs. For inbound requests, Jackson may allocate:</p> <ul> <li>request DTOs</li> <li>nested objects</li> <li>collections</li> <li>strings</li> <li>parser buffers</li> <li>temporary metadata structures</li> </ul> <p><span style="margin-left: 10px;"></span> For outbound responses, it may allocate:</p> <ul> <li>serialization buffers</li> <li>field name strings</li> <li>nested DTO traversal structures</li> <li>temporary byte/char arrays</li> </ul> <p><span style="margin-left: 10px;"></span> Example:</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@PostMapping</span><span class="o">(</span><span class="s">"/search"</span><span class="o">)</span>
<span class="kd">public</span> <span class="nc">List</span><span class="o">&lt;</span><span class="nc">BeerResponse</span><span class="o">&gt;</span> <span class="nf">search</span><span class="o">(</span><span class="nd">@RequestBody</span> <span class="nc">SearchRequest</span> <span class="n">request</span><span class="o">)</span> <span class="o">{</span>
    <span class="k">return</span> <span class="n">beerService</span><span class="o">.</span><span class="na">search</span><span class="o">(</span><span class="n">request</span><span class="o">);</span>
<span class="o">}</span>
</code></pre></div></div> <p><span style="margin-left: 10px;"></span> If this endpoint returns thousands of beers, the cost is not only database time. It is also heap pressure from the returned object graph and serialization process. Use pagination, streaming responses when appropriate, response-size limits, and avoid returning unnecessary fields.</p> <h3 id="hibernate-persistence-context">Hibernate Persistence Context</h3> <p><span style="margin-left: 10px;"></span> Hibernate does not just return objects from the database it also manages them. Inside a transaction, Hibernate keeps entities in the persistence context for identity tracking and dirty checking. For example:</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@Transactional</span>
<span class="kd">public</span> <span class="kt">void</span> <span class="nf">processBeers</span><span class="o">()</span> <span class="o">{</span>
<span class="nc">List</span><span class="o">&lt;</span><span class="nc">Beer</span><span class="o">&gt;</span> <span class="n">beers</span> <span class="o">=</span> <span class="n">beerRepository</span><span class="o">.</span><span class="na">findAll</span><span class="o">();</span>

    <span class="k">for</span> <span class="o">(</span><span class="nc">Beer</span> <span class="n">beer</span> <span class="o">:</span> <span class="n">beers</span><span class="o">)</span> <span class="o">{</span>
        <span class="n">process</span><span class="o">(</span><span class="n">beer</span><span class="o">);</span>
    <span class="o">}</span>
<span class="o">}</span>
</code></pre></div></div> <p><span style="margin-left: 10px;"></span> This can retain a large number of entities until the transaction ends. The persistence context may hold:</p> <ul> <li>entity instances</li> <li>entity snapshots</li> <li>proxy objects</li> <li>collections</li> <li>dirty-checking data</li> <li>relationship graphs</li> </ul> <p><span style="margin-left: 10px;"></span> For small requests, this is fine. For large reads or batch operations, it can create old-generation pressure. Safer patterns:</p> <ul> <li>pagination</li> <li>read-only transactions</li> <li>smaller transaction boundaries</li> <li>DTO projections</li> <li>clearing the persistence context in batches</li> <li>avoiding large findAll operations</li> </ul> <p><span style="margin-left: 10px;"></span> Example:</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">entityManager</span><span class="o">.</span><span class="na">flush</span><span class="o">();</span>
<span class="n">entityManager</span><span class="o">.</span><span class="na">clear</span><span class="o">();</span>
</code></pre></div></div> <p><span style="margin-left: 10px;"></span> The key idea is that a large Hibernate session can keep objects alive longer than your code suggests.</p> <h3 id="n1-queries-can-create-excessive-entities">N+1 Queries Can Create Excessive Entities</h3> <p><span style="margin-left: 10px;"></span> N+1 queries are usually discussed as a database performance problem, but they are also a memory problem. Example:</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nc">List</span><span class="o">&lt;</span><span class="nc">Beer</span><span class="o">&gt;</span> <span class="n">beers</span> <span class="o">=</span> <span class="n">beerRepository</span><span class="o">.</span><span class="na">findAll</span><span class="o">();</span>

<span class="k">for</span> <span class="o">(</span><span class="nc">Beer</span> <span class="n">beer</span> <span class="o">:</span> <span class="n">beers</span><span class="o">)</span> <span class="o">{</span>
    <span class="n">beer</span><span class="o">.</span><span class="na">getBreweries</span><span class="o">().</span><span class="na">size</span><span class="o">();</span> <span class="c1">// may trigger lazy loading</span>
<span class="o">}</span>
</code></pre></div></div> <p><span style="margin-left: 10px;"></span> This can create:</p> <ul> <li>many SQL queries</li> <li>many entity objects</li> <li>many Hibernate proxies</li> <li>many collections</li> <li>large object graphs</li> <li>extra persistence context entries</li> </ul> <p><span style="margin-left: 10px;"></span> So the cost is not only too many database round trips it is also too many Java objects allocated and retained. Fixes may include:</p> <ul> <li>fetch joins</li> <li>entity graphs</li> <li>DTO projections</li> <li>batch fetching</li> <li>pagination</li> <li>query-specific data loading</li> </ul> <p><span style="margin-left: 10px;"></span> A backend engineer should understand both sides: database impact and heap impact.</p> <h3 id="large-result-sets">Large Result Sets</h3> <p><span style="margin-left: 10px;"></span> This is one of the simplest ways to create GC pressure. Example:</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@GetMapping</span><span class="o">(</span><span class="s">"/beers"</span><span class="o">)</span>
<span class="kd">public</span> <span class="nc">List</span><span class="o">&lt;</span><span class="nc">BeerResponse</span><span class="o">&gt;</span> <span class="nf">getBeers</span><span class="o">()</span> <span class="o">{</span>
    <span class="k">return</span> <span class="n">beerRepository</span><span class="o">.</span><span class="na">findAll</span><span class="o">()</span>
        <span class="o">.</span><span class="na">stream</span><span class="o">()</span>
        <span class="o">.</span><span class="na">map</span><span class="o">(</span><span class="nl">beerMapper:</span><span class="o">:</span><span class="n">toResponse</span><span class="o">)</span>
        <span class="o">.</span><span class="na">toList</span><span class="o">();</span>
<span class="o">}</span>
</code></pre></div></div> <p><span style="margin-left: 10px;"></span> This may allocate:</p> <ul> <li>all beer entities</li> <li>related entities</li> <li>DTOs</li> <li>lists</li> <li>strings</li> <li>JSON serialization buffers</li> </ul> <p><span style="margin-left: 10px;"></span> Large result sets are dangerous because they create memory pressure across multiple layers:</p> <ul> <li>database driver</li> <li>Hibernate</li> <li>DTO mapper</li> <li>Jackson</li> <li>HTTP response buffer</li> </ul> <p><span style="margin-left: 10px;"></span> A production-safe API should usually use:</p> <ul> <li>pagination</li> <li>limits</li> <li>filters</li> <li>cursor-based pagination</li> <li>streaming for specific use cases</li> </ul> <h3 id="logging-full-payloads">Logging Full Payloads</h3> <p><span style="margin-left: 10px;"></span> Logging is often underestimated. Example:</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">log</span><span class="o">.</span><span class="na">info</span><span class="o">(</span><span class="s">"Request: {}"</span><span class="o">,</span> <span class="n">request</span><span class="o">);</span>
<span class="n">log</span><span class="o">.</span><span class="na">info</span><span class="o">(</span><span class="s">"Response: {}"</span><span class="o">,</span> <span class="n">response</span><span class="o">);</span>
</code></pre></div></div> <p><span style="margin-left: 10px;"></span> This can allocate large strings, especially when DTOs have generated toString() methods that include nested objects. Problems with full-payload logging:</p> <ul> <li>large string allocations</li> <li>sensitive data exposure</li> <li>slower request processing</li> <li>larger log volume</li> <li>more pressure on logging appenders</li> <li>possible async logging queue growth</li> </ul> <p><span style="margin-left: 10px;"></span> This is worse when logging happens on every request. Prefer:</p> <ul> <li>request ID</li> <li>user/tenant ID</li> <li>endpoint</li> <li>status code</li> <li>duration</li> <li>small business identifiers</li> <li>error code</li> <li>payload size</li> </ul> <p><span style="margin-left: 10px;"></span> Instead of logging the whole object graph, log the useful identifiers.</p> <h3 id="validation-frameworks">Validation Frameworks</h3> <p><span style="margin-left: 10px;"></span> Bean Validation is convenient and common in Spring Boot APIs. Example:</p> <div class="language-java highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nd">@PostMapping</span><span class="o">(</span><span class="s">"/beers"</span><span class="o">)</span>
<span class="kd">public</span> <span class="nc">BeerResponse</span> <span class="nf">create</span><span class="o">(</span><span class="nd">@Valid</span> <span class="nd">@RequestBody</span> <span class="nc">CreatebeerRequest</span> <span class="n">request</span><span class="o">)</span> <span class="o">{</span>
    <span class="k">return</span> <span class="n">beerService</span><span class="o">.</span><span class="na">create</span><span class="o">(</span><span class="n">request</span><span class="o">);</span>
<span class="o">}</span>
</code></pre></div></div> <p><span style="margin-left: 10px;"></span> Validation may allocate:</p> <ul> <li>constraint violation objects</li> <li>property paths</li> <li>message templates</li> <li>interpolated messages</li> <li>metadata lookups</li> <li>temporary collections</li> </ul> <p><span style="margin-left: 10px;"></span> Usually this is fine, it becomes more relevant when:</p> <ul> <li>payloads are large</li> <li>nested validation is deep</li> <li>many invalid requests arrive</li> <li>validation runs in hot paths</li> <li>custom validators allocate heavily</li> <li>error responses include many details</li> </ul> <p><span style="margin-left: 10px;"></span> Validation should be clear and useful, but avoid creating huge validation responses for massive payloads.</p> <h3 id="reflection-and-proxy-heavy-frameworks">Reflection and Proxy-Heavy Frameworks</h3> <p><span style="margin-left: 10px;"></span> Spring Boot applications use reflection, dynamic proxies, annotations, and generated infrastructure heavily. Examples:</p> <ul> <li>Spring AOP proxies</li> <li>@Transactional proxies</li> <li>security proxies</li> <li>repository proxies</li> <li>reflection-based binding</li> <li>annotation scanning</li> <li>method interceptors</li> <li>metrics/tracing instrumentation</li> </ul> <p><span style="margin-left: 10px;"></span> Most of this cost is acceptable and often paid during startup or cached internally. But in request paths, proxies and interceptors can still add allocation and call overhead. This matters especially in:</p> <ul> <li>very high-QPS endpoints</li> <li>deep service call chains</li> <li>heavy AOP usage</li> <li>reflection-heavy mappers</li> <li>dynamic serialization/deserialization</li> <li>excessive instrumentation</li> </ul> <p><span style="margin-left: 10px;"></span> Again, the point is not to avoid Spring. The point is to know that abstractions are not free.</p> <h3 id="how-this-connects-to-gc">How This Connects to GC</h3> <p><span style="margin-left: 10px;"></span> A typical Spring Boot request may look like this:</p> <ul> <li>Spring MVC objects</li> <li>Jackson request DTOs</li> <li>validation objects</li> <li>service-layer allocations</li> <li>Hibernate entities/proxies</li> <li>DTO mapping</li> <li>Jackson response serialization</li> <li>logging/tracing/metrics</li> <li>GC later cleans temporary objects</li> </ul> <p><span style="margin-left: 10px;"></span> This is why GC behavior is connected to everyday backend code. If traffic increases, all of these allocations increase too. If payloads get larger, object graphs get larger. If Hibernate loads too much data, old-generation pressure increases. If logs include full payloads, string allocation increases. If queues or sessions retain these objects, they stop being temporary.</p> <h2 id="conclusion">Conclusion</h2> <p><span style="margin-left: 10px;"></span> Garbage collection issues in Java backend systems are rarely just “GC problems.” More often, they are symptoms of how the application allocates, retains, and processes objects under real production load.</p> <p><span style="margin-left: 10px;"></span> For backend engineers, the important skill is not memorizing every GC algorithm. It is understanding the connection between application code and runtime behavior: request allocation patterns, object lifetime, old-generation growth, p99 latency, container memory limits, and observability data. A strong Java developer knows that GC tuning should come after evidence. Before changing JVM flags, look at allocation rate, GC logs, heap usage after collection, live set growth, thread pools, database pools, and request latency.</p> <p><span style="margin-left: 10px;"></span> The core lesson is simple, GC performance starts in application design, not in JVM flags. If most request-scoped objects die young, caches and queues are bounded, ThreadLocals are cleaned up, large payloads are controlled, and GC metrics are correlated with production behavior, the JVM can do its job efficiently :)</p>]]></content><author><name></name></author><summary type="html"><![CDATA[JVM Garbage Collection in Production Systems]]></summary></entry><entry><title type="html">gRPC - Google Remote Procedure Call</title><link href="https://p-almeida12.github.io/blog/2025/grpc/" rel="alternate" type="text/html" title="gRPC - Google Remote Procedure Call"/><published>2025-11-30T14:59:00+00:00</published><updated>2025-11-30T14:59:00+00:00</updated><id>https://p-almeida12.github.io/blog/2025/grpc</id><content type="html" xml:base="https://p-almeida12.github.io/blog/2025/grpc/"><![CDATA[<p><span style="margin-left: 10px;"></span> In this post, I want to consolidate my research on gRPC in a way that feels practical. Although I might be 10 years late to the party, gRPC is a widely used and mature framework.</p> <h2 id="what-is-grpc">What Is gRPC</h2> <p><span style="margin-left: 10px;"></span> At its core, gRPC is an RPC framework built on top of HTTP/2 and Protocol Buffers (protobuf). Instead of thinking about “resources” like <code class="language-plaintext highlighter-rouge">/beers/123</code>, you think in terms of methods you can call on a service:</p> <ul> <li>GetBeerById</li> <li>CreateBeerBatch</li> <li>StreamInventory</li> </ul> <p><span style="margin-left: 10px;"></span> From a single <code class="language-plaintext highlighter-rouge">.proto</code> file, gRPC generates strongly typed clients and servers for multiple languages like Java, Go, Python and many others. One file becomes the source of truth.</p> <h2 id="protocol-buffers">Protocol Buffers</h2> <p><span style="margin-left: 10px;"></span> Everything in gRPC starts with protobuf. Protobuf files define:</p> <ul> <li>Messages: the data structures exchanged between client and server</li> <li>Services: collections of callable RPC methods</li> <li>Methods: how information flows—unary, streaming, etc.</li> </ul> <p><span style="margin-left: 10px;"></span> Here’s a piece of the project I made on gRPC:</p> <div class="language-proto highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="na">syntax</span> <span class="o">=</span> <span class="s">"proto3"</span><span class="p">;</span>

<span class="k">option</span> <span class="na">java_multiple_files</span> <span class="o">=</span> <span class="kc">true</span><span class="p">;</span>
<span class="k">option</span> <span class="na">java_package</span> <span class="o">=</span> <span class="s">"com.palmeida.grpcplayground"</span><span class="p">;</span>
<span class="k">option</span> <span class="na">java_outer_classname</span> <span class="o">=</span> <span class="s">"BeerServiceProto"</span><span class="p">;</span>

<span class="kn">package</span> <span class="nn">beer</span><span class="p">;</span>

<span class="c1">//domain messages</span>
<span class="kd">message</span> <span class="nc">Beer</span> <span class="p">{</span>
  <span class="kt">string</span> <span class="na">id</span> <span class="o">=</span> <span class="mi">1</span><span class="p">;</span>
  <span class="kt">string</span> <span class="na">name</span> <span class="o">=</span> <span class="mi">2</span><span class="p">;</span>
  <span class="n">BeerStyle</span> <span class="na">style</span> <span class="o">=</span> <span class="mi">3</span><span class="p">;</span>
  <span class="kt">double</span> <span class="na">abv</span> <span class="o">=</span> <span class="mi">4</span><span class="p">;</span> <span class="c1">//alcohol by volume</span>
  <span class="kt">uint32</span> <span class="na">ibu</span> <span class="o">=</span> <span class="mi">5</span><span class="p">;</span> <span class="c1">//bitterness</span>
  <span class="kt">uint32</span> <span class="na">stock_quantity</span> <span class="o">=</span> <span class="mi">6</span><span class="p">;</span>
<span class="p">}</span>

<span class="kd">enum</span> <span class="n">BeerStyle</span> <span class="p">{</span>
  <span class="na">STYLE_UNKNOWN</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
  <span class="na">LAGER</span> <span class="o">=</span> <span class="mi">1</span><span class="p">;</span>
  <span class="na">IPA</span> <span class="o">=</span> <span class="mi">2</span><span class="p">;</span>
  <span class="na">STOUT</span> <span class="o">=</span> <span class="mi">3</span><span class="p">;</span>
  <span class="na">PORTER</span> <span class="o">=</span> <span class="mi">4</span><span class="p">;</span>
  <span class="na">PILSNER</span> <span class="o">=</span> <span class="mi">5</span><span class="p">;</span>
  <span class="na">WHEAT</span> <span class="o">=</span> <span class="mi">6</span><span class="p">;</span>
  <span class="na">SOUR</span> <span class="o">=</span> <span class="mi">7</span><span class="p">;</span>
<span class="p">}</span>

<span class="kd">message</span> <span class="nc">CreateBeerRequest</span> <span class="p">{</span>
  <span class="kt">string</span> <span class="na">name</span> <span class="o">=</span> <span class="mi">1</span><span class="p">;</span>
  <span class="n">BeerStyle</span> <span class="na">style</span> <span class="o">=</span> <span class="mi">2</span><span class="p">;</span>
  <span class="kt">double</span> <span class="na">abv</span> <span class="o">=</span> <span class="mi">3</span><span class="p">;</span>
  <span class="kt">uint32</span> <span class="na">ibu</span> <span class="o">=</span> <span class="mi">4</span><span class="p">;</span>
  <span class="kt">uint32</span> <span class="na">initial_stock</span> <span class="o">=</span> <span class="mi">5</span><span class="p">;</span>
<span class="p">}</span>

<span class="kd">message</span> <span class="nc">BeerResponse</span> <span class="p">{</span>
  <span class="n">Beer</span> <span class="na">beer</span> <span class="o">=</span> <span class="mi">1</span><span class="p">;</span>
<span class="p">}</span>

<span class="kd">message</span> <span class="nc">GetBeerByIdRequest</span> <span class="p">{</span>
  <span class="kt">string</span> <span class="na">id</span> <span class="o">=</span> <span class="mi">1</span><span class="p">;</span>
<span class="p">}</span>

<span class="kd">message</span> <span class="nc">ListBeersByStyleRequest</span> <span class="p">{</span>
  <span class="n">BeerStyle</span> <span class="na">style</span> <span class="o">=</span> <span class="mi">1</span><span class="p">;</span>
<span class="p">}</span>

<span class="c1">//client streaming message: client sends multiple beers to be created</span>
<span class="kd">message</span> <span class="nc">BeerBatchItem</span> <span class="p">{</span>
  <span class="kt">string</span> <span class="na">correlation_id</span> <span class="o">=</span> <span class="mi">1</span><span class="p">;</span>
  <span class="n">CreateBeerRequest</span> <span class="na">payload</span> <span class="o">=</span> <span class="mi">2</span><span class="p">;</span>
<span class="p">}</span>

<span class="kd">message</span> <span class="nc">BeerBatchSummary</span> <span class="p">{</span>
  <span class="kt">uint32</span> <span class="na">total_received</span> <span class="o">=</span> <span class="mi">1</span><span class="p">;</span>
  <span class="kt">uint32</span> <span class="na">total_created</span> <span class="o">=</span> <span class="mi">2</span><span class="p">;</span>
  <span class="kt">uint32</span> <span class="na">total_failed</span> <span class="o">=</span> <span class="mi">3</span><span class="p">;</span>
  <span class="k">repeated</span> <span class="kt">string</span> <span class="na">failed_names</span> <span class="o">=</span> <span class="mi">4</span><span class="p">;</span>
<span class="p">}</span>

<span class="c1">//inventory streaming messages for bidirectional streaming</span>
<span class="kd">enum</span> <span class="n">InventoryEventType</span> <span class="p">{</span>
  <span class="na">EVENT_UNKNOWN</span> <span class="o">=</span> <span class="mi">0</span><span class="p">;</span>
  <span class="na">STOCK_INCREASED</span> <span class="o">=</span> <span class="mi">1</span><span class="p">;</span>
  <span class="na">STOCK_DECREASED</span> <span class="o">=</span> <span class="mi">2</span><span class="p">;</span>
<span class="p">}</span>

<span class="kd">message</span> <span class="nc">InventoryEvent</span> <span class="p">{</span>
  <span class="kt">string</span> <span class="na">beer_id</span> <span class="o">=</span> <span class="mi">1</span><span class="p">;</span>
  <span class="n">InventoryEventType</span> <span class="na">type</span> <span class="o">=</span> <span class="mi">2</span><span class="p">;</span>
  <span class="kt">int32</span> <span class="na">quantity_delta</span> <span class="o">=</span> <span class="mi">3</span><span class="p">;</span>
<span class="p">}</span>

<span class="kd">message</span> <span class="nc">InventoryUpdate</span> <span class="p">{</span>
  <span class="kt">string</span> <span class="na">beer_id</span> <span class="o">=</span> <span class="mi">1</span><span class="p">;</span>
  <span class="kt">uint32</span> <span class="na">new_stock_quantity</span> <span class="o">=</span> <span class="mi">2</span><span class="p">;</span>
  <span class="kt">string</span> <span class="kd">message</span> <span class="o">=</span> <span class="mi">3</span><span class="p">;</span>
<span class="p">}</span>

<span class="c1">//service definition using all four RPC types</span>
<span class="kd">service</span> <span class="n">BeerService</span> <span class="p">{</span>

  <span class="c1">//unary: get a beer by id</span>
  <span class="k">rpc</span> <span class="n">GetBeerById</span><span class="p">(</span><span class="n">GetBeerByIdRequest</span><span class="p">)</span> <span class="k">returns</span> <span class="p">(</span><span class="n">BeerResponse</span><span class="p">);</span>

  <span class="c1">//unary: create a beer</span>
  <span class="k">rpc</span> <span class="n">CreateBeer</span><span class="p">(</span><span class="n">CreateBeerRequest</span><span class="p">)</span> <span class="k">returns</span> <span class="p">(</span><span class="n">BeerResponse</span><span class="p">);</span>

  <span class="c1">//server streaming: list all beers by style</span>
  <span class="k">rpc</span> <span class="n">ListBeersByStyle</span><span class="p">(</span><span class="n">ListBeersByStyleRequest</span><span class="p">)</span> <span class="k">returns</span> <span class="p">(</span><span class="n">stream</span> <span class="n">BeerResponse</span><span class="p">);</span>

  <span class="c1">//client streaming: client sends multiple beers to create, server returns summary</span>
  <span class="k">rpc</span> <span class="n">CreateBeerBatch</span><span class="p">(</span><span class="n">stream</span> <span class="n">BeerBatchItem</span><span class="p">)</span> <span class="k">returns</span> <span class="p">(</span><span class="n">BeerBatchSummary</span><span class="p">);</span>

  <span class="c1">//bidirectional streaming:</span>
  <span class="c1">//client sends inventory events (stock in/out),</span>
  <span class="c1">//server responds with continuous inventory updates</span>
  <span class="k">rpc</span> <span class="n">StreamInventory</span><span class="p">(</span><span class="n">stream</span> <span class="n">InventoryEvent</span><span class="p">)</span> <span class="k">returns</span> <span class="p">(</span><span class="n">stream</span> <span class="n">InventoryUpdate</span><span class="p">);</span>
<span class="p">}</span>
</code></pre></div></div> <p><span style="margin-left: 10px;"></span> What I love about protobuf is that it’s engineered for change. The tag numbers (= 1, = 2, etc.) are what matter for the binary encoding. You can reorder fields, rename them, and evolve the schema as long as those tag numbers remain stable.</p> <p><span style="margin-left: 10px;"></span> When compiled, protobuf generates language-specific classes with getters, builders, enums, etc…</p> <h2 id="how-a-grpc-method-works-internally">How a gRPC Method Works Internally</h2> <p><span style="margin-left: 10px;"></span> I like to think of gRPC as “REST but binary” but the internal flow is quite different. A typical unary RPC call looks like this:</p> <ol> <li>Client code calls a method on a stub. e.g. beerServiceStub.getBeerById(request)</li> <li>The stub serializes the request using protobuf</li> <li>It sends the binary payload over an HTTP/2 stream</li> <li>The server receives it, deserializes it into a message</li> <li>The server implementation executes your business logic</li> <li>The response is serialized back and returned on the same stream</li> </ol> <p><span style="margin-left: 10px;"></span> All of this happens behind the scenes. The user only sees strongly typed request/response objects.</p> <h2 id="why-grpc-is-fast">Why gRPC Is Fast</h2> <p><span style="margin-left: 10px;"></span> From my research, I found three major reasons:</p> <h3 id="protobuf--json">Protobuf &gt; JSON</h3> <p><span style="margin-left: 10px;"></span> Binary serialization is significantly lighter—smaller payloads and faster encoding/decoding.</p> <p><span style="margin-left: 10px;"></span> It stores data in byte formats instead of human-readable text, eliminating overhead like quotes, field names, and whitespace. Primitive values are written as fixed or efficiently packed bytes rather than long strings, and schemas allow binary formats to omit repeated metadata.</p> <p><span style="margin-left: 10px;"></span> Since the data maps closely to how CPUs store and manipulate values, encoding and decoding requires far fewer parsing steps and conversions, making the process much faster while producing much smaller serialized messages.</p> <h3 id="http2">HTTP/2</h3> <p><span style="margin-left: 10px;"></span> gRPC sits entirely on HTTP/2, inheriting benefits like multiplexed streams, persistent connections, server push and header compression.</p> <p><span style="margin-left: 10px;"></span> The protocol was designed for streaming from the start.</p> <h3 id="contract-generated-code">Contract-generated code</h3> <p><span style="margin-left: 10px;"></span> There’s no string-based routing, reflection-heavy serialization, or dynamic payload parsing. Everything compiles down to direct method calls</p> <h2 id="requestresponse-types-in-grpc">Request/response types in gRPC</h2> <p><span style="margin-left: 10px;"></span> gRPC defines four fundamental interaction models.</p> <h3 id="unary">Unary</h3> <p><span style="margin-left: 10px;"></span> <strong>Pattern:</strong> single request → single response</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>rpc GetBeerById(GetBeerByIdRequest) returns (BeerResponse);
</code></pre></div></div> <p><span style="margin-left: 10px;"></span> Used for:</p> <ul> <li>standard request/response operations (similar to a typical REST call)</li> <li>fetching or updating a single resource</li> </ul> <p><span style="margin-left: 10px;"></span> In this project:</p> <ul> <li>GetBeerById – looks up a beer by its ID</li> <li>CreateBeer – creates a single beer (also unary)</li> </ul> <h3 id="server-streaming">Server Streaming</h3> <p><span style="margin-left: 10px;"></span> <strong>Pattern:</strong> single request → stream of responses</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>rpc ListBeersByStyle(ListBeersByStyleRequest) returns (stream BeerResponse);
</code></pre></div></div> <p><span style="margin-left: 10px;"></span> Use cases:</p> <ul> <li>sending back a sequence of items without the client having to keep polling</li> <li>real-time updates, or “pages” of data where streaming is more efficient than pagination</li> </ul> <p><span style="margin-left: 10px;"></span> In this project:</p> <ul> <li>the client sends one ListBeersByStyleRequest with a style like IPA</li> <li>the server streams each BeerResponse (every IPA beer) one by one</li> </ul> <p><span style="margin-left: 10px;"></span> This is especially nice when:</p> <ul> <li>results might be large</li> <li>you want to start processing / displaying them before the full list is ready (better user experience)</li> </ul> <h3 id="client-streaming">Client Streaming</h3> <p><span style="margin-left: 10px;"></span> <strong>Pattern:</strong> stream of requests → single response</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>rpc CreateBeerBatch(stream BeerBatchItem) returns (BeerBatchSummary);
</code></pre></div></div> <p><span style="margin-left: 10px;"></span> Use cases:</p> <ul> <li>client wants to upload or send a batch of information</li> <li>server wants to respond with a summary or aggregated result</li> </ul> <p><span style="margin-left: 10px;"></span> In this project:</p> <ul> <li>the client sends multiple BeerBatchItem messages, each wrapping a CreateBeerRequest</li> <li>the server: <ul> <li>processes each BeerBatchItem as it arrives</li> <li>aggregates counts for total_received, total_created, and failures</li> <li>returns a single BeerBatchSummary when the client closes the stream</li> </ul> </li> </ul> <p><span style="margin-left: 10px;"></span> This shows:</p> <ul> <li>handling of per-item validation errors without failing the entire stream</li> <li>batch processing via streaming rather than a massive single request</li> </ul> <h3 id="bidirectional-streaming">Bidirectional Streaming</h3> <p><span style="margin-left: 10px;"></span> <strong>Pattern:</strong> stream of requests ↔ stream of responses (independent)</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>rpc StreamInventory(stream InventoryEvent) returns (stream InventoryUpdate);
</code></pre></div></div> <p><span style="margin-left: 10px;"></span> Use cases:</p> <ul> <li>real-time bidirectional communication</li> <li>chat, live telemetry, streaming analytics, collaborative apps, continuous control channels</li> </ul> <p><span style="margin-left: 10px;"></span> In this project:</p> <ul> <li>the client streams InventoryEvent messages, such as: <ul> <li>“stock increased by 20”</li> <li>“stock decreased by 10”</li> </ul> </li> <li>the server: <ul> <li>applies each event to the domain model</li> <li>immediately streams back an InventoryUpdate with the new stock and a human-readable message</li> </ul> </li> </ul> <p><span style="margin-left: 10px;"></span> This shows:</p> <ul> <li>full duplex communication</li> <li>stateful interactions over a single connection</li> <li>how to coordinate business logic over a long-lived stream</li> </ul> <h2 id="channels-and-stubs-how-clients-connect">Channels and Stubs: How Clients Connect</h2> <p><span style="margin-left: 10px;"></span> In gRPC:</p> <ul> <li>A channel is a long-lived connection to a server (like localhost:9090 I would say)</li> <li>A stub is the client-side interface used to call remote methods</li> </ul> <p><span style="margin-left: 10px;"></span> Example using Spring Boot + gRPC:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>@GrpcClient("beerClient")
private BeerServiceGrpc.BeerServiceBlockingStub blockingStub;
</code></pre></div></div> <p><span style="margin-left: 10px;"></span> Then you just call:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>BeerResponse created = blockingStub.createBeer(createRequest);
</code></pre></div></div> <p><span style="margin-left: 10px;"></span> No REST templates. No serialization code. No URL construction. Just strongly typed method calls (like) calling another class in the same JVM, even if it’s running in another country.</p> <h2 id="grpc-in-the-real-world">gRPC in the Real World</h2> <p><span style="margin-left: 10px;"></span> gRPC is widely used at companies that performance and internal service reliability very seriously, like Google, Netflix, etc…</p> <p><span style="margin-left: 10px;"></span> You’ll see it in:</p> <ul> <li>High-throughput microservices</li> <li>Polyglot server ecosystems</li> <li>Low-latency applications (search, recommendations, ads)</li> <li>Game backends</li> <li>Streaming analytics</li> </ul> <p><span style="margin-left: 10px;"></span> It’s not a public API replacement for REST or GraphQL, gRPC excels behind the scenes, powering internal networks where speed and efficiency matter more than human-readability.</p> <h2 id="security-and-transport">Security and Transport</h2> <p><span style="margin-left: 10px;"></span> gRPC is also very strong in terms of security. Because it’s built on HTTP/2, TLS is not optional, it’s an integral part of how services communicate. In production environments, most teams rely on TLS or mutual TLS (mTLS) for strong authentication and encrypted traffic.</p> <h3 id="tls--mtls">TLS / mTLS</h3> <ul> <li>TLS encrypts traffic and protects data in transit.</li> <li>mTLS adds service-to-service authentication so both the client and server present certificates, which means each party can cryptographically verify the other’s identity. In zero-trust networks, this is the norm.</li> </ul> <h3 id="jwt--oauth">JWT / OAuth</h3> <p><span style="margin-left: 10px;"></span> For user-facing flows or API gateway level auth:</p> <ul> <li>gRPC headers support JWT tokens as metadata</li> <li>OAuth2 tokens flow exactly as they do in REST</li> <li>Interceptors can enforce auth logic at the application layer</li> </ul> <h2 id="error-handling-and-resiliency">Error Handling and Resiliency</h2> <p><span style="margin-left: 10px;"></span> gRPC has a clear error model that’s more structured than HTTP’s status codes, but the mindset is similar.</p> <h3 id="deadlines">Deadlines</h3> <p><span style="margin-left: 10px;"></span> The client specifies how long it’s willing to wait:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>response = blockingStub.withDeadlineAfter(deadlineMs, TimeUnit.MILLISECONDS).sayHello(request);
</code></pre></div></div> <p><span style="margin-left: 10px;"></span> This sets the deadline from when the client RPC is set to when the response is picked up by the client.</p> <p><span style="margin-left: 10px;"></span> Deadlines travel across the call chain, which prevents downstream services from being overloaded by “stuck” upstream calls.</p> <h3 id="cancellations">Cancellations</h3> <p><span style="margin-left: 10px;"></span> Clients can cancel requests to free up resources—especially useful with streaming RPCs or long-running operations.</p> <p><span style="margin-left: 10px;"></span> Also, according to the documentation: “When an RPC is cancelled, the server should stop any ongoing computation and end its side of the stream. Often, servers are also clients to upstream servers, so that cancellation operation should ideally propagate to all ongoing computation in the system that was initiated due to the original client RPC call.”</p> <p><span style="margin-left: 10px;"></span> Refer to <a href="https://github.com/grpc/grpc-java/tree/master/examples/src/main/java/io/grpc/examples/cancellation">https://github.com/grpc/grpc-java/tree/master/examples/src/main/java/io/grpc/examples/cancellation</a> for a detailed example.</p> <h2 id="schema-evolution-and-versioning">Schema Evolution and Versioning</h2> <p><span style="margin-left: 10px;"></span> Protobuf is designed to evolve gracefully, but only if you respect the rules:</p> <ul> <li>Tag numbers are forever. Do not reuse them.</li> <li>The name can change, the tag cannot.</li> </ul> <p><span style="margin-left: 10px;"></span> If you remove a field, reserve its tag and name:</p> <div class="language-proto highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">reserved</span> <span class="mi">3</span><span class="p">;</span>
<span class="n">reserved</span> <span class="s">"old_field_name"</span><span class="p">;</span>
</code></pre></div></div> <p><span style="margin-left: 10px;"></span> This prevents accidental reuse.</p> <h2 id="when-to-use-and-not-use-grpc">When to Use (and Not Use) gRPC</h2> <h3 id="when-grpc-is-a-great-fit">When gRPC Is a Great Fit</h3> <ul> <li>Service-to-service communication inside a microservices architecture</li> <li>High-performance, low-latency environments</li> <li>Polyglot systems where shared types matter</li> <li>Real-time streaming (telemetry, analytics)</li> </ul> <h3 id="when-grpc-is-not-ideal">When gRPC Is Not Ideal</h3> <ul> <li>Public APIs: REST or GraphQL are more approachable</li> <li>Browser clients: gRPC-Web works but is still limited compared to REST</li> <li>Human-readable debugging: binary payloads require tooling</li> <li>Large file uploads: sometimes REST with multipart/form-data is simply easier</li> </ul> <p><span style="margin-left: 10px;"></span> I would choose gRPC for internal systems, not as your universal external API.</p>]]></content><author><name></name></author><summary type="html"><![CDATA[gRPC overview, architecture, and practical use cases]]></summary></entry><entry><title type="html">Elasticsearch - My homework</title><link href="https://p-almeida12.github.io/blog/2025/elastic-search/" rel="alternate" type="text/html" title="Elasticsearch - My homework"/><published>2025-11-30T14:59:00+00:00</published><updated>2025-11-30T14:59:00+00:00</updated><id>https://p-almeida12.github.io/blog/2025/elastic-search</id><content type="html" xml:base="https://p-almeida12.github.io/blog/2025/elastic-search/"><![CDATA[<p><span style="margin-left: 10px;"></span> Elasticsearch is often introduced as “a search engine” or “a document store.” Both descriptions are incomplete and, in production systems, misleading. Elasticsearch is a distributed, near-real-time search and analytics engine built on top of Apache Lucene, optimized for fast reads, flexible querying, and horizontal scalability.</p> <h2 id="what-elasticsearch-is-and-what-it-is-not">What Elasticsearch Is (And What It Is Not)</h2> <h3 id="elasticsearch-as-a-distributed-lucene-engine">Elasticsearch as a Distributed Lucene Engine</h3> <p><span style="margin-left: 10px;"></span> At its core, Elasticsearch is a coordinator over many Lucene indices. Each index is split into shards, and each shard is a self-contained Lucene index with its own:</p> <ul> <li>inverted indices</li> <li>Segment files</li> <li>Term dictionaries</li> <li>Doc values</li> </ul> <p><span style="margin-left: 10px;"></span> When you index a document:</p> <ul> <li>The document is routed to a shard</li> <li>It is analyzed (tokenized, filtered, normalized)</li> <li>It is written to an in-memory buffer</li> <li>It becomes searchable after a refresh (near-real-time, not immediate)</li> <li>It is eventually flushed to disk as immutable segments</li> </ul> <p><span style="margin-left: 10px;"></span> This design has consequences:</p> <ul> <li>Writes are fast, but not transactional</li> <li>Reads are fast, but slightly stale</li> <li>Updates are actually delete + reinsert operations</li> </ul> <h3 id="what-elasticsearch-is-used-for">What Elasticsearch Is Used For</h3> <p><span style="margin-left: 10px;"></span> Elasticsearch excels at:</p> <ul> <li>Full-text search</li> <li>Fuzzy matching and relevance scoring</li> <li>Faceted navigation</li> <li>Aggregations and analytics</li> <li>Log and event exploration</li> <li>Autocomplete and suggestions</li> </ul> <p><span style="margin-left: 10px;"></span> It is not well suited for:</p> <ul> <li>Strong consistency</li> <li>Complex joins</li> <li>Multi-row transactions</li> <li>High-frequency point updates</li> <li>Referential integrity</li> </ul> <p><span style="margin-left: 10px;"></span> In production systems, Elasticsearch is almost always a secondary system, derived from a primary data source.</p> <h3 id="near-real-time-and-eventual-consistency">Near-Real-Time and Eventual Consistency</h3> <p><span style="margin-left: 10px;"></span> Elasticsearch is near-real-time, not real-time. There is always a delay between indexing and visibility. This delay is acceptable for search, but disastrous if misunderstood.</p> <p><span style="margin-left: 10px;"></span> Any architecture that assumes:</p> <ul> <li>Immediate visibility</li> <li>Strong consistency</li> <li>Exactly-once semantics</li> </ul> <p>…will eventually produce incorrect results.</p> <h2 id="how-elasticsearch-actually-works">How Elasticsearch Actually Works</h2> <h3 id="data-organization-and-the-inverted-index">Data Organization and the Inverted Index</h3> <p><span style="margin-left: 10px;"></span> At the lowest level, Elasticsearch relies on Lucene’s inverted index — a data structure optimized for fast full-text search. Instead of scanning documents on query time, Elasticsearch pre-processes content into a lookup structure that maps every unique term (token) to the documents in which it appears. This inversion of document -&gt; terms to term -&gt; documents enables milliseconds-level search even across very large datasets.</p> <p><span style="margin-left: 10px;"></span> For example, a field containing “Elasticsearch tutorial” is tokenized into terms like “elasticsearch” and “tutorial”, and the inverted index tracks which documents contain those terms.</p> <h3 id="what-is-an-index">What Is an Index?</h3> <p><span style="margin-left: 10px;"></span> In Elasticsearch, an index is a logical namespace for storing related documents — conceptually similar to a database in RDBMS terms (if it even makes sense). An index is the unit you interact with when performing searches and aggregations. Internally, however, an index is not a single monolithic structure; it’s a logical grouping of shards.</p> <p><span style="margin-left: 10px;"></span> Documents in an index are JSON objects. Each document has:</p> <ul> <li>A set of fields (key/value pairs)</li> <li>A unique identifier</li> <li>A type-agnostic mapping that determines how each field is indexed and searched</li> </ul> <p><span style="margin-left: 10px;"></span> Once data hits the index, it goes through analysis — tokenization and normalization — before being persisted in the inverted index.</p> <h3 id="shards">Shards</h3> <p><span style="margin-left: 10px;"></span> An index is partitioned into shards — the basic units of storage and parallelism. Each shard is itself a full Lucene index with its own inverted index and segment files, and shards can be located on different nodes in a cluster.</p> <p><span style="margin-left: 10px;"></span> There are two shard types:</p> <ul> <li>Primary shards — hold the original data and are the destination for write operations</li> <li>Replica shards — copies of primary shards that provide redundancy and read scalability</li> </ul> <p><span style="margin-left: 10px;"></span> Shard distribution across nodes achieves three major goals:</p> <ol> <li>Horizontal Scalability: More shards can be spread across more nodes, boosting capacity.</li> <li>Fault Tolerance: If a node fails, replicas on other nodes ensure availability.</li> <li>Parallel Query Execution: Queries are executed on all relevant shards in parallel, with the results merged by a coordinating node.</li> </ol> <p><span style="margin-left: 10px;"></span> Once you create an index, the number of primary shards is fixed. Changing it later requires reindexing. Replicas, by contrast, can be adjusted dynamically.</p> <p><span style="margin-left: 10px;"></span> Shard count is one of the most consequential design decisions in Elasticsearch. Every shard is a full Lucene index with its own segment files, caches, and metadata. This means shards are not free — they consume heap, file descriptors, and CPU scheduling overhead.</p> <p><span style="margin-left: 10px;"></span> Oversharding is one of the most common production failures. Clusters with thousands of tiny shards often experience:</p> <ul> <li>High cluster state update latency</li> <li>Excessive heap pressure on master nodes</li> <li>Slow shard allocation and recovery</li> <li>Increased query fan-out</li> </ul> <p><span style="margin-left: 10px;"></span> A practical production rule is:</p> <ul> <li>Target shard sizes between <strong>10GB and 50GB</strong></li> <li>Keep shard counts <strong>below ~20–40 shards per node</strong></li> <li>Prefer <strong>fewer larger shards</strong> over many small ones</li> </ul> <p><span style="margin-left: 10px;"></span> Shard sizing is a capacity planning exercise, not just a data partitioning strategy. It determines how efficiently a cluster can scale, recover, and execute queries in parallel.</p> <h3 id="nodes-and-their-roles">Nodes and Their Roles</h3> <p><span style="margin-left: 10px;"></span> A node is a single Elasticsearch process (typically on a single machine). Nodes collaborate within a cluster and can have specialized roles:</p> <ul> <li>Master-eligible nodes — manage cluster metadata and orchestrate shard allocation</li> <li>Data nodes — actually store shards and perform indexing/search operations</li> <li>Ingest nodes — preprocess documents before indexing (e.g., enrich or transform)</li> <li>Coordinating (client) nodes — accept client requests and forward them to the relevant shards</li> </ul> <p><span style="margin-left: 10px;"></span> Roles can be mixed or dedicated depending on workload and scale. In heavy-production environments, it’s common to isolate master nodes to reduce load and prevent instability during high query or indexing throughput.</p> <h3 id="cluster">Cluster</h3> <p><span style="margin-left: 10px;"></span> A cluster is a group of nodes that share the same cluster name and work together. The cluster state — a shared data structure that tracks indices, shards, mapping schemas, node membership, and more — is maintained by the elected master. Any change affecting the topology (e.g., new index, node joining) updates the cluster state and propagates it to all nodes.</p> <p><span style="margin-left: 10px;"></span> Clusters allow Elasticsearch to behave as a single logical system rather than a bunch of independent servers.</p> <h3 id="data-flow">Data Flow</h3> <h4 id="indexing">Indexing</h4> <ol> <li>A client sends a JSON document to a coordinating node</li> <li>The coordinating node routes it to a specific primary shard</li> <li>The document is analyzed and written to a translog and memory buffer</li> <li>The coordinating node forwards the operation to replica shards</li> <li>After an index refresh, the document becomes searchable</li> </ol> <p><span style="margin-left: 10px;"></span> A critical nuance here is that indexing is near-real-time: documents only become visible after a refresh, which happens automatically but is tunable.</p> <p><span style="margin-left: 10px;"></span> Indexing documents one at a time is rarely acceptable in production systems. Elasticsearch is optimized for batch ingestion using the <strong>Bulk API</strong>, which allows many indexing operations to be processed in a single request.</p> <p><span style="margin-left: 10px;"></span> Production systems typically batch documents into groups of hundreds or thousands before sending them to Elasticsearch. This approach reduces network overhead and significantly improves throughput.</p> <p><span style="margin-left: 10px;"></span> However, aggressive batching without flow control can overwhelm a cluster. Indexing pipelines must include backpressure mechanisms to avoid saturating:</p> <ul> <li>thread pools</li> <li>disk I/O</li> <li>translog writes</li> <li>refresh cycles</li> </ul> <p>Common production practices include:</p> <ul> <li>limiting concurrent bulk requests</li> <li>dynamically adjusting batch size</li> <li>retrying rejected requests</li> <li>routing failed operations to a dead-letter queue</li> </ul> <p>A stable indexing pipeline is not defined by maximum throughput, but by <strong>sustained throughput under load without destabilizing the cluster</strong>.</p> <h4 id="searching">Searching</h4> <ol> <li>A search request reaches any node (often a coordinator)</li> <li>The coordinator identifies which shards (primaries and replicas) need to be queried</li> <li>Queries execute in parallel on each shard</li> <li>Partial results return to the coordinator</li> <li>Results are merged, scored, sorted, and sent back to the client</li> </ol> <p><span style="margin-left: 10px;"></span> Because shards act in parallel, overall latency is bounded by the slowest responding shard.</p> <h4 id="replication-and-high-availability">Replication and High Availability</h4> <p><span style="margin-left: 10px;"></span> Replica shards exist on different nodes from their primaries. This protects the cluster against machine failures — if a node with primaries fails, its replicas can be promoted. Replica shards also serve read operations, increasing throughput for search and retrieval.</p> <h2 id="the-production-contract-slos-consistency-and-search-truth">The Production Contract: SLOs, Consistency, and “Search Truth”</h2> <p><span style="margin-left: 10px;"></span> Before Elasticsearch becomes useful in production, it must be demoted. It is not the source of truth. It is not a transactional store. It is a derived system whose correctness is defined by explicit service-level objectives, not by database guarantees.</p> <p><span style="margin-left: 10px;"></span> Production systems that succeed with Elasticsearch do so because they define — early and explicitly — what Elasticsearch is allowed to be wrong about.</p> <h3 id="elasticsearch-is-not-a-consistent-read-model">Elasticsearch Is Not a Consistent Read Model</h3> <p><span style="margin-left: 10px;"></span> Elasticsearch provides near-real-time visibility, not linearizability and not read-your-writes semantics. There is always a window where:</p> <ul> <li>A document has been acknowledged as indexed</li> <li>The same document is not yet visible to search</li> <li>Different replicas may expose different views</li> </ul> <p><span style="margin-left: 10px;"></span> This is not a bug. It is a deliberate tradeoff that enables high write throughput and low-latency search.</p> <p><span style="margin-left: 10px;"></span> Any architecture that assumes:</p> <ul> <li>Immediate visibility after indexing</li> <li>Monotonic reads</li> <li>Exactly-once processing</li> </ul> <p><span style="margin-left: 10px;"></span> …will eventually violate correctness under load, node failure, or shard relocation.</p> <h3 id="defining-the-search-contract">Defining the Search Contract</h3> <p><span style="margin-left: 10px;"></span> In production, Elasticsearch must operate under an explicit search contract, typically defined per use case:</p> <table> <thead> <tr> <th>Use Case</th> <th>Allowed Staleness</th> <th>Correctness Expectation</th> </tr> </thead> <tbody> <tr> <td>Full-text search</td> <td>Seconds</td> <td>Eventually consistent</td> </tr> <tr> <td>Autocomplete</td> <td>Sub-second</td> <td>Best-effort</td> </tr> <tr> <td>Analytics dashboards</td> <td>Minutes</td> <td>Approximate</td> </tr> <tr> <td>User-facing filtering</td> <td>Seconds</td> <td>Deterministic but stale</td> </tr> <tr> <td>Compliance / auditing</td> <td>Never</td> <td>Do not use Elasticsearch</td> </tr> </tbody> </table> <p><span style="margin-left: 10px;"></span> This contract drives:</p> <ul> <li>Refresh interval configuration</li> <li>Indexing pipeline design</li> <li>Retry and reconciliation strategies</li> <li>User-facing guarantees (or lack thereof)</li> </ul> <p><span style="margin-left: 10px;"></span> If you cannot write this table for your system, Elasticsearch is being used without a safety net.</p> <h3 id="system-of-record-and-repairability">System of Record and Repairability</h3> <p><span style="margin-left: 10px;"></span> In well-designed systems, Elasticsearch is always downstream from a system of record:</p> <ul> <li>Relational database</li> <li>Event log</li> <li>Immutable data lake</li> </ul> <p><span style="margin-left: 10px;"></span> This is not just a data flow choice — it is an operational escape hatch.</p> <p><span style="margin-left: 10px;"></span> When (not if) Elasticsearch drifts:</p> <ul> <li>Documents can be replayed</li> <li>Indices can be rebuilt</li> <li>Mappings can be corrected</li> <li>Relevance models can be retrained</li> </ul> <p><span style="margin-left: 10px;"></span> If rebuilding an index is considered catastrophic, the architecture is already fragile.</p> <h3 id="read-after-write-when-you-actually-need-it">Read-After-Write: When You Actually Need It</h3> <p><span style="margin-left: 10px;"></span> Occasionally, systems do require immediate visibility — for example, after a user edits content and expects to see it reflected instantly.</p> <p><span style="margin-left: 10px;"></span> In these cases, production systems do not change Elasticsearch’s consistency model. Instead, they:</p> <ul> <li>Read directly from the primary datastore</li> <li>Overlay recent writes in-memory</li> <li>Delay search exposure explicitly</li> <li>Or accept temporary inconsistency in UI</li> </ul> <p><span style="margin-left: 10px;"></span> Forcing Elasticsearch to behave like a transactional database (e.g., via frequent refreshes or synchronous indexing) trades correctness illusions for throughput collapse.</p> <h3 id="the-hidden-failure-mode-false-confidence">The Hidden Failure Mode: False Confidence</h3> <p><span style="margin-left: 10px;"></span> The most dangerous failure mode in Elasticsearch is not downtime — it is silent incorrectness.</p> <p><span style="margin-left: 10px;"></span> Search results that are:</p> <ul> <li>Slightly stale</li> <li>Incompletely indexed</li> <li>Missing edge cases</li> </ul> <p><span style="margin-left: 10px;"></span> …are often accepted by systems and users without detection.</p> <p><span style="margin-left: 10px;"></span> This is why production Elasticsearch is designed around:</p> <ul> <li>Rebuildability</li> <li>Observability</li> <li>Explicit correctness boundaries</li> </ul> <p><span style="margin-left: 10px;"></span> Once those boundaries are clear, Elasticsearch becomes an extremely powerful component — not because it is always right, but because it is predictably wrong in controlled ways.</p> <h2 id="mapping-strategy-how-you-avoid-reindexing-yourself-into-a-corner">Mapping Strategy: How You Avoid Reindexing Yourself Into a Corner</h2> <p><span style="margin-left: 10px;"></span> In Elasticsearch, mappings are not a convenience feature — they are your schema. Unlike relational databases, this schema is not enforced at write time in a transactional sense, but it is baked permanently into the index’s physical structure. Once a field is indexed a certain way, that decision cannot be undone without reindexing.</p> <p><span style="margin-left: 10px;"></span> Production systems treat mappings as versioned, reviewed artifacts, not as emergent side effects of incoming data.</p> <h3 id="index-lifecycle-management-ilm">Index Lifecycle Management (ILM)</h3> <p><span style="margin-left: 10px;"></span> Data rarely has the same value forever. Log data, metrics, and event streams often grow continuously and require automated lifecycle management.</p> <p><span style="margin-left: 10px;"></span> Elasticsearch provides <strong>Index Lifecycle Management (ILM)</strong> to automate index transitions across storage tiers and retention policies.</p> <p><span style="margin-left: 10px;"></span> Typical lifecycle phases include:</p> <ul> <li><strong>Hot phase</strong> – active indexing and frequent queries</li> <li><strong>Warm phase</strong> – less frequent queries, optimized storage</li> <li><strong>Cold phase</strong> – infrequent access, cheaper hardware</li> <li><strong>Delete phase</strong> – automatic removal after retention expires</li> </ul> <p><span style="margin-left: 10px;"></span> ILM policies can automatically trigger actions such as:</p> <ul> <li>index rollover when size thresholds are reached</li> <li>shard shrinking</li> <li>segment merging</li> <li>index deletion</li> </ul> <p><span style="margin-left: 10px;"></span> Without lifecycle management, clusters accumulate historical data indefinitely and eventually become unstable due to excessive shard counts and storage pressure.</p> <h2 id="query-cost-and-cluster-stability">Query Cost and Cluster Stability</h2> <p><span style="margin-left: 10px;"></span> Not all queries are equal. Some query patterns are extremely expensive and can destabilize clusters under load.</p> <p><span style="margin-left: 10px;"></span> Production Elasticsearch deployments explicitly control or prohibit queries such as:</p> <ul> <li>leading wildcard searches (<code class="language-plaintext highlighter-rouge">*term</code>)</li> <li>regular expression queries</li> <li>large cardinality aggregations</li> <li>script-based scoring</li> <li>unbounded nested queries</li> </ul> <p><span style="margin-left: 10px;"></span> These operations may trigger full index scans or large in-memory structures that significantly increase CPU usage and heap pressure.</p> <p><span style="margin-left: 10px;"></span> In multi-tenant systems or public APIs, query complexity is often restricted through:</p> <ul> <li>server-side query templates</li> <li>query validation layers</li> <li>request timeouts</li> <li>rate limiting</li> </ul> <p><span style="margin-left: 10px;"></span> A single poorly constructed query can consume more resources than thousands of normal queries. For this reason, production systems treat query design as an operational concern, not just an application concern.</p> <h3 id="deep-pagination-and-why-it-breaks-search">Deep Pagination and Why It Breaks Search</h3> <p><span style="margin-left: 10px;"></span> Elasticsearch supports pagination using the <code class="language-plaintext highlighter-rouge">from</code> and <code class="language-plaintext highlighter-rouge">size</code> parameters, but this mechanism becomes increasingly expensive for deep result pages.</p> <p><span style="margin-left: 10px;"></span> When a query requests results far into a result set (for example <code class="language-plaintext highlighter-rouge">from=10000</code>), every shard must still compute and sort all preceding results. This causes large memory allocations and unnecessary work across the cluster.</p> <p>For deep pagination scenarios, production systems instead rely on:</p> <ul> <li><code class="language-plaintext highlighter-rouge">search_after</code> for cursor-like pagination</li> <li>the <code class="language-plaintext highlighter-rouge">scroll</code> API for large batch exports</li> <li>user interface limits that prevent navigating arbitrarily deep result pages</li> </ul> <p><span style="margin-left: 10px;"></span> Deep pagination is rarely meaningful for users and often becomes a hidden performance trap for search clusters.</p> <h3 id="dynamic-mapping">Dynamic Mapping</h3> <p><span style="margin-left: 10px;"></span> By default, Elasticsearch will infer field types dynamically. This is useful for experimentation and catastrophic in production.</p> <p><span style="margin-left: 10px;"></span> Dynamic mapping failures are subtle:</p> <ul> <li>A field starts as keyword, later becomes text</li> <li>Numeric fields oscillate between long and float</li> <li>Date formats diverge across producers</li> <li>User-generated JSON explodes into thousands of distinct fields</li> </ul> <p><span style="margin-left: 10px;"></span> Once indexed, these mistakes cannot be corrected in-place.</p> <p><span style="margin-left: 10px;"></span> Production-grade indices either:</p> <ul> <li>Disable dynamic mapping entirely, or</li> <li>Constrain it with dynamic: strict and explicit templates</li> </ul> <p><span style="margin-left: 10px;"></span> If a document fails to index due to an unexpected field, that is a feature, not a bug — it forces schema drift to surface early.</p> <h3 id="field-types-are-query-decisions">Field Types Are Query Decisions</h3> <p><span style="margin-left: 10px;"></span> Every field type encodes assumptions about how the data will be queried.</p> <p><span style="margin-left: 10px;"></span> Common production patterns include:</p> <ul> <li>text for full-text search</li> <li>keyword for filtering, sorting, and aggregations</li> <li>Multi-fields (text + keyword) when both behaviors are required</li> <li>scaled_float for monetary or precision-sensitive numeric data</li> <li>date with explicit formats, never defaults</li> </ul> <p><span style="margin-left: 10px;"></span> Choosing a field type is not about storage — it is about:</p> <ul> <li>Whether the field participates in relevance scoring</li> <li>Whether it can be aggregated efficiently</li> <li>Whether it can be sorted without loading fielddata into heap</li> </ul> <p><span style="margin-left: 10px;"></span> These decisions directly affect query latency, heap pressure, and cluster stability.</p> <h3 id="analyzers">Analyzers</h3> <p><span style="margin-left: 10px;"></span> Analyzers determine what a field means in search. Tokenization, normalization, stemming, synonym expansion — these are not technical details; they are product semantics.</p> <p><span style="margin-left: 10px;"></span> Production systems:</p> <ul> <li>Explicitly define analyzers per field</li> <li>Version analyzer configurations</li> <li>Avoid global analyzer reuse across unrelated fields</li> </ul> <p><span style="margin-left: 10px;"></span> Changing an analyzer changes the inverted index. This means:</p> <ul> <li>Existing documents do not retroactively benefit</li> <li>Queries become inconsistent across old and new data</li> <li>A full reindex is required for correctness</li> </ul> <p><span style="margin-left: 10px;"></span> Treat analyzer changes with the same gravity as a schema migration in a relational database.</p> <h3 id="field-explosion-and-cluster-state-pressure">Field Explosion and Cluster State Pressure</h3> <p><span style="margin-left: 10px;"></span> Every mapped field contributes to:</p> <ul> <li>Cluster state size</li> <li>Heap usage on master nodes</li> <li>Mapping propagation latency</li> </ul> <p><span style="margin-left: 10px;"></span> Field explosion — often caused by unbounded user input or polymorphic JSON blobs — is one of the fastest ways to destabilize a cluster.</p> <p><span style="margin-left: 10px;"></span> Production mitigations include:</p> <ul> <li>Flattening or denormalizing data intentionally</li> <li>Using flattened fields for schemaless blobs</li> <li>Rejecting documents with excessive field counts</li> <li>Separating “searchable” data from “stored” data</li> </ul> <p><span style="margin-left: 10px;"></span> If your cluster state grows faster than your data volume, mapping design is already broken.</p> <h3 id="mappings-as-versioned-infrastructure">Mappings as Versioned Infrastructure</h3> <p><span style="margin-left: 10px;"></span> In production, mappings are:</p> <ul> <li>Stored alongside application code</li> <li>Reviewed like API contracts</li> <li>Applied through index templates</li> <li>Versioned with explicit lifecycle rules</li> </ul> <p><span style="margin-left: 10px;"></span> A common pattern is to treat each breaking mapping change as a new index version:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>products_v12
products_v13
</code></pre></div></div> <p><span style="margin-left: 10px;"></span> This enables:</p> <ul> <li>Zero-downtime migrations</li> <li>Side-by-side validation</li> <li>Safe rollback via alias switching</li> </ul> <p><span style="margin-left: 10px;"></span> If your deployment process cannot create a new index with a new mapping at will, Elasticsearch is already constraining your delivery velocity.</p> <h3 id="the-core-rule">The Core Rule</h3> <p><span style="margin-left: 10px;"></span> You can evolve queries freely. You can evolve documents carefully. You cannot evolve mappings without reindexing.</p> <p><span style="margin-left: 10px;"></span> Production Elasticsearch systems succeed not because they avoid reindexing — but because they design for it from day one.</p> <h2 id="the-end">The End</h2> <p><span style="margin-left: 10px;"></span> Although I never used Elasticsearch in production, I have a much deeper understanding of how it works and what it is for after writing these “notes”. I’m aware that true knowledge comes from experience, but I hope this research will help me avoid common errors when I eventually do use it in production. If I will ever do it, that is.</p> <p><span style="margin-left: 10px;"></span> I may not ever use Elasticsearch in production, but I always like to understand how things work. I think that understanding the inner workings of a technology can help me make better decisions about when and how to use it, even if I never end up using it directly.</p>]]></content><author><name></name></author><summary type="html"><![CDATA[Discovering Elasticsearch and researching how it works in production systems, what are common problems and how to fix them.]]></summary></entry><entry><title type="html">SOAP, REST, and GraphQL - My experience</title><link href="https://p-almeida12.github.io/blog/2025/soap-rest-graphql/" rel="alternate" type="text/html" title="SOAP, REST, and GraphQL - My experience"/><published>2025-08-04T14:59:00+00:00</published><updated>2025-08-04T14:59:00+00:00</updated><id>https://p-almeida12.github.io/blog/2025/soap-rest-graphql</id><content type="html" xml:base="https://p-almeida12.github.io/blog/2025/soap-rest-graphql/"><![CDATA[<p><span style="margin-left: 10px;"></span> APIs define how services talk, how frontends fetch data, and ultimately how systems scale. Throughout my career, I’ve spent most of my time working with REST APIs in enterprise contexts.</p> <p><span style="margin-left: 10px;"></span> In a recent personal project with a friend, I migrated from REST to GraphQL (using Netflix DGS) to overcome data-fetching inefficiencies. I’ve also tinkered with SOAP, though never in production — enough to appreciate its design philosophy and understand why it still powers parts of the enterprise world.</p> <p><span style="margin-left: 10px;"></span> This post is my deep dive into SOAP, REST, and GraphQL — how they differ, how they compare (or don’t), and what lessons I’ve learned from working with them.</p> <h1 id="are-soap-rest-and-graphql-comparable">Are SOAP, REST, and GraphQL Comparable?</h1> <p><span style="margin-left: 10px;"></span> At first glance, these three might seem like direct competitors — but they’re not perfectly comparable.</p> <table> <thead> <tr> <th><strong>Comparison Aspect</strong></th> <th><strong>SOAP</strong></th> <th><strong>REST</strong></th> <th><strong>GraphQL</strong></th> </tr> </thead> <tbody> <tr> <td><strong>Category</strong></td> <td>Communication protocol.</td> <td>Architectural style</td> <td>Query language.</td> </tr> <tr> <td><strong>Level of abstraction</strong></td> <td>Low-level, rigid.</td> <td>Conceptual and flexible</td> <td>Declarative and client-driven.</td> </tr> <tr> <td><strong>Intent</strong></td> <td>Standardized data exchange.</td> <td>Resource-oriented access</td> <td>Flexible, client-specific data queries.</td> </tr> </tbody> </table> <p><span style="margin-left: 10px;"></span> In short:</p> <ul> <li>SOAP is a protocol — like a contractually defined tunnel between systems.</li> <li>REST is a style — a set of guiding principles for web-based resource communication.</li> <li>GraphQL is a language/runtime layered on top of HTTP — a tool to query data in a declarative, efficient way.</li> </ul> <p><span style="margin-left: 10px;"></span> So while they all enable API communication, they do it at different levels of abstraction and serve different design goals. Comparing them might seem wrong, but its possible, we just need to keep in mind that they are different and optimized for different situations.</p> <h2 id="soap">SOAP</h2> <p><span style="margin-left: 10px;"></span> SOAP (Simple Object Access Protocol) was designed for interoperability in large, distributed systems. It enforces a strict structure using XML envelopes, WSDL contracts, and WS-extensions for things like security, transactions, and reliable messaging.</p> <p><span style="margin-left: 10px;"></span> SOAP message example:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>&lt;soap:Envelope xmlns:soap="http://schemas.xmlsoap.org/soap/envelope/"&gt;
  &lt;soap:Header&gt;
    &lt;auth:Token&gt;VerYsECUreTOkeN124&lt;/auth:Token&gt;
  &lt;/soap:Header&gt;
  &lt;soap:Body&gt;
    &lt;m:GetBeer xmlns:m="http://goodbeer.com/beers"&gt;
      &lt;m:BeerId&gt;42&lt;/m:BeerId&gt;
    &lt;/m:GetBeer&gt;
  &lt;/soap:Body&gt;
&lt;/soap:Envelope&gt;
</code></pre></div></div> <h3 id="architecture">Architecture</h3> <p><span style="margin-left: 10px;"></span> SOAP follows a strictly layered and contract-driven architecture. It was designed to ensure interoperability between systems written in different languages and running on different platforms. The architecture is protocol-centric, with a focus on message structure, transport abstraction, and formal service contracts.</p> <p><span style="margin-left: 10px;"></span> At its core, a SOAP system consists of:</p> <ul> <li>SOAP Envelope — the root XML element that defines the start and end of the message.</li> <li>Header — optional section that carries metadata such as authentication tokens, transaction IDs, or routing information.</li> <li>Body — contains the actual request or response payload.</li> <li>Fault Element — a special XML element used for standardized error reporting.</li> </ul> <p><span style="margin-left: 10px;"></span> SOAP services are usually defined by a WSDL (Web Services Description Language) file. The WSDL acts as a contract that describes:</p> <ul> <li>The available operations (methods) and their parameters.</li> <li>The XML schema (XSD) defining the structure of input/output data.</li> <li>The binding details (which protocol and encoding are used).</li> <li>The service endpoint (where the service lives).</li> </ul> <p><span style="margin-left: 10px;"></span> When a client consumes a SOAP service, it typically:</p> <ol> <li>Parses the WSDL to generate client-side proxy classes.</li> <li>Constructs a XML request message conforming to the schema.</li> <li>Sends the message over a chosen transport (HTTP, SMTP, or others).</li> <li>Receives an XML response and deserializes it into native objects.</li> </ol> <p><span style="margin-left: 10px;"></span> Because SOAP is transport-independent, it can use multiple communication protocols. HTTP is the most common, but SOAP can also run over JMS (Java Message Service) for asynchronous enterprise communication.</p> <p><span style="margin-left: 10px;"></span> This rigidity brings predictability and strong validation, but also makes SOAP services heavier and harder to evolve. Every change in schema or contract typically requires regenerating stubs and revalidating clients.</p> <h3 id="strengths-and-weaknesses">Strengths and Weaknesses</h3> <table> <thead> <tr> <th><strong>Pros</strong></th> <th><strong>Cons</strong></th> </tr> </thead> <tbody> <tr> <td>Strong typing and contract-first design with WSDL</td> <td>Verbose XML payloads increase complexity</td> </tr> <tr> <td>Built-in standards for security and reliability (WS-Security, WS-ReliableMessaging)</td> <td>Harder to integrate with lightweight clients (web, mobile)</td> </tr> <tr> <td>Transport-agnostic (HTTP, SMTP, TCP, etc.)</td> <td>Tooling can feel heavy and outdated</td> </tr> <tr> <td>Excellent for regulated industries (finance, telecom, insurance)</td> <td>Difficult to evolve and version APIs flexibly</td> </tr> </tbody> </table> <p><span style="margin-left: 10px;"></span> When I experimented with SOAP, I found it, for lack of a better word, “elegant”: you get guarantees, schemas, and rigid consistency. But in practice, the verbosity and dependency on specialized tooling (like JAX-WS or WSDL generators) made it feel awkward for modern distributed systems.</p> <p><span style="margin-left: 10px;"></span> SOAP is a tank — extremely reliable but slow and rigid. For systems where every transaction must be traceable, it still earns its place. But for web APIs that need agility, it’s probably not my first choice.</p> <h2 id="rest">REST</h2> <p><span style="margin-left: 10px;"></span> REST (Representational State Transfer) redefined how we think about APIs. Instead of operations, REST focuses on resources — entities exposed through predictable URLs and standard HTTP verbs.</p> <p><span style="margin-left: 10px;"></span> For example:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>GET /beers/123
PUT /beers/123
DELETE /beers/123
</code></pre></div></div> <p><span style="margin-left: 10px;"></span> REST emphasizes:</p> <ul> <li>Statelessness — each request contains everything needed.</li> <li>Uniform interface — clients and servers communicate via standard verbs.</li> <li>Cacheability — responses can be cached via HTTP.</li> <li>Layered system — intermediaries can optimize performance.</li> </ul> <h3 id="architecture-1">Architecture</h3> <p><span style="margin-left: 10px;"></span> REST is an architectural style, not a protocol. Its architecture revolves around a set of constraints defined by Roy Fielding, which make web systems scalable, cacheable, and loosely coupled.</p> <p><span style="margin-left: 10px;"></span> A RESTful system is organized around resources — conceptual entities exposed via URIs. Each resource can have multiple representations (usually JSON or XML) and is manipulated through standard HTTP verbs.</p> <p><span style="margin-left: 10px;"></span> The key architectural constraints of REST are:</p> <ol> <li>Client-Server Separation — clients handle UI and state, servers handle data and logic.</li> <li>Statelessness — every request from the client must contain all information needed by the server. The server doesn’t store client session state.</li> <li>Cacheability — responses must define themselves as cacheable or not, enabling intermediate caches (CDNs, proxies).</li> <li>Uniform Interface — consistent behavior across all endpoints via HTTP verbs and standard response codes.</li> <li>Layered System — clients don’t need to know whether they’re talking directly to the server or through intermediaries.</li> <li>Optional Code on Demand — servers can return executable code (e.g., JavaScript) to clients.</li> </ol> <p><span style="margin-left: 10px;"></span> A typical REST architecture looks like this:</p> <ul> <li>Client layer — browsers, mobile apps, or other services making HTTP calls.</li> <li>API Gateway / Load Balancer — handles routing, authentication, rate limiting.</li> <li>Application layer — REST controllers or handlers process requests, apply business logic, and access data sources.</li> <li>Persistence layer — databases or external systems providing the underlying data.</li> </ul> <p><span style="margin-left: 10px;"></span> Data is exchanged through representations (usually JSON), with HATEOAS (Hypermedia as the Engine of Application State) being a powerful concept — embedding links in responses to guide clients through available actions.</p> <p><span style="margin-left: 10px;"></span> In microservice environments, REST APIs are often interconnected through internal HTTP calls or API gateways like Kong, NGINX, or Spring Cloud Gateway. Each microservice maintains its own resources, forming a loosely coupled ecosystem.</p> <p><span style="margin-left: 10px;"></span> The architectural simplicity and alignment with web standards are what make REST so ubiquitous — but its statelessness and fixed endpoints can create inefficiencies when clients need complex, nested data.</p> <p><span style="margin-left: 10px;"></span> I found this post by Fielding very clarifying and interesting: <a href="https://roy.gbiv.com/untangled/2008/rest-apis-must-be-hypertext-driven">https://roy.gbiv.com/untangled/2008/rest-apis-must-be-hypertext-driven</a>.</p> <h3 id="strengths-and-weaknesses-1">Strengths and Weaknesses</h3> <table> <thead> <tr> <th><strong>Pros</strong></th> <th><strong>Cons</strong></th> </tr> </thead> <tbody> <tr> <td>Simple, human-readable, easy to test and debug</td> <td>Over-fetching: clients often get more data than needed</td> </tr> <tr> <td>Natively supported by web infrastructure</td> <td>Under-fetching: multiple requests to build composite views</td> </tr> <tr> <td>Tooling and standards (OpenAPI, Swagger, Postman) are mature</td> <td>Versioning can be messy (/v1/, /v2/, etc.)</td> </tr> <tr> <td>Easy integration with browsers, mobile apps</td> <td>Limited flexibility for complex or nested data</td> </tr> <tr> <td>Ideal for microservices and stateless systems</td> <td>Can become chatty (many endpoints, round-trips)</td> </tr> </tbody> </table> <p><span style="margin-left: 10px;"></span> At work, REST is the backbone of our service-to-service communication. It’s pragmatic, reliable, and well-understood by everyone — from backend to frontend engineers.</p> <p><span style="margin-left: 10px;"></span> However, over time I’ve fought the classic REST challenges, both at work and in my personal projects:</p> <ul> <li>Endpoints proliferating (/beer, /beer/details, /beer/details/extended).</li> <li>UIs needing multiple requests for a single view.</li> <li>The friction of maintaining backward compatibility when clients depend on specific payload shapes.</li> </ul> <p><span style="margin-left: 10px;"></span> These trade-offs eventually led me to explore GraphQL for my personal projects.</p> <h2 id="graphql">GraphQL</h2> <p><span style="margin-left: 10px;"></span> GraphQL represents a shift from resource-oriented design (REST) to data-oriented design.</p> <p><span style="margin-left: 10px;"></span> Instead of defining how clients access resources, we define what data exists, and let clients query the exact shape they need.</p> <p><span style="margin-left: 10px;"></span> Single endpoint: /graphql</p> <p><span style="margin-left: 10px;"></span> Example query:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>query {
  brewery(id: 1) {
    name
    beers {
      name
      reviews {
        text
      }
    }
  }
}
</code></pre></div></div> <p><span style="margin-left: 10px;"></span> Server responds exactly with:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>{
  "data": {
    "brewery": {
      "name": "Super Bock Brewery",
      "beers": [
        {
          "name": "Super Bock Original",
          "reviews": [
            { "text": "Smooth!" },
            { "text": "Perfect summer beer." }
          ]
        },
        {
          "name": "Stout",
          "reviews": [
            { "text": "Good." }
          ]
        }
      ]
    }
  }
}
</code></pre></div></div> <h3 id="architecture-2">Architecture</h3> <p><span style="margin-left: 10px;"></span> GraphQL’s architecture flips the REST model by introducing a query engine between the client and the data sources. Instead of exposing multiple endpoints, GraphQL exposes a single endpoint that accepts declarative queries describing the exact shape of the data the client wants.</p> <p><span style="margin-left: 10px;"></span> At a high level, a GraphQL system consists of:</p> <ul> <li>Schema Definition Layer — written in SDL (Schema Definition Language), describing types, queries, mutations, and relationships.</li> <li>Execution Layer (Resolvers / Data Fetchers) — functions that fetch data for specific fields or types.</li> <li>Query Engine — parses, validates, and executes incoming queries, orchestrating calls to resolvers.</li> <li>Data Sources Layer — the data providers</li> </ul> <p><span style="margin-left: 10px;"></span> Here’s how a typical query flow works:</p> <ol> <li>The client sends a JSON payload with a query or mutation to /graphql.</li> <li>The GraphQL server parses the query and validates it against the schema.</li> <li>The query engine executes each field’s resolver, often asynchronously.</li> <li>Data from various sources (SQL, REST APIs, caches) is merged into the requested shape.</li> <li>The server returns exactly the requested fields in JSON.</li> </ol> <p><span style="margin-left: 10px;"></span> Resolvers are small, composable units that describe how to fetch a specific piece of data. This makes GraphQL naturally extensible and federated — different services can own parts of the schema and resolve their respective data independently.</p> <p><span style="margin-left: 10px;"></span> Because of this architecture, GraphQL excels at aggregating data from multiple backend sources into a single queryable interface. It trades some server-side simplicity for client flexibility and performance. While REST structures the world by endpoints, GraphQL structures it by types and relationships.</p> <h3 id="strengths-and-weaknesses-2">Strengths and Weaknesses</h3> <table> <thead> <tr> <th><strong>Pros</strong></th> <th><strong>Cons</strong></th> </tr> </thead> <tbody> <tr> <td>Fetch exactly what you need (no over/under-fetching)</td> <td>Complexity in caching and monitoring</td> </tr> <tr> <td>Single endpoint simplifies API evolution</td> <td>Initial learning curve for schema &amp; resolver patterns</td> </tr> <tr> <td>Self-documenting schema (introspection)</td> <td>Potential N+1 problem in naive resolvers</td> </tr> <tr> <td>Excellent for frontend-driven development</td> <td>Overhead for simple CRUD use cases</td> </tr> <tr> <td>Strong tooling ecosystem (DGS and many others)</td> <td>Requires runtime execution layer, not just HTTP routing</td> </tr> </tbody> </table> <h3 id="the-n1-problem--and-the-dgs-solution">The N+1 Problem — and the DGS Solution</h3> <p><span style="margin-left: 10px;"></span> In my personal project, I initially ported a REST backend to GraphQL to reduce the multiple HTTP calls needed for the same thing in different views. The benefits were immediate — fewer endpoints, cleaner frontend queries, and faster perceived performance.</p> <p><span style="margin-left: 10px;"></span> But then I hit the N+1 query problem — where nested resolvers cause multiple database hits per entity. For example, fetching a list of breweries with their beers triggered one DB query per brewery.</p> <p><span style="margin-left: 10px;"></span> Thankfully, using Netflix DGS, I leveraged its DataLoader integration — a batching and caching mechanism that consolidates those queries into efficient bulk operations. It was a game changer, turning what could have been a performance regression into a well-optimized data flow.</p> <p><span style="margin-left: 10px;"></span> Netflix DGS (Domain Graph Service) is an open-source GraphQL framework built by Netflix and designed to simplify GraphQL server development in Java and Kotlin. It’s opinionated in a good way — integrating tightly with Spring Boot, enforcing schema-first development.</p> <p><span style="margin-left: 10px;"></span> The Netflix DGS framework implements this architecture within the Spring ecosystem. It follows a schema-first approach:</p> <ul> <li>You start by defining .graphqls schema files.</li> <li>Then implement @DgsQuery, @DgsMutation, and @DgsData components that act as resolvers.</li> <li>DGS automatically wires them into a GraphQL runtime, handling execution, validation, and DataLoader batching.</li> </ul> <p><span style="margin-left: 10px;"></span> Internally, DGS uses graphql-java as the core engine and adds Netflix’s tooling for:</p> <ul> <li>Schema Federation (composing multiple domain services into one unified graph).</li> <li>Instrumentation and metrics collection.</li> <li>Schema registry integration and validation pipelines.</li> <li>DataLoader integration to batch and cache nested queries efficiently.</li> </ul> <p><span style="margin-left: 10px;"></span> Unlike some other GraphQL implementations, DGS treats the GraphQL schema (.graphqls files) as the source of truth. From that schema, you implement data fetchers — the functions that resolve the data for each type or field.</p> <p><span style="margin-left: 10px;"></span> For example:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>type Brewery {
  id: ID!
  name: String!
  beers: [Beer]
}

type Beer {
  id: ID!
  name: String!
  reviews: [Review]
}

type Query {
  brewery(id: ID!): Brewery
}
</code></pre></div></div> <p><span style="margin-left: 10px;"></span> What I really appreciate about DGS is how it enforces structure without removing flexibility. Resolvers are just annotated functions, so it fits neatly into Spring Boot’s dependency injection ecosystem. You also get built-in integration with DataLoader, which helps solve the N+1 query problem by batching and caching database calls.</p> <p><span style="margin-left: 10px;"></span> Another big plus is how well DGS plays with GraphQL Federation. In a larger setup, multiple teams can build their own GraphQL services (each describing a “domain”), and Netflix DGS provides tools to stitch these schemas together.</p> <h2 id="final-thoughts">Final Thoughts</h2> <p><span style="margin-left: 10px;"></span> SOAP, REST, and GraphQL are not direct competitors, but evolutionary stages in how we exchange data:</p> <ul> <li>SOAP gave us rigor — at the cost of agility.</li> <li>REST gave us simplicity — at the cost of precision.</li> <li>GraphQL gives us precision — at the cost of complexity.</li> </ul> <p><span style="margin-left: 10px;"></span> In my professional work, REST has been the backbone of countless microservices. It’s consistent, predictable, and incredibly well-understood across teams. The tooling is mature, the caching model is natural, and HTTP’s semantics fit like a glove. But REST also taught me its limits — especially in frontend-heavy applications where data rarely fits neatly into single resources. I’ve seen endpoints multiply, clients chained across several requests, and payloads balloon to cover every edge case.</p> <p><span style="margin-left: 10px;"></span> That pain is exactly what led me, in a personal project with a friend, to experiment with GraphQL. The immediate payoff was huge: fewer round trips, cleaner frontend code, and a noticeable reduction in friction between backend and UI.</p> <p><span style="margin-left: 10px;"></span> But the joy was brief. As our schema grew and queries became more nested, we ran headlong into the N+1 query problem — a classic GraphQL problem where resolvers trigger multiple database hits per record. Performance went down the drain. The problem wasn’t GraphQL itself; it was our implementation.</p> <p><span style="margin-left: 10px;"></span> That’s when we discovered Netflix DGS. DGS reframed GraphQL for us — not as an experimental tool, but as an enterprise-ready framework. Its schema-first approach gave structure where GraphQL alone offered freedom, and its DataLoader integration handled batching so elegantly that our N+1 nightmare disappeared almost overnight. Suddenly, GraphQL development felt disciplined again — type-safe, introspectable, and scalable.</p> <p><span style="margin-left: 10px;"></span> If SOAP is the tank, REST the reliable pickup truck, GraphQL is the modern EV — sleek and efficient, but requiring a bit more engineering under the hood to reach its potential.</p> <p><span style="margin-left: 10px;"></span> The lesson? Each has its place — the key is knowing when to drive which one.</p>]]></content><author><name></name></author><summary type="html"><![CDATA[SOAP, REST, and GraphQL - trying to compare the uncomparable.]]></summary></entry><entry><title type="html">Event-Driven Microservices - Kafka and RabbitMQ</title><link href="https://p-almeida12.github.io/blog/2024/event-driven-microservices/" rel="alternate" type="text/html" title="Event-Driven Microservices - Kafka and RabbitMQ"/><published>2024-12-01T14:59:00+00:00</published><updated>2024-12-01T14:59:00+00:00</updated><id>https://p-almeida12.github.io/blog/2024/event-driven-microservices</id><content type="html" xml:base="https://p-almeida12.github.io/blog/2024/event-driven-microservices/"><![CDATA[<p><span style="margin-left: 10px;"></span> How can we achieve seamless communication in microservices? Event-driven architecture is the key to making that happen, by connecting services through events, systems can handle complex processes with ease, scale efficiently, and respond to changes in real time. Apache Kafka and RabbitMQ are tools commonly used for this purpose, both offer powerful features but serving different purposes. Choosing the right one is important, but for that we have to understand how both tools work and where they shine, that’s why I’m writing this post, to get a better understanding of these giants in the IT world.</p> <h1 id="what-is-event-driven-architecture">What is Event-Driven Architecture?</h1> <p><span style="margin-left: 10px;"></span> Event-driven architecture (EDA) is a software design pattern where systems revolve around the production, detection, and reaction to events. An event represents any significant change in the state of a system, such as a user placing an order or a payment being processed. These events are transmitted to other parts of the system asynchronously, enabling various components to respond in near real-time without direct dependencies.</p> <p><span style="margin-left: 10px;"></span> EDA typically includes three core components:</p> <ul> <li>Event Producers: Systems or applications that generate events (e.g., a user action or a system update).</li> <li>Event Routers: Tools like message brokers (e.g., Kafka, RabbitMQ) that transport these events to the right consumers.</li> <li>Event Consumers: Services or systems that react to these events by performing tasks, such as sending notifications or updating records.</li> </ul> <p><span style="margin-left: 10px;"></span> This decoupled structure ensures that event producers don’t need to know who consumes their events, fostering a highly modular system architecture (Martin Fowler, Event-Driven Architecture).</p> <h2 id="the-role-of-event-driven-architecture-in-microservices">The Role of Event-Driven Architecture in Microservices</h2> <p><span style="margin-left: 10px;"></span> EDA is particularly valuable in microservices because it allows individual services to communicate without being tightly coupled. Instead of directly invoking each other’s APIs, services emit events representing actions. Other services subscribe to these events and respond as needed. This indirect communication method enhances system flexibility and simplifies service management.</p> <p><span style="margin-left: 10px;"></span> For instance, consider an e-commerce system:</p> <ul> <li>The order service emits an “Order Placed” event when a user completes a purchase.</li> <li>The inventory service listens for this event to adjust stock levels.</li> <li>Simultaneously, the notification service sends a confirmation email, while the shipping service begins preparing for delivery.</li> </ul> <p><span style="margin-left: 10px;"></span> This design allows these services to function independently while still coordinating efficiently, as proposed by Sam Newman in Building Microservices. This is what decoupling is all about, services can evolve independently, scale, etc…</p> <h2 id="benefits-of-event-driven-architecture">Benefits of Event-Driven Architecture</h2> <ul> <li><strong>Decoupling</strong>: services remain independent, reducing dependencies. For example, a producer like an order service doesn’t need to know which services consume its events, as discussed in Martin Fowler’s article on EDA.</li> <li><strong>Scalability</strong>: events are processed asynchronously, services can scale independently based on demand. Apache Kafka’s distributed architecture is explicitly designed to handle millions of events per second.</li> <li><strong>Responsiveness</strong>: systems can react to events in real time. For instance, fraud detection systems can immediately analyze suspicious activity by consuming events from a payment service.</li> <li><strong>Resilience</strong>: Failures in one part of the system don’t cascade. Events can be queued and retried later, ensuring no critical data is lost. RabbitMQ supports this with its dead-letter queues and acknowledgment mechanisms (ack).</li> <li><strong>Extensibility</strong>: New services can be added simply by subscribing to existing events, making it easier to evolve the system over time.</li> <li><strong>Real-Time Data Processing</strong>: Tools like Kafka allow for continuous data streaming, essential for real-time analytics and machine learning workflows. For example, LinkedIn uses Kafka for activity tracking and user recommendations.</li> </ul> <h1 id="overview-of-kafka-and-rabbitmq">Overview of Kafka and RabbitMQ</h1> <h2 id="apache-kafka">Apache Kafka</h2> <p><span style="margin-left: 10px;"></span> Kafka follows a distributed, publish-subscribe architecture with a few key components:</p> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <img src="/assets/img/kafka_architecture.png" class="img-fluid rounded z-depth-1" width="auto" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> <div class="caption"> Figure 1 - Apache Kafka Architecture </div> <h3 id="producer">Producer</h3> <p><span style="margin-left: 10px;"></span> Any application or service that sends events (messages) to Kafka topics. A kafka producer holds a buffer of unsent records per partition and sends the records to the cluster using an internal batch.size property. In most of the cases the default size could be good enough, but you may want to play around with this value to get a better throughput by increasing the batching. But keep in mind that if the batch size is too big the memory will be wasted since the part of the memory that is allocated for batching will not be used. This happens because the data will be sent before the batch size limit hits. Using a larger batch size makes compression more efficient.</p> <p><span style="margin-left: 10px;"></span> Under heavy loads, batching will probably be in place, however, in light loads data may not be batched until the batch size limit hits, in that case increasing the linger.ms can increase the throughput by increasing batching with fewer requests and with an increased latency on producer send. By default linger.ms is 0, which means no delay. As mentioned previously in Kafka there are multiple broker nodes that holds the same data to enable resilience and availability. By default the producer will wait all replica nodes to return the result because the default value for acknowledgement is all, by setting ack to 1 only the broker that sent the request will send confirmation instead of waiting all in-sync replicas.</p> <p><span style="margin-left: 10px;"></span> Producers can also compress the data before sending it to the broker, this can be done by setting the compression.type property. The compression is done by batch and improves with larger batch sizes. By using compressed data, you can send more data at once through the network and increase the throughput. End to end compression is also possible, this way compression only happens once and is reused by the producer and consumer (better for performance).</p> <h3 id="broker">Broker</h3> <p><span style="margin-left: 10px;"></span> A Kafka Broker is a server (or node) in a Kafka cluster responsible for receiving, storing, and forwarding messages (or events) to consumers. It is a fundamental component of Kafka’s distributed architecture. A Kafka cluster consists of one or more brokers, where each broker is a part of a larger system that stores the actual data (i.e., the events/messages). Each broker is responsible for managing a set of partitions for one or more topics.</p> <p><span style="margin-left: 10px;"></span> Kafka brokers store messages in a distributed manner, partitioning the data across multiple disks for better performance, scalability, and fault tolerance. Kafka brokers receive messages from producers, append them to the appropriate partitions, and handle message delivery to consumers. Brokers handle the replication of data to ensure fault tolerance. Each partition’s data is replicated across multiple brokers to ensure that if one broker fails, data remains available from other brokers in the cluster.</p> <p><span style="margin-left: 10px;"></span> Each partition has one leader broker responsible for handling all read and write operations for that partition. The leader ensures that data is consistently written to the partition, maintains the order of messages, and manages replication. If a producer wants to send a message to a topic, it sends the message to the leader of the corresponding partition.</p> <p><span style="margin-left: 10px;"></span> Followers are brokers that maintain copies of the partition’s data. These replicas are updated with messages from the leader to ensure fault tolerance. Followers do not handle writes; instead, they replicate data from the leader to maintain synchronization. If a follower is too far behind, it may be temporarily excluded from the replication process.</p> <p><span style="margin-left: 10px;"></span> If a leader broker fails, Kafka automatically promotes one of the followers to become the new leader. This process is known as leader election, which is crucial for maintaining high availability. Kafka uses ZooKeeper (in the traditional setup) or KRaft (in newer versions) for leader election and managing partition metadata.</p> <h3 id="consumer">Consumer:</h3> <p><span style="margin-left: 10px;"></span> The application or service that reads data from Kafka topics. Kafka relies on logs which are known as partitions, the producers write to the end of a specific partition and the consumers read the logs starting from the beginning while holding an offset to keep track. When it is done with the data, the consumer sends a commit request to the broker so that there will be an offset per consumer per partition, to remember the latest record that was read. This way consumer will continue reading new data instead of reading the same data again.</p> <p><span style="margin-left: 10px;"></span> Each consumer belongs to a consumer group and Kafka distributes partitions to consumers based on these consumer groups. Each partition is strictly assigned to a single consumer in a consumer group, therefore it is not possible to assign the same partition to more than one consumer. This is to prevent conflicts with the offset values and make consuming easier. Different consumer groups with different ids can read the same data form the same partition, so kafka allows you to read the same data more than once, but for that you need to create a new consumer group and read the data again. For maximum concurrency, it is wise to set the consumer number as equal to the partition number. If you create more consumers than partition numbers, after assigning each partition to a consumer, some consumers will be idle and will not read any data.</p> <p><span style="margin-left: 10px;"></span> There is a poll time out in Kafka consumer and setting it to a too high value will make the waiting take longer and cause delays in the application. If the poll time out is too low, the consumer will poll more frequently and this will cause more network traffic and can lead to CPU stall. If you have a single consumer with multiple partitions, there will be no concurrent work on multiple partitions by default (multithreading), but we can still use multiple threads while consuming partitions. This should be implemented on the client side.</p> <p><span style="margin-left: 10px;"></span> When it comes to delivery semantics, Kafka provides three options: at most once, at least once, and exactly once. This behavior relies on the consumer strategy and the ack property on the client side. For example, if you commit after processing the data, the commit operation might fail, but you already processed the data, so in the next poll you will read the same data and it will imply at least once behavior. On the other hand, if you commit before processing the data, you might lose the data if the consumer crashes, so it will imply at most once behavior. Exactly once requires to coordinate between producer and consumer, using transactions starting from the producer part. There is a transaction Kafka API which will start the transaction from producer to use the same transaction in the consumer part. Normally auto commit is set to true on the consumer client, so it does a commit after a configured timer and then reset it. But keep in mind that in some scenarios you might still encounter an error while auto commit is set to true, because a failure can always happen. With this being said, you might need to implement some rollback mechanism or use strict exactly once semantic depending on your needs.</p> <h3 id="topic">Topic</h3> <p><span style="margin-left: 10px;"></span> A Topic is a logical channel or category to which Kafka producers send their messages and from which consumers read their messages. Kafka topics are a fundamental abstraction that organizes messages for efficient routing, storage, and consumption. Topics are essentially message queues to which producers publish data and from which consumers pull data.</p> <p><span style="margin-left: 10px;"></span> Topics help categorize messages by their purpose, such as a “user_signup” topic for user registration events or a “payment_processed” topic for payment-related messages. Consumers subscribe to one or more topics and receive messages (events) that are published to those topics. Kafka follows a publish-subscribe model, where producers publish messages to a topic, and consumers subscribe to topics to receive those messages. A topic can have multiple producers and consumers, allowing for a highly scalable and distributed pub-sub system. Kafka topics are stored on disk, and messages are retained in a topic for a configurable retention period (even after they are consumed, unless manually deleted).</p> <h3 id="zookeeper">ZooKeeper</h3> <p><span style="margin-left: 10px;"></span> Apache ZooKeeper is a distributed coordination service that is used by Kafka to manage and maintain the Kafka cluster. It acts as a centralized service for maintaining configuration information, providing synchronization, and ensuring the integrity of the system.</p> <p><span style="margin-left: 10px;"></span> In earlier versions of Kafka, ZooKeeper was an essential component for managing brokers and maintaining Kafka’s distributed system metadata, such as which broker holds which partition and its leadership. Now newer versions of Kafka are transitioning away from ZooKeeper (Kafka KRaft mode), it is still used in many existing Kafka deployments. ZooKeeper ensures that even if one broker goes down, another broker can take over the responsibility of a partition, thanks to its role in maintaining partition leader information.</p> <h3 id="kraft">KRaft</h3> <p><span style="margin-left: 10px;"></span> KRaft (Kafka Raft) is a significant shift in how Apache Kafka manages its cluster metadata and leader election. Traditionally, Kafka used ZooKeeper to manage cluster metadata, including partition leaders, topic configurations, and broker statuses. However, with the introduction of KRaft mode in Kafka, ZooKeeper is no longer required, and Kafka now uses its own internal protocol to manage these responsibilities.</p> <p><span style="margin-left: 10px;"></span> In KRaft mode, Kafka maintains a metadata quorum that is responsible for managing cluster metadata, including topic configurations, partition leadership, and broker statuses. This quorum uses Raft to achieve consensus on metadata changes across all brokers.</p> <p><span style="margin-left: 10px;"></span> The Raft protocol ensures that Kafka’s metadata is consistently replicated across multiple nodes in the cluster. Raft allows Kafka brokers to elect a leader for the metadata quorum, and that leader is responsible for updating metadata and synchronizing changes with follower brokers.</p> <p><span style="margin-left: 10px;"></span> One of the biggest changes with KRaft mode is the removal of ZooKeeper. This drastically simplifies Kafka’s architecture, as Kafka now manages its own leader election and metadata replication without relying on an external service like ZooKeeper. This change reduces operational overhead, makes cluster management more straightforward, and eliminates the complexity of maintaining and configuring ZooKeeper.</p> <p><span style="margin-left: 10px;"></span> Final considerations:</p> <ul> <li><strong>Transition from ZooKeeper</strong>: Migrating from a traditional ZooKeeper-based Kafka setup to KRaft can be a complex process, especially for large and heavily used Kafka clusters. Users must ensure that their applications and systems are compatible with KRaft mode and handle any potential disruptions during the migration.</li> <li><strong>New Features and Stability</strong>: KRaft mode is still being refined, and certain features that were available with ZooKeeper may take time to fully mature in KRaft. For instance, features like multi-cluster replication and some advanced configurations may not be as mature as in Zookeeper.</li> <li><strong>Cluster Scaling</strong>: While KRaft simplifies the Kafka architecture, scaling the cluster (both in terms of adding brokers and handling large amounts of metadata) requires careful planning and monitoring. The Raft protocol can introduce its own set of challenges when scaling up a Kafka cluster, especially as the metadata grows more complex.</li> </ul> <h2 id="rabbitmq">RabbitMQ</h2> <p><span style="margin-left: 10px;"></span> RabbitMQ’s architecture is based on a client-server model and revolves around producers, exchanges, queues, and consumers. These components work together to provide a robust, reliable messaging system.</p> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <img src="/assets/img/rabbitmq_architecture.png" class="img-fluid rounded z-depth-1" width="auto" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> <div class="caption"> Figure 2 - RabbitMQ Architecture </div> <h3 id="producer-1">Producer</h3> <p><span style="margin-left: 10px;"></span> A Producer is any application or service that sends messages to RabbitMQ. Producers do not directly interact with queues; instead, they send messages to an Exchange, which is responsible for routing the message to the appropriate queue or queues. Producers include a routing key with each message. This key acts as a tag used by exchanges to determine where the message should go. Producers specify the exchange name when sending a message.</p> <p><span style="margin-left: 10px;"></span> If no exchange is specified, RabbitMQ uses the default exchange. Producers send messages without waiting for confirmation that a consumer has processed them, ensuring decoupled communication (although it is possible to wait for the response, but breaks the decoupling).</p> <h3 id="exchange">Exchange</h3> <p><span style="margin-left: 10px;"></span> An Exchange is a central routing mechanism in RabbitMQ. It receives messages from producers and determines how to route them to one or more queues based on routing keys and bindings. RabbitMQ supports several types of exchanges, each tailored for specific routing scenarios:</p> <ul> <li><strong>Direct Exchange</strong>: Routes messages to queues based on an exact match between the routing key and the queue’s binding key.</li> <li><strong>Fanout Exchange</strong>: Broadcasts messages to all queues bound to the exchange, ignoring the routing key.</li> <li><strong>Topic Exchange</strong>: Routes messages based on pattern matching of the routing key. Patterns use wildcards: <ul> <li>* matches exactly one word (e.g., log.* matches log.info but not log.error.system).</li> <li># matches zero or more words (e.g., log.# matches log.info and log.error.system).</li> </ul> </li> <li><strong>Headers Exchange</strong>: Routes messages based on custom headers (not the routing key).</li> </ul> <h3 id="queues">Queues</h3> <p><span style="margin-left: 10px;"></span> A Queue is a buffer where messages are stored until they are retrieved by consumers. Queues decouple producers and consumers, ensuring that message delivery is reliable even if the consumer is temporarily unavailable. Some of the features of RabbitMQ queues:</p> <ul> <li><strong>Persistence</strong>: Queues can be configured as durable to be able survive broker restarts. This happens because messages are saved to disk.</li> <li><strong>Message Priority</strong>: Queues can prioritize certain messages to ensure critical tasks are handled first.</li> <li><strong>Message TTL</strong>: If a message is unconsumed, a Time-to-Live (TTL) can be set to discard it after a given period.</li> </ul> <p><span style="margin-left: 10px;"></span> Messages that cannot be routed or are rejected by a consumer can be redirected to a Dead Letter Queue (DLQ) for later inspection or retrying. This is useful for debugging and analyzing failed messages.</p> <h3 id="bindings">Bindings</h3> <p><span style="margin-left: 10px;"></span> A Binding is the relationship between an exchange and a queue. Bindings define the rules that exchanges use to route messages to specific queues. Binding keys act as filters for routing messages. For example, a queue bound to a direct exchange with the binding key “error” will receive only messages with a routing key of error. Multiple Bindings is when a single queue can be bound to an exchange with multiple binding keys. This allows the queue to receive messages matching multiple patterns.</p> <h3 id="consumers">Consumers</h3> <p><span style="margin-left: 10px;"></span> A Consumer is any application or service that retrieves and processes messages from a queue. Consumers are responsible for ensuring message reliability by acknowledging successful processing.</p> <ul> <li><strong>Manual Acknowledgment</strong>: Consumers explicitly acknowledge each message after successful processing. This ensures that messages are not lost if the consumer crashes during processing.</li> <li><strong>Automatic Acknowledgment</strong>: RabbitMQ automatically considers the message acknowledged once it is delivered to the consumer. This is faster but less reliable.</li> </ul> <p><span style="margin-left: 10px;"></span> Consumers can configure a pre-fetch count to limit the number of messages delivered at a time. This prevents the consumer from being overwhelmed and ensures fair distribution in a multi-consumer scenario.</p> <h3 id="broker-1">Broker</h3> <p><span style="margin-left: 10px;"></span> The RabbitMQ broker is the server managing the exchanges, queues, and bindings. It ensures that messages are reliably routed and stored as needed. The broker also handles load balancing and fault tolerance.</p> <h3 id="other-features">Other Features</h3> <p><span style="margin-left: 10px;"></span> RabbitMQ nodes can form clusters, allowing them to share metadata and distribute workloads across multiple machines. While clusters improve load balancing, queues are hosted by a single node unless explicitly mirrored.</p> <p><span style="margin-left: 10px;"></span> To ensure high availability, RabbitMQ supports mirrored queues, replicating data across multiple nodes. If the primary node fails, a replica takes over to avoid service interruptions.</p> <p><span style="margin-left: 10px;"></span> RabbitMQ can shard queues across nodes, distributing the load and preventing bottlenecks in high-throughput systems.</p> <p><span style="margin-left: 10px;"></span> Routing mechanisms like dynamic routing, priority queues and dead-letter exchanges provide flexibility in message handling and delivery.</p> <h2 id="kafka-vs-rabbitmq">Kafka vs. RabbitMQ</h2> <p><span style="margin-left: 10px;"></span> When deciding between Apache Kafka and RabbitMQ for your microservices architecture, it’s crucial to understand their core differences. Below is a comparison based on various aspects like message models, use cases, scalability, durability, and routing.</p> <p><span style="margin-left: 10px;"></span> Kafka is designed as a distributed log system that streams messages in real time. It follows a publish-subscribe model, where producers write messages to topics, and consumers read them independently. Kafka retains messages for a specified duration regardless of consumption, enabling replayability for debugging and analytics. Kafka is ideal for high-throughput, real-time use cases such as:</p> <ul> <li>Event sourcing in microservices.</li> <li>Real-time analytics pipelines.</li> <li>Log aggregation across distributed systems.</li> <li>Use in industries like finance (stock), retail (inventory updates), and for sensor data streaming.</li> </ul> <p><span style="margin-left: 10px;"></span> RabbitMQ operates as a traditional message broker with a focus on message queuing. Producers send messages to an exchange, which routes them to queues based on bindings. Consumers then process messages, and once acknowledged, they are removed from the queue. RabbitMQ prioritizes reliable delivery over retention. Here are some use cases where RabbitMQ is commonly used:</p> <ul> <li>Asynchronous task execution (e.g., sending emails, processing images).</li> <li>Workload balancing across multiple consumers.</li> <li>Systems where message ordering and routing flexibility are critical.</li> </ul> <p><span style="margin-left: 10px;"></span> In terms of scalability and performance, Kafka’s partitioned, distributed architecture makes it highly scalable. Topics are divided into partitions spread across brokers, allowing for parallel processing. It achieves millions of messages per second but requires significant setup and infrastructure. On the other hand, RabbitMQ scales well within a single cluster but has limitations at extreme throughput levels. It uses mirrored queues and sharding for scalability but typically handles hundreds of thousands of messages per second, focusing on reliability over raw speed.</p> <p><span style="margin-left: 10px;"></span> When it comes to durability and message retention, Kafka retains messages for a configurable time (e.g., days or weeks), irrespective of consumption. This log-based retention ensures that even if a consumer fails, it can replay messages from the past, making Kafka suitable for event sourcing and data reprocessing. RabbitMQ ensures message durability by persisting messages to disk. However, once a message is acknowledged, it’s removed from the queue. This makes RabbitMQ more suitable for workflows where message replay is not required.</p> <p><span style="margin-left: 10px;"></span> For routing and flexibility, Kafka relies on its topic-based publish-subscribe model for routing. Messages are routed to topics, and consumers subscribe to specific partitions. This simplicity makes Kafka less flexible for complex routing scenarios. RabbitMQ offers advanced routing options through exchanges (direct, fanout, topic, headers). These options allow developers to implement complex routing rules, making RabbitMQ highly versatile.</p> <h3 id="error-handling-and-dead-letter-queues-dlqs">Error Handling and Dead Letter Queues (DLQs)</h3> <h4 id="kafka">Kafka</h4> <ul> <li><strong>Message Failures</strong>: Kafka’s approach is based on retries and offset management. If a consumer fails to process a message, it can adjust its offset to reprocess the message. This makes error handling flexible but relies on developers to implement the correct retry logic.</li> <li><strong>Dead Letter Queues</strong>: Kafka doesn’t natively support DLQs but provides mechanisms to implement them. For example, failed messages can be redirected to a separate “dead letter topic” using stream processing frameworks like Kafka Streams or with a custom consumer.</li> </ul> <h4 id="rabbitmq-1">RabbitMQ</h4> <ul> <li><strong>Message Failures</strong>: RabbitMQ includes built-in support for message acknowledgments. If a consumer fails to acknowledge a message, RabbitMQ can requeue it for a configurable amount of times or send it to a Dead Letter Exchange (DLX).</li> <li><strong>Dead Letter Queues</strong>: RabbitMQ’s DLX is an exchange where rejected messages are routed. This feature is straightforward to configure by setting specific queue parameters (x-dead-letter-exchange) to handle undeliverable messages.</li> </ul> <h3 id="message-ordering-and-idempotency">Message Ordering and Idempotency</h3> <h4 id="kafka-1">Kafka</h4> <ul> <li><strong>Ordered Processing</strong>: Kafka ensures ordering within a partition. Producers decide the partition for each message, and messages within the same partition are processed in the order they arrive. Consumer groups distribute partitions among consumers, maintaining order at the partition level.</li> <li><strong>Idempotency</strong>: Kafka supports idempotent producers to ensure that duplicate messages are not produced, even in retry scenarios. This is particularly useful for applications that require strict once-only delivery semantics.</li> </ul> <h4 id="rabbitmq-2">RabbitMQ</h4> <ul> <li><strong>Ordered Processing</strong>: RabbitMQ does not guarantee message ordering, especially when using multiple consumers for a queue. Message ordering can break due to acknowledgments being processed out of sequence or consumer distribution.</li> <li><strong>Idempotency</strong>: RabbitMQ leaves idempotency handling to the application layer. Developers must implement mechanisms to detect and handle duplicate messages, often by using unique message identifiers.</li> </ul> <h3 id="replication-and-disaster-recovery">Replication and Disaster Recovery</h3> <h4 id="kafka-2">Kafka</h4> <ul> <li><strong>Partition Replication</strong>: Kafka ensures high availability through partition replication. Each partition has a leader and multiple followers. If the leader fails, one of the followers takes over.</li> <li><strong>Failover Scenarios</strong>: Kafka’s ZooKeeper (or KRaft in newer versions) automatically manages leader election during failover. This ensures minimal disruption to message processing.</li> </ul> <h4 id="rabbitmq-3">RabbitMQ</h4> <ul> <li><strong>Mirrored Queues</strong>: RabbitMQ offers mirrored queues to replicate messages across multiple nodes. If the primary node fails, a replica can take over.</li> <li><strong>Failover Scenarios</strong>: Failover in RabbitMQ depends on clustering. While it provides high availability, managing clusters can be complex, especially for large-scale deployments.</li> </ul> <h2 id="hybrid-architectures">Hybrid Architectures</h2> <p><span style="margin-left: 10px;"></span> Hybrid architectures leverage the strengths of both Kafka and RabbitMQ to handle diverse requirements in a system. Instead of viewing Kafka and RabbitMQ as competitors, this approach integrates them to balance scalability, reliability, and functionality across various workloads. Let’s dive deeper into how this is achieved and explore specific examples, challenges, and best practices. I’m not a guru in this field, but in my mind makes sense we would use the best of both worlds. I didn’t find many information about this topic, but I’ll try to make the best of it.</p> <p><span style="margin-left: 10px;"></span> Hybrid architectures are beneficial because Kafka and RabbitMQ complement each other in terms of their core functionalities. Kafka shines in high-throughput scenarios that require real-time event streaming, distributed logging, and analytics. On the other hand, RabbitMQ is ideal for managing reliable, task-based messaging and handling point-to-point communication with advanced routing features. By using both tools, systems can take advantage of Kafka’s strengths in event streaming and RabbitMQ’s strengths in task management and message routing, leading to an architecture that optimizes performance, reliability, and scalability.</p> <p><span style="margin-left: 10px;"></span> When you need to handle diverse workloads, hybrid architectures provide the flexibility to use the right tool for the job. For instance, systems that require both real-time data processing and reliable task delivery can leverage Kafka for the data streaming aspects and RabbitMQ for the task management, ensuring that each function operates at its best.</p> <p><span style="margin-left: 10px;"></span> In a hybrid setup, Kafka and RabbitMQ interact in different ways depending on the specific needs of the system. One common pattern is to use Kafka for real-time event streams and RabbitMQ for task-based messaging. Kafka can process high-throughput events such as tracking user interactions or processing sensor data, and then RabbitMQ can take over to manage tasks like sending notifications, updating databases, or performing background processing based on those events.</p> <p><span style="margin-left: 10px;"></span> In an e-commerce platform, Kafka could be responsible for streaming real-time customer behavior data, such as page views, cart interactions, and purchases. This data can be used to power recommendation engines or provide marketing insights. Meanwhile, RabbitMQ could handle the actual order processing tasks like confirming payment, updating inventory, and sending emails or notifications to customers.</p> <p><span style="margin-left: 10px;"></span> Although it looks powerful, I think it would have some challenges. First, managing two systems can increase complexity. Kafka, with its distributed nature and partitioned topic structure, is fundamentally different from RabbitMQ’s queuing and task-based model. As a result, maintaining such an architecture requires expertise in both systems.</p> <p><span style="margin-left: 10px;"></span> Data synchronization can also be a challenge in hybrid architectures. Ensuring that data flows consistently and smoothly from one system to the other requires careful consideration, especially if messages are produced faster than they can be consumed.</p> <p><span style="margin-left: 10px;"></span> Monitoring and troubleshooting become more complex as well. Since Kafka and RabbitMQ are different tools with their own set of metrics and logging structures, effective monitoring requires using multiple tools to track both systems. It’s critical to implement a unified monitoring system that can track events across both Kafka and RabbitMQ to avoid performance bottlenecks and ensure smooth operations.</p> <h2 id="in-the-real-world">In the Real World</h2> <h3 id="apache-kafka-1">Apache Kafka</h3> <p>Netflix uses Apache Kafka for its real-time event streaming needs, to stream, process, and analyze this data in real-time. This enables capabilities like monitoring user activity, delivering personalized recommendations, and improving the system logging and diagnostics.</p> <p>LinkedIn, the creator of Kafka, uses it as the backbone for its activity streams. Kafka ensures high-throughput message delivery across LinkedIn’s services, such as tracking user interactions, delivering notifications, and supporting the real-time flow of information across its platform.</p> <p>Uber integrates Kafka to handle its real-time trip-tracking and pricing mechanisms. Uber’s platform generates streams of events, such as driver location updates and customer requests. Kafka processes this data in real-time to power features like dynamic pricing, which adjusts fares based on demand and supply, and to ensure that ride status updates are instantaneously reflected in the app.</p> <h3 id="rabbitmq-4">RabbitMQ</h3> <p>Airbnb relies on RabbitMQ for task queuing and reliable communication between its microservices. RabbitMQ ensures that critical tasks, such as confirming bookings or processing payments, are delivered and processed reliably even under high system load. This guarantees that no message is lost, making it an essential component.</p> <p>Mozilla employs RabbitMQ as part of its telemetry system. RabbitMQ handles millions of daily messages from Firefox users, which include performance data and error logs.</p> <p>Instagram uses RabbitMQ to manage asynchronous task processing, such as sending notifications, processing media uploads, and moderating content.</p> <table> <thead> <tr> <th><strong>Use Case</strong></th> <th><strong>Description</strong></th> <th><strong>Tool</strong></th> <th><strong>Real-World Example</strong></th> </tr> </thead> <tbody> <tr> <td><strong>Real-time Analytics</strong></td> <td>Processing large volumes of streaming data for dashboards and insights.</td> <td>Kafka</td> <td><strong>Netflix</strong> for viewing trends and recommendations.</td> </tr> <tr> <td><strong>Task Queue Management</strong></td> <td>Asynchronous task processing where reliability and acknowledgment are critical.</td> <td>RabbitMQ</td> <td><strong>Instagram</strong> for notifications and media processing.</td> </tr> <tr> <td><strong>Event Sourcing</strong></td> <td>Capturing state changes as events to rebuild system state for auditability and reprocessing.</td> <td>Kafka</td> <td><strong>Uber</strong> for trip lifecycle management.</td> </tr> <tr> <td><strong>Microservices Communication</strong></td> <td>Enabling decoupled communication between microservices for scalable applications.</td> <td>RabbitMQ</td> <td><strong>Airbnb</strong> for managing inter-service communication in booking systems.</td> </tr> <tr> <td><strong>Log Aggregation</strong></td> <td>Collecting and centralizing logs from distributed systems for analysis.</td> <td>Kafka</td> <td><strong>LinkedIn</strong> for operational monitoring and debugging.</td> </tr> <tr> <td><strong>Data Pipelines</strong></td> <td>Streaming data from multiple sources to storage and processing systems.</td> <td>Kafka</td> <td><strong>Spotify</strong> for streaming user interactions to analytics platforms.</td> </tr> <tr> <td><strong>Data Streaming</strong></td> <td>Handling high-frequency data from devices in real-time.</td> <td>Kafka</td> <td><strong>Tesla</strong> for collecting telemetry data from vehicles.</td> </tr> <tr> <td><strong>Dynamic Pricing</strong></td> <td>Adjusting prices in real-time based on demand and supply.</td> <td>Kafka</td> <td><strong>Uber</strong> for surge pricing models.</td> </tr> <tr> <td><strong>Notification Systems</strong></td> <td>Sending real-time notifications to users across platforms.</td> <td>RabbitMQ</td> <td><strong>Instagram</strong> for user notifications on new messages and updates.</td> </tr> <tr> <td><strong>Financial Transactions</strong></td> <td>Ensuring reliable message delivery for payment systems.</td> <td>RabbitMQ</td> <td><strong>Stripe</strong> for managing payment workflows securely.</td> </tr> <tr> <td><strong>Content Moderation</strong></td> <td>Processing and flagging inappropriate content asynchronously.</td> <td>RabbitMQ</td> <td><strong>Reddit</strong> for detecting and moderating flagged user posts.</td> </tr> <tr> <td><strong>Supply Chain Tracking</strong></td> <td>Monitoring inventory levels and logistics in real-time.</td> <td>Kafka</td> <td><strong>Walmart</strong> for real-time inventory updates across stores.</td> </tr> <tr> <td><strong>Gaming Leaderboards</strong></td> <td>Streaming player activity data to generate real-time leaderboards.</td> <td>Kafka</td> <td><strong>League of Legends</strong> for updating player rankings dynamically.</td> </tr> </tbody> </table> <h2 id="conclusion">Conclusion</h2> <p><span style="margin-left: 10px;"></span> Choosing between Apache Kafka and RabbitMQ isn’t just a technical decision—it’s about understanding the system’s needs and aligning those with the strengths of each tool. In my opinion, both are strong and mature tools, but they shine in different ways, and that’s what makes this comparison so interesting. It actually very interesting to understand how each architecture and structure have an impact on the usages of each tool.</p> <p><span style="margin-left: 10px;"></span> Kafka is like the powerhouse you call when you need to stream massive amounts of data in real-time, analyze it on the fly, and handle large amounts of data (million messages per second is no joke). It’s built for distributed systems, thrives in high-throughput environments, and is perfect for use cases like real-time analytics, data streams, and event sourcing. Its log-based retention and replayability make it an event-driven architecture perfect match. But let’s be real—it’s not the easiest tool to learn or manage, especially when you’re diving into topics, partitions, replication, and the complexities of ZooKeeper or KRaft. I’ve made a repository on event driven architecture with Kafka, and I can say that it was a challenge to understand the concepts and implement them. I still have a lot to learn about Kafka, but I think I’ll get there!</p> <p><span style="margin-left: 10px;"></span> RabbitMQ, on the other hand, is all about simplicity and reliability. It’s the go-to tool for task queues, asynchronous job processing, and microservices communication when you need messages delivered and acknowledged. Its flexibility with exchanges and protocols gives you all the routing options you could ask for, and it’s far easier to set up and use compared to Kafka. I’ve used RabbitMQ on a project I’m developing with a friend and the setup, the structure and the concept are much easier to understand and implement in comparison to Kafka. However, it’s not designed to scale in the same way Kafka is, and for scenarios involving massive data streams or real-time analytics, it might not be the smartest choice.</p> <p><span style="margin-left: 10px;"></span> At the end of the day, it’s all about the problem you’re solving. I know that this is a cliche, but it’s the truth! In the IT world is actually something that we have to keep in mind at all times, because the tools are not just tools, we need to have a good understanding of how they work to make informed choices. With that being said, if you’re building a scalable system to handle high-throughput, low-latency event streams, Kafka is your pick. If you’re managing complex routing and reliable message delivery between services, RabbitMQ might be a better fit. And if you’re somewhere in between, it’s worth considering whether these two tools can coexist in your stack, although it might add some complexity to your architecture.</p> <p><span style="margin-left: 10px;"></span> The goal isn’t just to pick the “best” tool but the right tool for your unique requirements. So, take a step back, think about your architecture, your team’s expertise, and the future scale of your system. And if you’re still stuck? Maybe it’s time to build a proof of concept and see these tools in action. Because at the end of the day, nothing beats real-world experimentation when it comes to figuring out what works best. This is one of the things that I learned in my journey as a developer, the best way to learn is by doing and discussing with experienced people.</p>]]></content><author><name></name></author><summary type="html"><![CDATA[Event-Driven Microservices with Kafka and RabbitMQ.]]></summary></entry><entry><title type="html">Big O Notation for Developers</title><link href="https://p-almeida12.github.io/blog/2024/big-o-notation/" rel="alternate" type="text/html" title="Big O Notation for Developers"/><published>2024-06-02T14:59:00+00:00</published><updated>2024-06-02T14:59:00+00:00</updated><id>https://p-almeida12.github.io/blog/2024/big-o-notation</id><content type="html" xml:base="https://p-almeida12.github.io/blog/2024/big-o-notation/"><![CDATA[<p><span style="margin-left: 10px;"></span> In the realm of computer science and software development, efficiency is extremely important. As the size of data and the complexity of operations increases, the performance of algorithms becomes a critical concern. Big O Notation is a mathematical concept that helps programmers understand and quantify the efficiency of their algorithms. In school, we always wondered why we needed math, all those formulas and equations with no sight of numbers. Well, Big O Notation is one of the reasons why math is essential in programming, and although we don’t like it, it is a necessary evil that can be of great help in optimizing our code.</p> <p><span style="margin-left: 10px;"></span> In this article, we explore the essential concept of Big O Notation and its significance in the world of programming. Understanding Big O Notation is crucial for evaluating and optimizing the performance of algorithms, ensuring efficient and scalable code. We will delve into what Big O Notation is, how it helps in analyzing algorithm complexity, and why mastering it is vital for every programmer aiming to write high-performance software.</p> <h2 id="what-is-big-o-notation">What is Big O Notation?</h2> <h3 id="definition-and-purpose">Definition and Purpose</h3> <p><span style="margin-left: 10px;"></span> Big O Notation is a mathematical concept used in computer science to describe the efficiency of algorithms in terms of time and space complexity. It provides a way to classify algorithms according to how their run time or space requirements grow as the input size increases. The primary purpose of Big O Notation is to give programmers a high-level understanding of the performance characteristics of an algorithm without getting lost in implementation details [<a href="https://www.simplilearn.com/big-o-notation-in-data-structure-article">https://www.simplilearn.com/big-o-notation-in-data-structure-article</a>].</p> <p><span style="margin-left: 10px;"></span> In essence, Big O Notation helps answer the question: “How does the algorithm’s performance change as the input size grows?” By focusing on the largest contributing factors, Big O Notation abstracts away constants and lower-order terms, allowing for a simplified analysis of an algorithm’s efficiency.</p> <h3 id="how-it-measures-time-and-space-complexity">How it Measures Time and Space Complexity</h3> <p><span style="margin-left: 10px;"></span> Time complexity refers to the amount of time an algorithm takes to complete as a function of the input size. It is measured in terms of the number of fundamental operations an algorithm performs relative to the size of the input. The key here is to understand how the time scales as the input size grows. For example, for an algorithm with a time complexity of O(n), the number of operations increases linearly with the input size. If the input size doubles, the time it takes to complete the algorithm also doubles.</p> <p><span style="margin-left: 10px;"></span> On the other hand, space complexity refers to the amount of memory an algorithm uses as a function of the input size. Space complexity measures the extra space or memory required by the algorithm, not counting the space needed for the input itself. Like time complexity, it focuses on how the memory requirements grow with the input size. For example, for an algorithm with a space complexity of O(n), the amount of memory needed grows linearly with the input size. If the input size doubles, the memory usage also doubles, following the same thought of the time complexity.</p> <h2 id="why-is-big-o-notation-important">Why is Big O Notation Important?</h2> <p><span style="margin-left: 10px;"></span> Understanding and applying Big O Notation is crucial for efficiency, scalability, and overall quality of software applications. Big O Notation is essential for programmers as it helps identify and select the most efficient algorithms based on their time complexity. By understanding how different algorithms scale with input size, developers can choose those that handle larger datasets or insert/delete more effectively. For instance, an algorithm with O(n log n) complexity will outperform an O(n^2) algorithm as data size increases. This knowledge is crucial for optimizing application performance, resulting in faster execution times and a better user experience.</p> <p><span style="margin-left: 10px;"></span> To make this analysis, we need to understand the existent Big O notations and how structures and algorithms are classified. Of course, we can’t know all of them, but keeping a cheat sheet handy can be a good idea. Further down this post, I will provide a table with the most common Big O notations.</p> <p><span style="margin-left: 10px;"></span> Scalability is another critical aspect addressed by Big O Notation. As applications grow and process more data, it’s crucial to ensure algorithms can scale efficiently. Algorithms with lower complexity usually ensure applications remain performant under increasing user loads, such as in web applications handling large and continuously growing<br/> amounts of user data.</p> <p><span style="margin-left: 10px;"></span> Moreover, Big O Notation promotes high code quality by encouraging developers to write clean, efficient, and maintainable code. Well-designed algorithms with optimal complexity not only perform better but also tend to be more readable and easier to maintain, leading to fewer bugs and simpler debugging processes.</p> <p><span style="margin-left: 10px;"></span> In conclusion, Big O Notation serves as a foundational tool for every programmer, providing insights into algorithm efficiency and scalability. By focusing on performance optimization, scalability, resource management, and code quality, developers can create robust, high-performance software capable of meeting the demands of today’s software.</p> <h2 id="common-big-o-notations">Common Big O Notations</h2> <p><span style="margin-left: 10px;"></span> Here are the most commonly encountered Big O Notations, along with their definitions and examples:</p> <table> <thead> <tr> <th style="text-align: left">Complexity</th> <th style="text-align: center">Name</th> <th style="text-align: right">Example</th> </tr> </thead> <tbody> <tr> <td style="text-align: left">O(1)</td> <td style="text-align: center">Constant</td> <td style="text-align: right">Accessing an element in an array</td> </tr> <tr> <td style="text-align: left">O(log n)</td> <td style="text-align: center">Logarithmic</td> <td style="text-align: right">Binary search in a sorted array</td> </tr> <tr> <td style="text-align: left">O(n)</td> <td style="text-align: center">Linear</td> <td style="text-align: right">Iterating through an array</td> </tr> <tr> <td style="text-align: left">O(n log n)</td> <td style="text-align: center">Linearithmic</td> <td style="text-align: right">Efficient sorting algorithms (e.g., mergesort)</td> </tr> <tr> <td style="text-align: left">O(n^2)</td> <td style="text-align: center">Quadratic</td> <td style="text-align: right">Simple sorting algorithms (e.g., bubble sort)</td> </tr> <tr> <td style="text-align: left">O(2^n)</td> <td style="text-align: center">Exponential</td> <td style="text-align: right">Solving the Tower of Hanoi problem</td> </tr> <tr> <td style="text-align: left">O(n!)</td> <td style="text-align: center">Factorial</td> <td style="text-align: right">Generating all permutations of a list</td> </tr> </tbody> </table> <p><span style="margin-left: 10px;"></span> I don’t think it’s necessary to further elaborate on the Big O Notation mentioned above, as they are self-explanatory.</p> <h2 id="importance-in-modern-software-development">Importance in Modern Software Development</h2> <p><span style="margin-left: 10px;"></span> In today’s fast-paced software development landscape, the ability to write efficient code is more critical than ever. With the exponential growth of data and the increasing complexity of operations, the performance of algorithms directly impacts the user experience and the scalability of applications. Big O Notation plays an extremely important role in modern software development by providing a framework for understanding and improving algorithm efficiency.</p> <p><span style="margin-left: 10px;"></span> Real-world applications often deal with vast amounts of data, whether it’s in search engines processing billions of queries per day, social media platforms managing terabytes of user-generated content, or financial systems executing thousands of transactions per second. In these scenarios, the choice of algorithms can significantly influence performance. By leveraging algorithms with optimal Big O characteristics, developers can ensure their applications remain performant and responsive under heavy loads.</p> <h2 id="case-studies-and-practical-examples">Case Studies and Practical Examples</h2> <p><span style="margin-left: 10px;"></span> To illustrate the practical applications of Big O Notation, let’s consider these 2 case studies of well-known algorithms that we encounter in everyday software development:</p> <h3 id="sorting-algorithms-quicksort-vs-bubble-sort">Sorting Algorithms: Quicksort vs. Bubble Sort</h3> <h4 id="quicksort">Quicksort:</h4> <ul> <li>Time Complexity of O(n log n) -&gt; on average</li> <li>How It Works: Quicksort is a divide-and-conquer algorithm. It works by selecting a ‘pivot’ element from the array and partitioning the other elements into two sub-arrays, according to whether they are less than or greater than the pivot. The sub-arrays are then sorted recursively.</li> <li>Efficiency: On average, Quicksort is very efficient, dividing the problem in half with each recursive call, leading to an average time complexity of O(n log n). This makes it suitable for large datasets.</li> </ul> <h4 id="bubble-sort">Bubble Sort:</h4> <ul> <li>Time Complexity: O(n^2) -&gt; on average</li> <li>How It Works: Bubble Sort is a simple comparison-based algorithm. It repeatedly steps through the list, compares adjacent elements, and swaps them if they are in the wrong order. This process is repeated until the list is sorted.</li> <li>Efficiency: Bubble Sort performs poorly on large datasets because it makes multiple passes through the list, with each pass having O(n) comparisons and potentially O(n) swaps, resulting in an overall time complexity of O(n^2).</li> </ul> <h4 id="comparison">Comparison:</h4> <p><span style="margin-left: 10px;"></span> For a dataset with 10,000 elements, Quicksort would, on average, take about 10,000 * log(10,000) = 10,000 * 4 = 40,000 operations.</p> <p><span style="margin-left: 10px;"></span> Bubble Sort, on the other hand, would take approximately 10,000 * 10,000 = 100,000,000 operations.</p> <p><span style="margin-left: 10px;"></span> According to this article <a href="https://mertmetin-1.medium.com/introduction-a12c801927a2">Bubble Sort Vs Quick Sort Algorithms</a>, as the dataset grows, the difference in performance becomes more pronounced. This example highlights why choosing Quicksort over Bubble Sort can lead to significant performance improvements for large datasets.</p> <h3 id="searching-algorithms-binary-search-vs-linear-search">Searching Algorithms: Binary Search vs. Linear Search</h3> <p><span style="margin-left: 10px;"></span> Searching is another common operation where algorithm choice is crucial for performance. Let’s compare Binary Search and Linear Search.</p> <h4 id="binary-search">Binary Search:</h4> <ul> <li>Time Complexity: O(log n)</li> <li>How It Works: Binary Search requires the array to be sorted. It works by repeatedly dividing the search interval in half. If the value of the search key is less than the item in the middle of the interval, it narrows the interval to the lower half. Otherwise, it narrows it to the upper half. The process continues until the value is found or the interval is empty.</li> <li>Efficiency: Binary Search is highly efficient for large datasets because it reduces the problem size by half with each step. This logarithmic time complexity makes it much faster than linear search for large arrays.</li> </ul> <h4 id="linear-search">Linear Search:</h4> <ul> <li>Time Complexity: O(n)</li> <li>How It Works: Linear Search scans each element of the array sequentially until the desired value is found or the end of the array is reached.</li> <li>Efficiency: Linear Search performs well for small arrays or unsorted data but becomes inefficient as the size of the dataset increases because each element must be checked, resulting in a linear time complexity.</li> </ul> <h4 id="comparison-1">Comparison:</h4> <p><span style="margin-left: 10px;"></span> For a dataset with 1,000,000 elements, Binary Search would take log(1,000,000) ≈ 20 comparisons. Linear Search, in the worst case, would take up to 1,000,000 comparisons. As the dataset grows, Binary Search’s efficiency becomes even more significant. For instance, doubling the size of the dataset to 2,000,000 elements would only increase Binary Search comparisons to 21, while Linear Search comparisons would double to 2,000,000.</p> <p><span style="margin-left: 10px;"></span> By analyzing this study <a href="https://www.geeksforgeeks.org/linear-search-vs-binary-search/">Linear Search vs Binary Search</a> we can understand the importance of Big O Notation to select and implement the most efficient algorithms for specific tasks.</p> <h2 id="big-o-notation-for-developers">Big O Notation for Developers</h2> <p><span style="margin-left: 10px;"></span> Big O Notation is a fundamental concept in computer science that every developer should understand. It provides a high-level understanding of the efficiency of algorithms by describing their time and space complexity. For developers, understanding Big O Notation is crucial for writing efficient, scalable, and high-performance code.</p> <p><span style="margin-left: 10px;"></span> Java developers often deal with a variety of data structures and algorithms provided by the Java Collections Framework. Understanding the Big O Notation of these data structures and algorithms helps in making informed decisions when writing and optimizing code.</p> <h3 id="comparison-table-of-java-data-structures">Comparison Table of Java Data Structures</h3> <table> <thead> <tr> <th>Data Structure</th> <th>Access Time</th> <th>Insertion Time</th> <th>Deletion Time</th> <th>Order Preserved</th> <th>Best Use Case</th> </tr> </thead> <tbody> <tr> <td><strong>ArrayList</strong></td> <td>O(1)</td> <td>O(n)</td> <td>O(n)</td> <td>No</td> <td>Random access</td> </tr> <tr> <td><strong>LinkedList</strong></td> <td>O(n)</td> <td>O(1) at ends</td> <td>O(1) at ends</td> <td>No</td> <td>Frequent insertions/deletions at ends</td> </tr> <tr> <td><strong>HashSet</strong></td> <td>O(1)</td> <td>O(1)</td> <td>O(1)</td> <td>No</td> <td>Unique elements with fast lookups</td> </tr> <tr> <td><strong>LinkedHashSet</strong></td> <td>O(1)</td> <td>O(1)</td> <td>O(1)</td> <td>Yes</td> <td>Unique elements with insertion order maintained</td> </tr> <tr> <td><strong>TreeSet</strong></td> <td>O(log n)</td> <td>O(log n)</td> <td>O(log n)</td> <td>Yes (Sorted)</td> <td>Sorted unique elements</td> </tr> <tr> <td><strong>HashMap</strong></td> <td>O(1)</td> <td>O(1)</td> <td>O(1)</td> <td>No</td> <td>Fast key-value lookups</td> </tr> <tr> <td><strong>LinkedHashMap</strong></td> <td>O(1)</td> <td>O(1)</td> <td>O(1)</td> <td>Yes</td> <td>Fast lookups with insertion order maintained</td> </tr> <tr> <td><strong>TreeMap</strong></td> <td>O(log n)</td> <td>O(log n)</td> <td>O(log n)</td> <td>Yes (Sorted)</td> <td>Sorted key-value pairs</td> </tr> </tbody> </table> <p><span style="margin-left: 10px;"></span> When choosing a data structure, Java developers need to consider the typical operations their application will perform. For example, if fast lookups are crucial, a HashMap might be the best choice. However, if ordered data is needed, a TreeMap or LinkedList might be more appropriate despite their higher access times.</p> <h3 id="comparison-table-of-java-sorting-algorithms">Comparison Table of Java Sorting Algorithms</h3> <table> <thead> <tr> <th>Sorting Algorithm</th> <th>Time Complexity (Best)</th> <th>Time Complexity (Average)</th> <th>Time Complexity (Worst)</th> <th>Space Complexity</th> <th>Best Use Case</th> </tr> </thead> <tbody> <tr> <td><strong>Collections.sort()</strong> (Timsort)</td> <td>O(n)</td> <td>O(n log n)</td> <td>O(n log n)</td> <td>O(n)</td> <td>For general-purpose sorting with objects</td> </tr> <tr> <td><strong>Arrays.sort()</strong> (Dual-Pivot Quicksort)</td> <td>O(n log n)</td> <td>O(n log n)</td> <td>O(n^2)</td> <td>O(log n)</td> <td>For sorting arrays of primitive types</td> </tr> <tr> <td><strong>Quicksort</strong></td> <td>O(n log n)</td> <td>O(n log n)</td> <td>O(n^2)</td> <td>O(log n)</td> <td>Fast in practice for large datasets</td> </tr> <tr> <td><strong>Mergesort</strong></td> <td>O(n log n)</td> <td>O(n log n)</td> <td>O(n log n)</td> <td>O(n)</td> <td>Linked lists or when stability is crucial</td> </tr> </tbody> </table> <p><span style="margin-left: 10px;"></span> Sorting algorithms are another critical area where Big O Notation comes into play. Java’s Collections.sort() method uses Timsort, which has a time complexity of O(n log n). Knowing this helps developers understand that the sorting operation will scale efficiently with the size of the data.</p> <p><span style="margin-left: 10px;"></span> For backend web developers, efficiency and scalability are paramount. Understanding Big O Notation helps in optimizing server-side code to handle increasing loads and large datasets.</p> <h4 id="database-queries">Database Queries:</h4> <p>Understanding the time complexity of database operations is essential. For instance, indexing can reduce query time complexity from O(n) to O(log n). However, too many indexes can increase the time complexity of insertions and updates.</p> <h4 id="api-response-times">API Response Times:</h4> <p>Optimizing algorithms that process data before sending responses can significantly impact API performance. For example, filtering and sorting operations on server-side collections should use efficient algorithms to ensure fast response times.</p> <h4 id="caching-strategies">Caching Strategies:</h4> <p>Caching frequently accessed data can reduce the time complexity of retrieval operations from O(n) to O(1). Implementing efficient cache eviction practices can also be analyzed using Big O Notation to ensure optimal performance.</p> <p><span style="margin-left: 10px;"></span> When designing APIs, backend developers need to be mindful of the algorithms they use for data processing. For example, an API endpoint that sorts a large list of users should use a sorting algorithm with O(n log n) complexity rather than O(n^2).</p> <p><span style="margin-left: 10px;"></span> In addition, understanding the space complexity of data structures can help in managing memory usage effectively. For example, using a linked list for a large dataset might lead to high memory overhead due to the storage of pointers, whereas an array-based list might be more memory-efficient.</p> <h2 id="conclusion">Conclusion</h2> <p><span style="margin-left: 10px;"></span> In the world of computer science and software development, mastering Big O Notation is like having a superpower. It empowers developers to make informed decisions about the efficiency and scalability of their algorithms, which is critical in our data-driven and performance-conscious era.</p> <p><span style="margin-left: 10px;"></span> By understanding Big O Notation, you gain insights into how algorithms perform as the size of the input grows. This understanding allows you to optimize your code, ensuring that your applications run faster and handle larger datasets more gracefully. When working on a small personal project, Big O notation might not make much difference, but in a massive enterprise system, choosing the right algorithm can mean the difference between a smooth user experience and a sluggish, unresponsive application.</p> <p><span style="margin-left: 10px;"></span> The examples of Quicksort versus Bubble Sort and Binary Search versus Linear Search illustrate just how crucial algorithm selection can be. Quicksort’s O(n log n) time complexity makes it a go-to for sorting large datasets efficiently, while Bubble Sort’s O(n^2) complexity shows why it’s best left to educational purposes rather than real-world applications. Similarly, Binary Search’s O(log n) complexity highlights its effectiveness for large, sorted datasets compared to the O(n) linear search.</p> <p><span style="margin-left: 10px;"></span> For developers, understanding the Big O complexities of common data structures and algorithms, especially those in the Java Collections Framework, is indispensable. This knowledge helps in selecting the right tool for the job, whether it’s an ArrayList for fast access, a HashMap for quick lookups, or a TreeMap for ordered data.</p> <p><span style="margin-left: 10px;"></span> Backend developers, too, benefit immensely from this knowledge. Efficient database querying, speedy API responses, and effective caching strategies all hinge on the principles of Big O Notation. By optimizing server-side algorithms, you can handle increasing loads and large datasets with ease, ensuring your application remains responsive and performant.</p> <p><span style="margin-left: 10px;"></span> In short, Big O Notation is more than just a theoretical concept; it’s a practical tool that every developer should have in their toolkit. It promotes high-quality code that’s not only efficient and scalable but also maintainable and robust. So, next time you’re writing or reviewing code, take a moment to consider the Big O implications. Feel free to check out these cheat sheets for a quick reference to common Big O complexities! <a href="https://www.bigocheatsheet.com/">https://www.bigocheatsheet.com/</a> and <a href="https://gist.github.com/marcinjackowiak/85f144d0f1ed5fd066d4d2a34961497c">https://gist.github.com/marcinjackowiak/85f144d0f1ed5fd066d4d2a34961497c/</a></p>]]></content><author><name></name></author><summary type="html"><![CDATA[Big O Notation and code efficiency.]]></summary></entry><entry><title type="html">The Rise of Reactive Programming with Spring WebFlux - A Game Changer or Overkill?</title><link href="https://p-almeida12.github.io/blog/2023/reactive-spring-webflux/" rel="alternate" type="text/html" title="The Rise of Reactive Programming with Spring WebFlux - A Game Changer or Overkill?"/><published>2023-10-18T15:59:00+00:00</published><updated>2023-10-18T15:59:00+00:00</updated><id>https://p-almeida12.github.io/blog/2023/reactive-spring-webflux</id><content type="html" xml:base="https://p-almeida12.github.io/blog/2023/reactive-spring-webflux/"><![CDATA[<p><span style="margin-left: 10px;"></span>Reactive programming has sparked a revolution in the realm of software development. With its capacity to efficiently handle large-scale, highly concurrent systems, it has become the go-to approach for building modern, responsive applications. In the world of Java development, Spring WebFlux has emerged as a formidable framework, offering developers the tools they need to harness the power of reactive programming.</p> <p><span style="margin-left: 10px;"></span>In this article, we embark on a journey through the landscape of reactive programming and its impact on the world of Java development. We’ll delve into the core principles that define reactive programming and explore how it transforms the way we approach application design. We will focus on Spring WebFlux, the driving force behind reactive Java applications, as we examine its capabilities and shed light on why it has gained momentum in recent years.</p> <h2 id="understanding-reactive-programming">Understanding Reactive Programming</h2> <p><span style="margin-left: 10px;"></span>Right now, in the IT community, the term “Reactive” is getting a little bit overloaded/abused, so according to the Reactive Manifesto (https://www.reactivemanifesto.org), the reactive systems are Responsive, Resilient, Elastic and Message Driven. The term “reactive” in the context of programming models revolves around the idea of reacting to change, such as components responding to various events like I/O or user input. Feel free to read the manifesto for a better understanding of this concepts.</p> <p><span style="margin-left: 10px;"></span>Complex systems are built from smaller, interconnected components, and their behavior hinges on the responsiveness of these constituent elements. This implies that Reactive Systems employ a set of design principles to ensure that these responsive properties are consistently exhibited across all levels of scale within the system, enabling seamless composability. In essence, every building block, whether large or small, is designed to exhibit reactive characteristics, allowing them to work in harmony and combine effectively to create a cohesive and adaptable system.</p> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <img src="/assets/img/post1_1.png" class="img-fluid rounded z-depth-1" width="auto" height="auto" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> <div class="caption"> Figure 1 - Reactive System (https://www.reactivemanifesto.org) </div> <p><span style="margin-left: 10px;"></span>Now that we have a better understanding of what the term “Reactive” really stands for, we can dive into Reactive Programming. Reactive programming is an implementation technique, a tool, that focuses on non-blocking, asynchronous execution, a key characteristic of Reactive Systems.</p> <p><span style="margin-left: 10px;"></span>Reactive programming is an asynchronous programming paradigm focused on streams of data an events in a non-blocking way. According to Gerad Berry, a French Computer Scientist, these programs also maintain a continuous interaction with their environment, not the program itself. Interactive programs work at their own pace, while reactive programs only work in response to external demands. That being said, the first one, tends to block threads, leading to performance bottlenecks and decreased system responsiveness. In contrast, the second one, uses non-blocking I/O operations, allowing applications to handle more requests concurrently with a faster response time.</p> <p><span style="margin-left: 10px;"></span>The Non-blocking concept is very important. In Blocking, the code will stop and wait for more data (ie reading from disk, network, etc). Non-blocking in contrast, will process available data, ask to be notified when more is available and then continue. In the images below, we can have a better understanding of what Non-blocking consists.</p> <div class="row mt-3"> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <img src="/assets/img/post1_2.png" class="img-fluid rounded z-depth-1" width="auto" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <img src="/assets/img/post1_3.png" class="img-fluid rounded z-depth-1" width="auto" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> </div> <div class="caption"> Figure 2 - Multi Thread Server (https://javarevisited.blogspot.com/2017/03/why-we-use-threads-in-java.html) and Figure 3 - Node.js Server (https://strongloop.com/strongblog/node-js-is-faster-than-java/) </div> <p><span style="margin-left: 10px;"></span>Java developers always have been pretty comfortable using Multi Threaded Servers, where we have a thread pool that take a given number os requests. When a given thread receives a request, it will get suspended while is waiting for a resource. This starting and stopping of the threads is pretty common and Java handles it very efficiently, so its not necessarily a bad programming paradigm and it has been around for a long time.</p> <p><span style="margin-left: 10px;"></span>In contrast, we have the Node.js server that uses different techniques to manage the threads. In the Figure 3 above, a request comes in and we have a single thread in loop to the the work, that delegates the requests to async threads as they come in. As we can see we have no waiting threads, at all times all the threads are processing, which can improve efficiency and performance. But this isn’t properly a good thing, imagine that something on the event loop takes a lot of CPU, that can really bring down Node.js performance, not to talk about the callback hell that Node.js is known for. It is a significantly different paradigm because your requests are expected to complete quickly on that event loop and get off of it.</p> <p><span style="margin-left: 10px;"></span>Another feature of reactive programming is the Back Pressure. Back Pressure is the ability of the subscriber to throttle data, to help avoid issues as buffering and blocking. Throttling is the practice of controlling the quantity of data that can be exchanged between two systems within a specific time frame. Typically, this is implemented to avoid one system overwhelming another with an excessive data load or to ensure that an individual user does not excessively deplete a shared resource. The failures are also a little different as well, since exceptions are not thrown in a traditional sense, since it would break the stream. Exceptions are processed by a handler function.</p> <h2 id="what-is-spring-webflux">What is Spring WebFlux?</h2> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <img src="/assets/img/post1_4.jpg" class="img-fluid rounded z-depth-1" width="auto" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> <div class="caption"> Figure 4 - Spring WebFlux (https://blog.onesaitplatform.com/en/2022/07/13/reactive-programming-spring-webflux/) </div> <p><span style="margin-left: 10px;"></span>WebFlux is a reactive programming model for building web applications and is a part of the Spring Framework ecosystem. Based on the documentation, it was created with two primary needs: Concurrency and Resource Efficiency and Functional Programming. It is built on top of the Reactive Streams API, which provides a model to build web applications on the JVM.</p> <p><span style="margin-left: 10px;"></span>Reactor is the reactive library for Spring WebFlux. It offers the Mono and Flux API types to work with data sequences of 0 to 1 and 0 to many, using a comprehensive set of operators aligned with the ReactiveX operator vocabulary. As a Reactive Streams library, Reactor ensures support for non-blocking back pressure.</p> <p><span style="margin-left: 10px;"></span>Spring WebFlux has two main components: the RouterFunction and the HandlerFunction. The RouterFunction serves the purpose of associating incoming requests with their corresponding HandlerFunction. The role of the HandlerFunction is to manage the request and provide a response. The objective of the Reactive Stream API is to create a standard for asynchronous stream processing with a non-blocking back pressure. There are two main components: the Publisher and the Subscriber. The publisher emits the data and the subscriber consumes it.</p> <p><span style="margin-left: 10px;"></span>For instance, a data repository acting as a Publisher can produce data, while an HTTP server acting as a Subscriber can write this data to the response. The primary purpose of Reactive Streams is to enable the subscriber to control the pace at which the publisher produces data, ensuring a balanced flow of information.</p> <h2 id="spring-webflux--spring-mvc">Spring WebFlux &amp; Spring MVC</h2> <p><span style="margin-left: 10px;"></span>I think by now we can have a pretty good idea where both of these paradigms differ. Although they’re both designed to build web applications, one uses a blocking approach and the other one uses a non-blocking approach. Spring Web MVC traditionally incorporates the servlet API and operates within a servlet container, which are by nature blocking as well as JDBC and JPA. In Spring WebFlux, the web server does not use the servlet container, because it is using a new stack underneath that. Although Spring made a really good job keeping it abstract to us developers, we must keep in mind that Web MVC components are blocking!</p> <p><span style="margin-left: 10px;"></span>But the real question is: which one is better? Is Spring WebFlux that good has it seems to be? And the answer is simple, it depends. I know, cliché, but its my honest opinion and a lot of times, especially in Software Engineering, is actually true. We can’t choose one over the other based only on this, we need to be skeptical and understand what are the needs of the program we are about to develop. That being said, let’s take a look at where and when one paradigm can be better than the other.</p> <h3 id="spring-webflux">Spring WebFlux</h3> <p><span style="margin-left: 10px;"></span>Spring WebFlux is ideal for applications that need to handle a large number of concurrent requests and require high scalability. For example, a high-traffic e-commerce website, that needs to handle a large number of concurrent requests. If your application deals with streams of data or events, such as real-time updates, IoT devices, or message-driven architectures, WebFlux enables handling of asynchronous operations and provides built-in support for WebSocket communication. It’s also really good with microservices and if your team is into functional programming, it will be worth your while for sure!</p> <h3 id="spring-mvc">Spring MVC</h3> <p><span style="margin-left: 10px;"></span>Spring MVC is a suitable choice when your application primarily handles synchronous request processing and doesn’t require the high concurrency and scalability benefits of reactive programming. Spring MVC has a widely adopted and mature ecosystem, making it a dependable choice for many applications. The existence of heavy dependency on Spring MVC libraries or if the project relies on blocking I/O operations, migrate it to Spring WebFlux can be tricky. It really comes down to the needs of your existent program and the experience of the team you’re working with, sometimes Spring MVC is the way to go.</p> <h2 id="performance-considerations">Performance Considerations</h2> <p><span style="margin-left: 10px;"></span>In my GitHub profile, I have repositories where I used Spring MVC and where I used Spring WebFlux and thats the reason I’m writing this article, to share my journey and what I could learn while doing it. So, if we take into consideration the performance of both approaches, we can take this example of my projects.</p> <p><span style="margin-left: 10px;"></span>Let’s consider a scenario where we need to handle multiple concurrent requests for fetching data from a remote service. We’ll compare the performance of Spring WebFlux and Spring MVC in this situation.</p> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <img src="/assets/img/post1_6.png" class="img-fluid rounded z-depth-1" width="auto" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> <div class="caption"> Figure 4 - Spring WebFlux example. </div> <div class="col-sm mt-3 mt-md-0"> <figure> <picture> <img src="/assets/img/post1_7.png" class="img-fluid rounded z-depth-1" width="auto" height="auto" data-zoomable="" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> </div> <div class="caption"> Figure 5 - Spring Web MVC example. </div> <p><span style="margin-left: 10px;"></span>In the WebFlux example, when a non-blocking request is made, the server thread efficiently manages other incoming requests while it awaits the response. This capability empowers the server to handle a larger number of concurrent requests, all with a reduced need of numerous threads. This results in a significant enhancement in scalability, as the server can efficiently serve multiple clients simultaneously.</p> <p><span style="margin-left: 10px;"></span>On the other hand, in the MVC example, synchronous requests are processed, and the server thread remains engaged until the response is received. This conventional approach poses limitations on the number of concurrent requests the server can effectively manage. Consequently, this method may experience performance degradation when subjected to high workloads, as the server’s resources can become bottlenecked due to the blocking nature of request processing.</p> <p><span style="margin-left: 10px;"></span>According to a <a href="https://medium.com/deno-the-complete-reference/spring-boot-vs-spring-webflux-performance-comparison-for-hello-world-case-386da4e9c418">Medium</a> post, where they compare WebFlux to Web MVC, the Spring Boot (threadpool), Spring WebFlux demonstrates noticeable performance advantages, particularly in terms of requests per second and response times. It achieves approximately double the RPS compared to Spring Boot while maintaining a similar resource cost. The performance characteristics differ based on concurrency levels. At lower concurrency levels, Spring WebFlux exhibits superior median response times. However, at higher levels of concurrency, Spring Boot outperforms Spring WebFlux. Feel free to read their post and analyze their research.</p> <p><span style="margin-left: 10px;"></span>But keep in mind, reactive and non-blocking programming may not inherently make applications run faster. They can enhance performance in certain scenarios, such as when using WebClient for parallel remote calls, but typically, they entail more complex implementation and may slightly increase processing time. Never the less, their primary advantage lies in the ability to scale effectively with a fixed number of threads and reduced memory usage. This scalability improves application resilience under heavy loads, offering more predictable performance. The true benefits of reactive and non-blocking become apparent in situations with latency, including slow and unpredictable network, where the reactive approach excels and can lead to significant performance improvements.</p> <h2 id="the-final-verdict">The Final Verdict</h2> <p><span style="margin-left: 10px;"></span>The moment we were all waiting for: the verdict. At least mine. After reading multiple posts of other fellow developers and enthusiasts, some think that Reactive Programming isn’t that big of a deal and others think it will take Java to the next level. Throughout this section I will try to be as impartial as possible, so we can actually take something from this and make up our own minds.</p> <p><span style="margin-left: 10px;"></span>Let’s start with the concept of Non-Blocking, if we need to make a call, then block to wait for the response and then handle the response because we need that same thread, we can’t and we’re not using Reactive, because we can get into an event loop and stop the application. What I’m trying to say is that everything that uses ThreadLocal, the Java special class that allows us to store data that will be accessible only by a specific thread, is not compatible with reactive. You might think “That isn’t that big of a deal, just don’t use it!”, well I’m afraid that sometimes it might not be possible. Like I said before, JDBC, for example, is intrinsically blocking, its not something we can switch on and off. It is dependent of the ThreadLocal to allow rollbacks, because it holds the transaction, and if we’re working with old JDBC databases, migration can be painful (trust me). The database driver must use the R2DBC spec in order to work with non-blocking calls. One important thing to mention, that sometimes can go unnoticed, is fact that REST calls aren’t blocking, because, in Spring WebClient, they’re not thread-dependent, meaning that the request thread doesn’t have to be the response thread.</p> <p><span style="margin-left: 10px;"></span>If f your application is already handling loads efficiently using traditional MVC or by resorting to horizontal scaling, it may not be the most prudent business decision to opt for WebFlux. In such cases, the additional complexity and resource allocation required by WebFlux might outweigh the potential benefits it offers, making it less compelling from a cost-benefit perspective. So reactive and non-reactive paradigms cannot be mixed, which means that the application has to be reactive end to end. It’s not impossible, but integrating blocking code into a reactive pipeline can be challenging for what I could gather and is an anti-pattern approach for me. Not to talk about debugging, the process that we learnt in the first day of college can become quite hard if the pipeline isn’t correctly built.</p> <p><span style="margin-left: 10px;"></span>Apart from the performance benefits, I found it cognitively complex, hard to read nested code, hard to debug and divides the ecosystem efforts. It’s hard enough to keep complicated synchronous code working and adding the complexity of these non-blocking apis only further complicates already complicated code. This could have been the projects issue. By harder to maintain I mean adding certain features. Sometimes this required to rewrite whole chains instead of simply adding to the existing code. Plain imperative sequential code is much much easier to reason about, and therefore maintain and evolve, than reactive code. This can lead to a team spending more time having to fix technical issues than they are adding business value and for someone who doesn’t have that much experience like me, the code can be unreadable, unlike other approaches that even though we’re not used to we can get around.</p> <p><span style="margin-left: 10px;"></span>As i was scavenging the internet, I began to understand that almost everyone doesn’t really need reactive, they think it will be the savior and it will resolve all their performance problems. Well that’s most definitely not true, WebFlux is used for very specific needs where the advantages surpass the downsides. And with the rise of Loom Project, these APIs begin to stay behind. I strongly recommend you to check out the Loom project, it can be a game changer. While I remain skeptical that Loom will serve as a silver bullet solution for these issues, I do hold the hope that the adoption of more linear control flows will enhance the readability and writability of code. There’s a certain comfort in following the familiar path of writing straightforward imperative code. Additionally, with the impending arrival of Virtual Threads, the value proposition of transitioning to a reactive approach appears to be somewhat diminished. These advancements lead me to question whether the adoption of reactive programming is as compelling as it once was.</p> <p><span style="margin-left: 10px;"></span>In a nutshell, if you find yourself in a position where you are willing to make a trade-off, sacrificing a degree of simplicity in exchange for the potential performance enhancements, then embracing reactive programming could be a strategic decision that aligns with the specific needs and goals of your project. It’s an approach that allows you to harness the power of asynchronous, non-blocking operations to cater to high-concurrency scenarios, thereby unlocking new possibilities for your application’s scalability and responsiveness.</p>]]></content><author><name></name></author><summary type="html"><![CDATA[Reactive Programming and Spring WebFlux]]></summary></entry></feed>